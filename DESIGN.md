# DESIGN.md — ระบบดีไซน์ GUI (brand-forward)

ออกแบบใหม่ ส.ค. 2569 ตามโจทย์เจ้าของ: *"สวยงาม ใช้งานง่าย อ่านง่าย — ไม่เอาแบบจืด ๆ เหมือน template AI"*
หลักการใหญ่: **สีแบรนด์ของหุ้นแต่ละตัวคุมทั้งหน้า** (brand-forward) และ **การตกแต่งทุกอย่าง inject ตอน build**
— ไฟล์รายงานทั้ง 900+ ใบเป็น content-only ไม่ถูกแตะเพื่อเรื่องดีไซน์แม้แต่ไฟล์เดียว

> spec ฉบับเต็ม (พร้อมผลวัด contrast/mobile ทุกข้อ): `docs/superpowers/specs/2026-08-11-gui-redesign-design.md`
> แผน implement 7 tasks: `docs/superpowers/plans/2026-08-11-gui-redesign.md`

---

## 1. Typography

| บทบาท | ฟอนต์ | หมายเหตุ |
|---|---|---|
| หัวเรื่อง/ตัวเลขใหญ่ (`--display`) | **Sarabun** 600–800 | ★ font เดิมของ production — เจ้าของสั่งถอด Kanit กลับ 12 ส.ค. 69 · **ห้ามเปลี่ยน typeface โดยไม่ถามเจ้าของ** |
| เนื้อความ | Sarabun 300–400 | จอเล็ก ≤760px ยกน้ำหนักรอง 300→400 (Sarabun บางอ่านยากบนมือถือ) |
| ตัวเลข/label กำกับ (`--monoff`) | IBM Plex Mono 500–600 | `tabular-nums` เสมอ · label ตัวพิมพ์ใหญ่ letter-spacing .11–.14em |

## 2. สถาปัตยกรรมสี (brand-forward)

ต่อหุ้นมีสีตั้งต้น 2 ค่าใน `report-data.theme`: `accent` + `accentDark` (คุมโดย `tools/seeds.json` + `pick-brand.js`)
ที่เหลือ **derive ตอน build** ด้วย `deriveTheme()` ใน `build.js` (สูตรผ่าน `bt.mixHex`):

| โทเคน | สูตร | ใช้กับ |
|---|---|---|
| `tintBg` | mix(accent 7%, `#f4f5f7`) | พื้นหลังทั้งหน้า (ย้อมสีแบรนด์จาง ๆ) |
| `tintCard` | mix(accent 4%, `#fff`) | พื้นการ์ด |
| `line` / `line2` | mix 14% / 26% | เส้นขอบ |
| `soft` | mix(accent 10%, `#fff`) | พื้นป้ายอ่อน — ★ 13% ตก AA (GNRC/HLI) ห้ามเพิ่ม |
| `shadow`/`shadowLg` | เงา rgba ของ accent | เงาการ์ด |

**กฎ contrast (วัดจริงทั้ง 908 ธีม · gate E38 คุม 4 คู่ derive):**
- ตัวหนังสือขาววางได้บน `accentDark`/`--badge` เท่านั้น — **ห้ามวางบน accent ดิบ** (ตก AA 386/908 ธีม)
- `muted` = `#5f6675` (`#6b7280` ตก 13/905) · ทุกคู่ตัวหนังสือ ≥4.5:1 · เส้นกราฟ ≥3:1

## 3. หน้าแรก (index — สร้างใน `build.js`)

- **Header**: ไล่โทนน้ำเงินเข้ม 135° (`#12141a → #27354f`) + glow ฟ้ามุมขวาบน/ส้มมุมซ้ายล่าง (ห้ามดำสนิท — เจ้าของ 12 ส.ค. 69) · แถบสเปกตรัมบนสุด = สีแบรนด์จริงของทุกหุ้นเรียงตาม hue
- **การ์ดสถิติ** (มุมขวาบน desktop · แถวใต้เส้นคั่นบน mobile ≤820): ตัวเลข 908/238/670 เป็น **ปุ่มกรองตลาดในตัว** (ทั้งหมด/ไทย/สหรัฐ + `aria-pressed`) — แทนแถบกรองแยกที่ซ้ำซ้อน
- **ไทล์**: หัวการ์ดสี `accentDark` + วงแสง accent · ชิปไฮไลต์บรรทัดเดียวกับชื่อย่อ (ที่ไม่พอ → ย่อเหลือตัวเลข) · footer บรรทัดเดียว `รายงาน → · 👁 👍 · 1d ago` (ไม่มี dislike)
- **ตาราง** (toggle ได้ทุกความกว้าง): หัวตารางไล่โทนเข้ม + ▾ คอลัมน์ที่เรียง · **คลิกหัวคอลัมน์เรียงได้** · zebra จาก JS ตามแถวที่มองเห็น (ห้าม `nth-child` — นับแถวที่ถูกกรองซ่อน) · MOS/Upside เขียว/แดงตามเครื่องหมาย (คลาส `pos`/`neg` ติดตอน build) · จุดสีแบรนด์หน้าชื่อย่อ · 25 แถว/หน้า (ไทล์ 12)
- **มือถือ**: ตารางเลื่อนข้างได้ + ป้ายบอก (ซ่อนถาวรเมื่อผู้ใช้เลื่อนเอง) + scrollbar มองเห็นตลอด
- **วันที่ทุกจุดบนการ์ด/ตาราง = "1d ago"** (คำนวณฝั่งผู้ชม · no-JS เห็นวันจริง · hover ดูวันเต็ม)
- footer หน้าแรก = 3 บรรทัดเดิมครบ (อัปเดต/ติดต่อ · เครดิต AI · disclaimer) แต่เครดิต AI เป็น **"Claude" แบบ generic ห้ามลิสต์ชื่อรุ่นโมเดล** (เจ้าของสั่ง 12 ส.ค. 69 — รุ่นจริงระบุใน footer ของแต่ละรายงานอยู่แล้ว)

## 4. หน้ารายงาน (inject ใน `decorateReport()`)

- **Header**: `--dark-grad` ไล่โทนจากสีแบรนด์ + glow · ราคาเป็นพระเอก (Sarabun 800)
- **การ์ดสถิติ** (`injectHeaderStats` — มุมขวาบน ≥901px · แถวท้ายโซน header ≤900px รวม iPad):
  แถวบน = 👁 ยอดดู · อัปเดต "1d ago" / แถวล่าง = 👍 ถูกใจ · 👎 ไม่ถูกใจ (**กดโหวตได้จริง** — ใช้ id ชุดเดิมของ counter script) · footer เหลือลิงก์กลับ + ติดต่อ (ตัดตัวนับซ้ำออก)
  ⚠️ ขอบเขต "การ์ดลอย" ต้องตรงกับขอบเขต `padding-right` กันชื่อบริษัทยาวมุดใต้การ์ด (≥901px ทั้งคู่)
- **Section nav**: sticky + scroll-spy สร้างจาก `<h2>` ตอน build (ป้ายย่อผ่าน `NAV_SHORT`) · <3 sections ไม่แสดง
- อีโมจิประดับถูกถอดตอน build เฉพาะ 5 ช่องที่รู้จัก (`EMOJI_SLOTS` — ห้ามกวาดทั้งเอกสาร)

## 5. กติกา mobile (สรุปจาก audit จริง 375px)

- tap target ≥44px ทุกปุ่ม · ไม่มี horizontal overflow ของหน้า (ตารางเลื่อนใน container ตัวเอง)
- **"แถวสองของเบียดกันแนวนอน → ซ้อนแนวตั้ง"** (mos-verdict/vmethod/fv-box) · หัว section: ชื่อเต็มแถว hint ขึ้นบรรทัดใหม่
- การ์ด/องค์ประกอบที่ลอย absolute บน desktop → เปลี่ยนเป็นแถวปกติใต้เส้นคั่น (pattern ที่เจ้าของ approve)

## 6. ข้อห้ามยืนพื้น

1. ห้ามแก้ไฟล์ใน `reports/` เพื่อเรื่องดีไซน์ — ทุกอย่างทำผ่าน `_template/` + inject ใน `build.js`
2. ห้ามเปลี่ยน typeface (ดู §1) · ห้ามตัวหนังสือขาวบน accent ดิบ (ดู §2)
3. class/id เดิม (`.chg .px .sub .gauge` ฯลฯ) เป็น contract ที่ gate/regex ตรวจ — เปลี่ยนชื่อ = แตะ `test/` พร้อมกันเสมอ
4. แก้ dashboard.css/design token แล้วต้อง `npm run verify` เต็ม (E38 วัด contrast ทุกธีมจริง) — และ **restart `wrangler dev`** หลัง build ใหม่ (Miniflare เสิร์ฟ manifest ค้าง)

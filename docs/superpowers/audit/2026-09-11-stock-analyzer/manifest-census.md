# Field manifest — census ทั้งคลัง (ระยะ 1 WS1 ข้อ 1–2)

- **วันที่:** 12 ก.ย. 2569 (เวลาไทย)
- **commit ที่วัด:** **HEAD ของ fix wave ส่วน B** (12 ก.ย. 2569) — วัดใหม่ทั้งตารางหลังรอบแก้จากรีวิวทั้งสาขา
  (รอบแรกวัดที่ `88117675` · ช่องที่ตัวเลขขยับคือ **f12** เท่านั้น — เปลี่ยนตัวอ่านเป็น `findDiscPriceDate` ดู §ผลหลังเปิด)
- **จำนวนไฟล์:** 908 ใบ (`reports/*.html` ทั้งหมด)
- **วิธีวัด:** `node tools/field-manifest.js --census` — เดินทุกไฟล์ผ่าน `expandReport()` แล้ว `buildCtx(html, file)`
  ของ gate จริง (`test/check-reports.js`) แล้วเรียก `extract(html, ctx)` ของทุกช่อง — **ctx เดียวกับที่ E/W code เห็น**
- **จำนวนช่อง: 70** — ไม่ใช่ 68 · code-audit §1.1 เดินเลขแถว #1–#68 แล้ว**แทรก** #25b/#63b ⇒ คำว่า "68 ช่อง"
  ในสเปก/แผนคือ *เลขแถวสูงสุด* ไม่ใช่จำนวนแถว (นับแถวในตารางจริง 12 ก.ย. 69 = 70) — **ห้ามตัดช่องทิ้งให้ครบ 68**

## กติกาตัดสิน `required` (Task 9 ใช้ตารางนี้)

| อัตราที่พบ | ตัดสิน |
|---|---|
| **≥ 99%** ของคลัง | `required: true` — ช่องที่ขาด = ของเสีย |
| **< 99%** | `required: false` (optional) — ไม่มีช่องนี้เป็นเรื่องปกติของรายงานบางใบ |
| **`extractor error` > 0** | **ห้ามตัดสิน** — ต้องแก้ extractor ให้คืน `{found:false}` แทนที่จะ throw ก่อน แล้ววัดใหม่ |

ผลรอบนี้: **`extractor error` = 0 ทุกช่อง** ⇒ ตัดสินได้ทั้งตาราง · แบ่งเป็น **≥99% 35 ช่อง · <99% 35 ช่อง**
(วัดซ้ำที่ HEAD ของ fix wave ส่วน B: เท่าเดิมทุกช่อง — f12 ขยับ 48.9% → 46.4% แต่ยังอยู่ฝั่ง <99% เหมือนเดิม)

> ในไฟล์ manifest ตอนนี้ `required` ยังเป็น `null` (ยังไม่ตัดสิน) ทุกช่อง **ยกเว้น**ช่องที่แผนสั่ง `false` ไว้ล่วงหน้า —
> Task 9 เป็นคนเขียนค่าจริงลงไปพร้อม `checkPairs` + W21/W22/W23

## ★ extractor 3 ตัวที่วัดรอบแรกได้ 0.0% เพราะตัวมันเองผิด (แก้แล้ว ก่อนตารางนี้)

ตัวเลข 0.0% บนช่องที่ code-audit ยืนยันว่ามีอยู่จริง = **การวัดพัง ไม่ใช่ข้อค้นพบ** — ไล่จนเจอเหตุทั้ง 3 ตัว:

| ช่อง | อาการ | เหตุ | หลังแก้ |
|---|---|---|---|
| f11 วันที่ทวนในวงเล็บ | 0.0% | `parsePriceDate()` รับ **HTML ที่ต้องสแกนหา anchor "ราคา"** ไม่ใช่สตริงวันที่เปล่า — ของเดิม slice วันที่ออกมาแล้วส่งเข้าไป ⇒ คืน `null` เสมอ | **0.8% (7 ใบ)** = AZN·CSGP·DPZ·HIG·PFE·PNC·SNNP ตรงกับที่ `price-date.js` ระบุไว้เองเป๊ะ |
| f12 disclaimer "ราคา ณ" | 0.0% | เหตุเดียวกัน (regex ดึงวันที่ออกมาก่อนแล้วค่อยส่ง) | **48.9% (444 ใบ)** → **46.4% (421 ใบ)** หลังเปลี่ยนไปใช้ `findDiscPriceDate` (ดู §ผลหลังเปิด) |
| f63b "จาก ATH −X%" | 0.0% | คลังเขียน **% มาก่อน** (`~−34% จาก ATH`) รูป "จาก ATH −X%" ไม่มีในคลังเลย (0/908) | **1.0% (9 ใบ)** |

แก้ f11/f12 ด้วยการเพิ่ม wrapper ขั้นต่ำ `dateIso(hit)` ใน **โมดูลเจ้าของ** (`tools/price-date.js`) แล้วให้
`parsePriceDate` เรียกใช้ตัวเดียวกัน (พฤติกรรมเดิมไม่เปลี่ยน) — ไม่เขียน regex วันที่ซ้ำใน manifest

## ★ NEITHER 18 แถว → ได้เจ้าของแล้วทั้ง 18 (เกณฑ์จบ "NEITHER 18 → 0")

18 แถวที่ code-audit §1.1 ชี้ว่า "ไม่มี cron เขียน ไม่มี gate ตรวจ" ตอนนี้ผูกกับ manifest ครบทุกแถว —
`pair` = มีคู่ให้เทียบค่า (Task 9 ทำ `checkPairs`) · `presence` = ตรวจว่ามี/ไม่มี (ยังไม่เทียบค่า)

| id | ช่อง | binding | คู่เทียบ |
|---|---|---|---|
| f13 | footer "ข้อมูล ณ" | `presence` | — |
| f23 | การ์ดเป้านักวิเคราะห์ — ราคา | `presence` | — |
| f24 | เป้านักวิเคราะห์บน gauge scale | `pair` | → f23 (money) |
| f25 | rating นักวิเคราะห์ | `presence` | — |
| f25b | จำนวนสำนัก n= | `presence` | — |
| f32 | การ์ด BVPS | `presence` | — |
| f33 | การ์ด EPS (TTM) | `presence` | — |
| f41 | กรอบ 52 สัปดาห์ | `pair` | → f01 (contains, stale) |
| f46 | report-data.chart.fairLine | `pair` | → f44 (money) |
| f47 | legend "มูลค่าเหมาะสม $FV" | `pair` | → f44 (money) |
| f48 | การ์ด "โซนเริ่มทยอยสะสม < $FV" | `pair` | → f44 (money) |
| f49 | ป้าย gauge mFair "เหมาะสม $FV" | `pair` | → f44 (money) |
| f54 | vcell กรอบ (FV_LOW–FV_HIGH) | `pair` | → f43 (range) |
| f55 | การ์ด "P/E เฉลี่ย ~N ปี" | `pair` | → f04 (years, stale) |
| f58 | การ์ดกำไรสุทธิ / YoY | `presence` | — |
| f60 | การ์ดอัตรากำไรขั้นต้น | `presence` | — |
| f61 | การ์ด Beta | `presence` | — |
| f63b | "จาก ATH −X%" ใน prose | `presence` | — |

สรุป: **`pair` 8 แถว · `presence` 10 แถว** — ไม่เหลือแถวที่ไม่มีใครอ่าน
(binding ทั้ง manifest: `cron` 27 · `gate` 17 · `presence` 13 · `pair` 12 · `deferred` 1)

`deferred` 1 แถวคือ f64 (ราคาในร้อยแก้ว) — ตั้งใจเลื่อนไประยะ 2 ตามแผน ไม่ใช่ช่องที่ตกหล่น

## ตาราง census

| id | ช่อง | binding | พบ | อัตรา | extractor error | required |
|---|---|---|---|---|---|---|
| f01 | .px ราคา header | cron | 908/908 | 100.0% | 0 | true |
| f02 | stock-meta.price | cron | 908/908 | 100.0% | 0 | true |
| f03 | report-data.gauge.cur | cron | 908/908 | 100.0% | 0 | true |
| f04 | report-data.chart.data[] | cron | 908/908 | 100.0% | 0 | true |
| f05 | chart.min/max/grid | cron | 908/908 | 100.0% | 0 | true |
| f06 | chart.highlight | cron | 908/908 | 100.0% | 0 | true |
| f07 | gauge.min/max | cron | 908/908 | 100.0% | 0 | true |
| f08 | theme.chgBg/chgColor | cron | 908/908 | 100.0% | 0 | true |
| f09 | .chg ป้าย % รอบปี | cron | 908/908 | 100.0% | 0 | true |
| f10 | วันที่ราคา (px-meta) | cron | 908/908 | 100.0% | 0 | true |
| f11 | วันที่ทวนในวงเล็บ (คนละศักราช) | pair | 7/908 | 0.8% | 0 | false |
| f12 | disclaimer "ราคา ณ" | pair | 421/908 | 46.4% | 0 | false |
| f13 | footer "ข้อมูล ณ" | presence | 896/908 | 98.7% | 0 | false |
| f14 | pxIn value | cron | 908/908 | 100.0% | 0 | true |
| f15 | MOS .big | cron | 908/908 | 100.0% | 0 | true |
| f16 | stock-meta.mos/upside | cron | 908/908 | 100.0% | 0 | true |
| f17 | vcell ส่วนต่างจากราคา — ตัวเลข | cron | 905/908 | 99.7% | 0 | true |
| f18 | vcell ส่วนต่างจากราคา — ข้อความ | cron | 907/908 | 99.9% | 0 | true |
| f19 | class mos-verdict | cron | 908/908 | 100.0% | 0 | true |
| f20 | การ์ด P/E | cron | 558/908 | 61.5% | 0 | false |
| f21 | stock-meta.pe | cron | 831/908 | 91.5% | 0 | false |
| f22 | การ์ดเป้านักวิเคราะห์ — % | cron | 112/908 | 12.3% | 0 | false |
| f23 | การ์ดเป้านักวิเคราะห์ — ราคา | presence | 112/908 | 12.3% | 0 | false |
| f24 | เป้านักวิเคราะห์บน gauge scale | pair | 832/908 | 91.6% | 0 | false |
| f25 | rating นักวิเคราะห์ | presence | 462/908 | 50.9% | 0 | false |
| f25b | จำนวนสำนัก n= | presence | 90/908 | 9.9% | 0 | false |
| f26 | การ์ด Market Cap | cron | 875/908 | 96.4% | 0 | false |
| f27 | จำนวนหุ้น (.d ของ Market Cap) | presence | 875/908 | 96.4% | 0 | false |
| f28 | การ์ด P/S | cron | 13/908 | 1.4% | 0 | false |
| f29 | การ์ดปันผล % | cron | 645/908 | 71.0% | 0 | false |
| f30 | stock-meta.dividendYield | cron | 839/908 | 92.4% | 0 | false |
| f31 | การ์ด P/BV | cron | 530/908 | 58.4% | 0 | false |
| f32 | การ์ด BVPS | presence | 600/908 | 66.1% | 0 | false |
| f33 | การ์ด EPS (TTM) | presence | 841/908 | 92.6% | 0 | false |
| f34 | หมวด 6 ผลตอบแทนรวม % | cron | 795/908 | 87.6% | 0 | false |
| f35 | หมวด 6 %/ปี | cron | 267/908 | 29.4% | 0 | false |
| f36 | หมวด 6 "จากจุดเข้า" | cron | 907/908 | 99.9% | 0 | false |
| f37 | หมวด 6 ราคาเป้า 3 ฉาก | gate | 908/908 | 100.0% | 0 | true |
| f38 | หมวด 6 class ret pos/neg | presence | 908/908 | 100.0% | 0 | false |
| f39 | หมวด 6 ปันผลรวม 3 ปี | presence | 788/908 | 86.8% | 0 | false |
| f40 | หมวด 6 EPS ฐาน | gate | 775/908 | 85.4% | 0 | false |
| f41 | กรอบ 52 สัปดาห์ | pair | 792/908 | 87.2% | 0 | false |
| f42 | บรรทัดที่มา | gate | 908/908 | 100.0% | 0 | true |
| f43 | .fv-box .r FV | gate | 908/908 | 100.0% | 0 | true |
| f44 | report-data.fv (const FV) | gate | 908/908 | 100.0% | 0 | true |
| f45 | report-data.gauge.fair | pair | 908/908 | 100.0% | 0 | true |
| f46 | report-data.chart.fairLine | pair | 908/908 | 100.0% | 0 | true |
| f47 | legend "มูลค่าเหมาะสม $FV" | pair | 905/908 | 99.7% | 0 | true |
| f48 | การ์ด "โซนเริ่มทยอยสะสม < $FV" | pair | 898/908 | 98.9% | 0 | false |
| f49 | ป้าย gauge mFair "เหมาะสม $FV" | pair | 906/908 | 99.8% | 0 | true |
| f50 | ป้าย gauge mCur "ปัจจุบัน $px" | pair | 908/908 | 100.0% | 0 | true |
| f51 | gauge scale MOS20/MOS30 | gate | 908/908 | 100.0% | 0 | true |
| f52 | การ์ดจุดซื้อ MOS20/MOS30 | gate | 908/908 | 100.0% | 0 | true |
| f53 | gauge scale กรอบบน FV | gate | 774/908 | 85.2% | 0 | false |
| f54 | vcell กรอบ (FV_LOW–FV_HIGH) | pair | 899/908 | 99.0% | 0 | true |
| f55 | การ์ด "P/E เฉลี่ย ~N ปี" | pair | 514/908 | 56.6% | 0 | false |
| f56 | การ์ด ROE / ROA | gate | 745/908 | 82.0% | 0 | false |
| f57 | stock-meta.roe | gate | 790/908 | 87.0% | 0 | false |
| f58 | การ์ดกำไรสุทธิ / YoY | presence | 760/908 | 83.7% | 0 | false |
| f59 | การ์ดรายได้ TTM | gate | 866/908 | 95.4% | 0 | false |
| f60 | การ์ดอัตรากำไรขั้นต้น | presence | 411/908 | 45.3% | 0 | false |
| f61 | การ์ด Beta | presence | 508/908 | 55.9% | 0 | false |
| f62 | .mval ต่อวิธี | gate | 908/908 | 100.0% | 0 | true |
| f63 | .mdesc ตัวคูณ | gate | 908/908 | 100.0% | 0 | true |
| f63b | "จาก ATH −X%" ใน prose | presence | 9/908 | 1.0% | 0 | false |
| f64 | ราคาใน prose | deferred | 798/908 | 87.9% | 0 | false |
| f65 | % ของราคาเป้าใน prose | gate | 379/908 | 41.7% | 0 | false |
| f66 | meta ai-model | gate | 908/908 | 100.0% | 0 | true |
| f67 | .sub คำโปรย | gate | 908/908 | 100.0% | 0 | true |
| f68 | tags (sidecar) | gate | 908/908 | 100.0% | 0 | false |

## ผลหลังเปิด W21/W22/W23 (Task 9 — วัด 12 ก.ย. 2569)

`required` ตั้งตามกติกาข้างบนแล้วทั้ง 70 ช่อง: **`true` 32 ช่อง · `false` 38 ช่อง · ไม่เหลือ `null`**
(≥99% มี 35 ช่อง แต่ 3 ช่องคง `false` ตามแผน — f36 hint "จากจุดเข้า" เป็น opt-in · f38 นับ class ret ที่ build ใส่ให้ ·
f68 tag อยู่ sidecar นอกไฟล์รายงาน ⇒ ทั้งสามขาดได้โดยไม่ใช่ของเสีย · เหตุผลกำกับท้าย entry ในไฟล์)

ยิงทั้งคลัง 908 ใบ (**error คงเดิม 0** · **ผลของ E/W เดิมทุกรหัสไม่เปลี่ยนแม้แต่ไบต์เดียว** — พิสูจน์ด้วยการถอด
3 อย่างที่งานนี้เพิ่ม (suffix `· ช่อง x/70` · บรรทัด W21–W23 · fragment `ช่องต่ำสุด`) แล้วคำนวณตัวนับ ⚠ ใหม่จาก
บรรทัดที่เหลือ → diff ว่าง) · **warning รวม 149 → 358 → 303** (เลขสุดท้าย = หลังรอบแก้จากรีวิวทั้งสาขา ดูข้อ 2 ข้างล่าง):

| รหัส | ใบที่ยิง | องค์ประกอบ (นับครั้ง) |
|---|---|---|
| **W21** ช่อง required อ่านไม่ได้ | **13** | f54 vcell กรอบ 9 · f47 legend 3 · f17 ช่องส่วนต่าง(ตัวเลข) 3 · f49 ป้าย mFair 2 · f18 ช่องส่วนต่าง(ข้อความ) 1 — รวม 18 ครั้ง |
| **W22** คู่ consistency ไม่ตรง | **88 → 33** | f47 legend ↔ FV 16 · **f12 วันที่ disclaimer ↔ วันที่ราคา 65 → 9** · f24 เป้าบน gauge ↔ การ์ดเป้า 4 · f49 mFair ↔ FV 2 · f54 vcell กรอบ ↔ .fv-box 2 · f46 fairLine ↔ FV 2 · f48 โซนสะสม ↔ FV 1 · กฎ roe 0 — รวม 92 → **36 ครั้ง** |
| **W23** คู่ค้างตามเวลา | **108** | f41 ราคาหลุดกรอบ 52 สัปดาห์ **108** · f55 ป้าย "P/E เฉลี่ย ~N ปี" **0** (ยิงไม่ได้ — ดูข้างล่าง) |

ทั้งหมดเป็น**หนี้เก่าที่ gate เพิ่งมองเห็น** ไม่ใช่ของที่งานนี้ทำพัง — W ไม่บล็อก push (ไล่แก้ระยะ 3 · fix-on-touch)

### เรื่องที่วัดแล้วต่างจากที่แผนคาด

1. **f48 "โซนเริ่มทยอยสะสม < $FV" = เพดาน ไม่ใช่ค่าเดียวกับ FV** ⇒ `pair.how` เป็น **`below`**
   (ผ่านเมื่ออยู่ในช่วง **0.7×FV ถึง FV** · ขอบบนใช้ค่าเผื่อแบบ `moneyEq` เพื่อให้การ์ดที่เท่า FV พอดีผ่าน)
   เหตุผลจากการวัด: ถ้าบังคับ "เท่า FV" จะยิง **103 ใบ** ซึ่งสัดส่วน การ์ด ÷ FV กระจุกเป็นชั้น
   **0.95 (37) · 0.90 (36) · 0.85 (21) · 0.80 (4) · 0.75 (1)** = **ธรรมเนียมตั้งจุดเริ่มสะสมต่ำกว่า FV ตามส่วนเผื่อ
   MOS 5–20%** ของผู้เขียนราว 11% ของคลัง (อีก 795 ใบเขียนเท่า FV เป๊ะ) — ไม่ใช่บั๊กและไม่ใช่ของค้างตามเวลา
   หลังเปลี่ยนเหลือ **1 ใบ**: `TT` การ์ด 447 > FV 443 = ชวนสะสม**เหนือ**มูลค่าเหมาะสม ซึ่งคือของจริงที่กฎนี้ตั้งใจจับ
   ⇒ **W22 180 → 88 ใบ** (คำตัดสิน controller 12 ก.ย. 69 · คู่นี้ยังอยู่ใน W22 ไม่ย้ายไป W23)

2. **`notes` ของ cron → 0 · f12 เปลี่ยนตัวอ่านเป็น `findDiscPriceDate` (ตัวอ่าน = ตัวเขียน)**
   รอบแรกทำ note เหลือ 65 ใบด้วยเงื่อนไข "อ่านออกแต่เขียนไม่ได้" — แต่รีวิวทั้งสาขาชี้ว่าตัวอ่าน (f12 ห่อ
   `parsePriceDate` = ตัวสแกน**หัวรายงาน**) กับตัวเขียน (regex ฝังใน `tools/update-prices.js` ที่ห้ามตัวเลขคั่น)
   **เป็นคนละกฎ** ⇒ 65 ใบนั้นไม่ใช่ "งานค้าง" ก้อนเดียว แต่เป็นสองเรื่องคนละชนิด:
   - **55 ใบเป็น snapshot ของแหล่ง** — "…(ราคา $79.39 · 2 ก.ค. 2569 · P/E 13.94 GAAP)" คือวันที่ของ**ราคาที่ยกมาอ้าง**
     ไม่ใช่วันที่ราคาในรายงาน ⇒ **ห้ามเขียนทับ และไม่ควรอ่านตั้งแต่แรก** (เตือนทุกวันมา = false positive)
   - **10 ใบเป็นวันที่ระดับเดือน** ("ราคา ณ ม.ค. 2569") = ช่องโหว่ของตัวเขียนจริง (regex เดิมบังคับต้องมี "วัน")
     — เขียนได้แล้ว **9 ใบ** (AMATAV · CKP · DOHOME · FNF · PRINC · RAM · RLI · TOST · UTHR) · ใบที่ 10 คือ **TLI
     ซึ่งไม่ใช่วันที่ราคา**: "Yahoo Finance (กราฟราคา **ก.ค. 2568**–ก.ค. 2569)" = ต้นช่วงกราฟ (ดูกฎ "ต้นช่วง" ข้างล่าง)

   แก้ที่ **เจ้าของเดียว**: `tools/price-date.js` เพิ่ม `findDiscPriceDate(discHtml, from?)` แล้วทั้ง f12 และ cron
   เรียกตัวนี้ตัวเดียว · กฎเป็นสองชั้นรวมกัน (เลือกจุดที่มาก่อน) — ชั้นกว้างรับวันที่ที่ **ระบุวัน** + คำเชื่อม ≤25
   ตัวอักษรที่ไม่มีตัวเลข/แท็ก/"เป้า" คั่น (= กฎเดิมของตัวเขียน ⇒ **ไม่มีจุดไหนที่ cron เคยเขียนแล้วหายไป: วัดแล้ว 0 ใบ**)
   · ชั้นแคบใช้คำศัพท์ตัวเชื่อมชุดเดียวกับหัวรายงาน (`GAP_CHUNKS`) และเป็นชั้นเดียวที่รับ**วันที่ระดับเดือน**
   ★ เหตุที่เดือนต้องใช้ชั้นแคบ: ในบล็อก `.disc` เดือนลอย ๆ มักเป็นช่วงกราฟ/งวดงบ ไม่ใช่วันที่ราคา — วัดจริง
   AEONTS "(ราคา + กราฟย้อนหลัง **ก.ค.2568**–ก.ค.2569)" · LPH "(Factsheet ราคา P/E P/BV EV/EBITDA · ณ **ก.ค. 2569** / FY2568)"
   ⇒ ถ้าปล่อยชั้นกว้างรับเดือนด้วย cron จะประทับวันรันทับข้อเท็จจริงในอดีต = บั๊กเดิม 9 ส.ค. 69 ที่ `price-date.js`
   ตั้งขึ้นมาเพื่อกันพอดี (รูปที่ถูกกันออกไปแบบนี้มี 12 ใบ)
   ★★ ชั้นแคบอย่างเดียวยังไม่พอ — เคส **TLI** "กราฟราคา ก.ค. 2568–ก.ค. 2569" ผ่านชั้นแคบได้เพราะ anchor "ราคา"
   เป็น**ท้ายคำ** ("กราฟราคา") แล้วตามด้วยช่องว่างกับเดือนพอดี ⇒ เพิ่มกฎเชิงโครงสร้าง: **วันที่ที่ตามด้วยตัวคั่นช่วง
   แล้วมีวันที่อีกตัว = "ต้นช่วง" ไม่ใช่จุดเวลาของราคา → ข้าม** (ใช้ทั้งสองชั้นบนเส้นทาง `.disc` เท่านั้น ไม่แตะหัวรายงาน)
   · กฎนี้ตัด TLI กับอีก 2 จุดในใบที่มีหลายจุดออก และ **ไม่ทำให้จุดที่ cron เคยเขียนหายแม้แต่จุดเดียว** (วัดทั้งคลัง: lost = 0)

   ผลวัดทั้งคลัง (12 ก.ย. 2569 · dateParts = 12 ก.ย. 2569):

   | ตัวชี้ | ก่อน | หลัง |
   |---|---|---|
   | ใบที่ตัวอ่าน `.disc` เจอวันที่ราคา (f12) | 444 (48.9%) | **421 (46.4%)** |
   | ใบที่ cron เขียน `.disc` ได้จริง | 412 | **421** (+9 ใบระดับเดือน · ไม่หายสักใบ — วัด "token ที่ตัวเขียนเดิมเขียน" ครบทุกจุด) |
   | `notes` "disclaimer … found:false" | 65 | **0** |
   | W22 คู่ f12 ↔ f10 | 65 ใบ | **9 ใบ** (= 9 ใบระดับเดือนที่ยังไม่ได้ patch — cron รอบถัดไปเคลียร์เอง) |
   | warning รวมทั้งคลัง | 358 | **303** |

   สาขา `found:false` ที่เหลือ **สร้างเคสจริงไม่ได้ด้วยตัวเรนเดอร์ปัจจุบัน**: ตัวเขียน splice ที่ index ของตัวอ่านเอง
   แล้ว**อ่านกลับ**ด้วยตัวเดียวกัน (เดือนตัวย่อ · คงรูประดับวัน/เดือน · คงศักราชทีละ token) ⇒ อ่านกลับได้เสมอทุกจุด
   · เก็บสาขาไว้เป็น canary ของคู่ อ่าน/เขียน และ `test/update-prices-test.js` พิสูจน์ **invariant ที่ทำให้มันไม่เกิด** แทน
   (หลัง patch ทุกจุดใน `.disc` อ่านกลับได้เป็นวันที่ที่เพิ่งเขียน) · ใบที่ `.disc` ไม่มีวันที่เลยยังเงียบเหมือนเดิม
   - วันที่ทวนในวงเล็บ: เช็คเฉพาะวงเล็บที่ **ติดกับ token วันที่ราคา** (= ช่องของการทวนซ้ำจริง ๆ) → **0/908 ใบวันนี้**
     ★ ถ้าเช็คแบบ "มีวงเล็บวันที่ที่ไหนก็ได้ใน header" ตามร่างเดิม จะเตือนปลอมทุกวันบน **13 ใบ** ที่วงเล็บเป็น
     **ข้อเท็จจริงคนละตัว** (AMKR "ร่วง ~24% วันเดียว (7 ส.ค. 2026)" · AEHR · ADVICE · AVY) ซึ่ง `price-date.js`
     ตั้งใจไม่นับเป็นการทวนซ้ำอยู่แล้ว

3. **ตัว `range` ของ f54 ต้อง anchor ที่ `class="fv-box"` เหมือน E20** — สเปคร่างไว้ว่าให้ match คำว่า "กรอบ"
   ทั้งไฟล์ ซึ่งวัดแล้วฟ้องปลอม **168 ใบ** (AAPL: vcell 222–315 ไปเทียบกับ "กรอบ 24–37" ของการ์ด P/E) · anchor แล้วเหลือ **2**

### ★ สองขาที่ "ยิงไม่ได้บนคลังปัจจุบัน" — เลข 0 ในตารางแปลว่า *ตรวจไม่ได้* ไม่ใช่ *สะอาด*

| ขา | ยิงได้กี่ใบวันนี้ | ทำไมถึงยิงไม่ได้ | ปิดที่ไหน |
|---|---|---|---|
| **f55 ↔ f04** ป้าย "P/E เฉลี่ย ~N ปี" เทียบปีในกราฟ | **0/908** | `yearsInChart` อ่านปีจาก label กราฟที่เป็น **ปี 4 หลัก** เท่านั้น แต่ **ไม่มีใบไหนในคลังใช้รูปนั้นเลย** (generator เขียน "ต.ค.25" = เดือนไทย+ปี 2 หลัก ทุกใบ ⇒ ทุกกราฟกินเวลา 1 ปีพอดี) · ทางเลือก "ใช้ช่วงเดือนจริงเป็นตัวหาร" ทิ้งไป เพราะกราฟราคา 1 ปีไม่เคยเป็นขอบเขตของ "P/E เฉลี่ย 5–10 ปี" ⇒ จะยิงทุกใบที่มีการ์ดนี้ (514 ใบ) เป็นเสียงรบกวนล้วน และ BBL fixture เองก็จะติด | **Task 24** — เอา `fyYears` (จำนวนคอลัมน์ FY ที่มีเลขจริง) จาก prep/state มาเป็นตัวหารแทน `f04` (คือตัวหารที่เคส GABLE ใช้จริง) |
| **กฎ `stock-meta.roe` ต้อง null เมื่อขาดทุน** (open-item #14) | **0/908** | `ctx.baseEPS` มาจาก `firstNum(/EPS ฐาน\s*~?\s*[฿$]?\s*([0-9.]+)/)` ซึ่ง **ไม่มีเครื่องหมายลบในกลุ่มที่จับ** ⇒ ใบที่เขียน "EPS ฐาน ~−X" (มีจริง **3 ใบ**) match ไม่ติดเลย ได้ `baseEPS = null` ไม่ใช่ค่าติดลบ ⇒ `baseEPS <= 0` เป็นจริงได้เฉพาะใบที่เขียน "EPS ฐาน 0" เป๊ะ (ไม่มีในคลัง) · กลุ่มเป้าหมายจริงมองไม่เห็น: `roe>0` + อ่าน EPS ฐานไม่ได้ = **76 ใบ** · scenario EPS ติดลบ + `baseEPS=null` = **5 ใบ** | **Task 26** (ปิด open-item #14) — เปลี่ยนตัวชี้เป็น `ctx.scenarios[].eps` หรือ `stock-meta.pe === null` (ค่าที่รายงานตั้ง null เมื่อขาดทุนอยู่แล้ว) แทน `baseEPS` |

ทั้งสองขา**ไม่มีเคสใน self-test ที่เดินเส้นทางจริงบนคลังวันนี้** — ขา f55 มีเคสที่บังคับให้ทำงานด้วยการเปลี่ยน label
กราฟเป็นปี 4 หลักก่อน (โค้ดถูกเดินจริง) · กฎ roe ยังไม่มีเคสเลย เพราะ fixture BBL/AAPL กำไรทั้งคู่ — ต้องมี fixture
ขาดทุนก่อน ซึ่งเป็นงานของ Task 26

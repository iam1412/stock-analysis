# ระยะ 3 — เกณฑ์จบ/checkpoint (วัดจริง)

## §1 — Task 13a: cron รอบเดิมวันนี้ (#40 checkpoint — พิสูจน์ write path จริงบนคลัง v2)

**Run:** [`34930505064`](https://github.com/iam1412/stock-analysis/actions/runs/34930505064) · schedule-triggered · 2026-09-15 04:52:32–05:00:58 UTC (~8.5 นาที) · conclusion: success
**Commit:** `135eeab3` "price: refresh 658 symbols (2026-09-15)" บน `main`

### ผลตรวจ 5 จุดตามแผน

1. **จำนวนที่ patch สำเร็จ**: `เขียนแล้ว อัปเดต 658 · ไม่เปลี่ยน 4 · ข้ามเพราะตลาดเปิด 234 · freeze 12 · error 0 (ทั้งหมด 908)` — ✓ ทั้งเว็บ 1729 บรรทัด
2. **patch-rejected / patch-failed บนคลัง v2**: **0 / 0** ทั้งคู่ (grep ทั้ง log)
3. **gate หลัง patch**: `check-reports` 908/908 · error 0 · warning 236 (ระดับเดียวกับ baseline หลัง Task 8/10/11 ที่ 235 — ต่างเพราะราคาขยับทำให้บางไฟล์ข้ามพ้น/เข้าเงื่อนไข W23 ตามราคาจริง ไม่ใช่ regression) · `check-site` error 0 warning 0
4. **สุ่ม `git show` 5 ไฟล์ v2 ที่ patch จริง** (AMKR/AAPL/WMT/BNY/WWD — รวม 2 ใบที่ migrate จาก Task 4/5 ของระยะ 3 เอง): มีแค่ hunk ของ `report-data.values`/`stock-meta` JSON, จุดกราฟ (`["ก.ย.26", px]`), `highlight` array (derive จากราคา), และการ์ด derived (`Forward P/E`, `Market Cap`, `P/BV`, `เงินปันผล %`) — **ไม่แตะ prose/EPS/FV เลยสักไฟล์** ตรงสัญญา cron 100%
5. **#33 (ยังไม่ merge Task 7 ตอนรอบนี้)**: วันนี้วันอังคาร (วันทำการ) รอบนี้รันขณะ SET ยังเปิดตามเวลาเดิม (04:52 UTC = 11:52 น. ไทย) ⇒ หุ้นไทยถูกข้าม 234 ตัว "ข้าม — ตลาดยังเปิด" ตามที่ #33 อธิบายไว้ — **นี่คือพฤติกรรมเดิมก่อนแก้ ยืนยันปัญหา #33 มีจริงตามที่วัดไว้** (ยังไม่ใช่ผลของ Task 7 ที่รอ merge อยู่)

### สรุป
✅ **#40 ผ่าน** — cron write path บนคลัง v2 865+ ใบ (**883/908 ณ รอบที่วัด** — Task 14 ย้าย `MU` เพิ่มทีหลัง ⇒ คลังปัจจุบัน **884/908** · ตัวเลข 883 ที่นี่คือสภาพคลังตอน run `34930505064` ห้ามแก้ย้อนหลัง) ทำงานถูกต้องบนราคาที่ขยับจริง ไม่มี regression ⇒ **Task 7 (PR #45), Task 9, Task 12 merge ได้แล้ว**

---

## §2 — Task 13b: cron รอบแรกที่เวลาใหม่ (#33 verification)

**สถานะ:** PR #45 (Task 7) **merge แล้ว** (`e2bcb3e9` · `.github/workflows/update-prices.yml` = `0 21 * * *` UTC — ไม่ใช่ ~20:30 UTC ตามข้อเสนอเดิมของ #33 ซึ่ง = 15:30 ET ในฤดู EST = กลาง session ตลาด US · เหตุผลเต็มใน `905267c4`)

**Run:** [`35035634931`](https://github.com/iam1412/stock-analysis/actions/runs/35035634931) · schedule-triggered (รอบแรกที่เวลาใหม่) · created `2026-09-15T23:26:03Z` (= 06:26 น. ไทย 16 ก.ย.) · conclusion: success
**Commit:** `9e226dc3` "price: refresh 657 symbols (2026-09-16)" บน `main`
**Skew ที่วัดได้จริง:** config `21:00 UTC` → รันจริง `23:26:03 UTC` = **2 ชม. 26 นาที** (อยู่ในกรอบ safe skew ceiling 5.78 ชม. ที่คำนวณไว้ตอนแก้ Task 7 สบาย ๆ)

### ผลตรวจ

1. **จำนวนที่ patch สำเร็จ**: `เขียนแล้ว อัปเดต 657 · ไม่เปลี่ยน 235 · freeze 16 · error 0 (ทั้งหมด 908)`
2. **patch-rejected / patch-failed**: **0 / 0** ทั้งคู่
3. **"ข้าม — ตลาดยังเปิด" (แก่นของ #33)**: **0 ครั้ง** ทั้งรอบ (เทียบกับ 230–234 ครั้ง/วันก่อนแก้ที่วัดไว้ใน §1 และก่อนหน้านั้น) ⇒ **guard `isIntradayQuote` ไม่ยิงเลยสักตัวที่เวลาใหม่นี้** ทั้งฝั่ง US และ TH
4. **gate หลัง patch**: 908/908 · error 0
5. **หมายเหตุสำคัญ — ทำไมหุ้นไทยไม่โผล่ในบรรทัด "อัปเดต 657"**: ตรวจ `git show 9e226dc3 --stat -- reports/` ไขว้กับ `reports.json.metrics.market==='TH'` (238 ใบ) พบว่า **0 ใบไทยอยู่ในรายการ 657 ที่แก้จริง** — เหตุผลคือ**การรัน `workflow_dispatch` มือของผมเองเมื่อวาน** (run `34962764081`, 15 ก.ย. 18:20 น. ไทย — ตอนนั้น SET ปิดแล้วเช่นกัน) ได้ patch ราคาปิดของวันเดียวกัน (15 ก.ย.) ให้หุ้นไทยไปก่อนแล้ว — พิสูจน์ด้วย `hash` ของ `PTT` เท่ากันเป๊ะระหว่าง commit เมื่อวาน (`255bfd9d`) กับวันนี้ (`9e226dc3`) คือ `ea068738fc19` ⇒ รอบเวลาใหม่ (06:26 น.) fetch ราคาปิดเดียวกันซ้ำ (ตลาดไทยยังไม่เปิดวันที่ 16 ก.ย. ตอนรันรอบนี้) จึงตกบัคเก็ต "ไม่เปลี่ยน" (เงียบในล็อก ไม่ใช่ error/skip) — **ไม่ใช่ว่าหุ้นไทยถูกข้าม** เป็นผลข้างเคียงที่ไม่ได้ตั้งใจของการ sanity-check มือเมื่อวาน ทำให้ไม่มีเคส "หุ้นไทย patch ราคาใหม่จริง" ให้ดูในรอบนี้ — รอบ `schedule` **ถัดไป** (คาด ~06:xx น. ไทย 17 ก.ย.) จะ fetch ราคาปิดวันที่ 16 ก.ย. ซึ่งเป็นข้อมูลใหม่จริง และจะเป็นหลักฐานที่ "สะอาด" กว่านี้

### สรุป
✅ **#33 ปิดได้** — แก่นของปัญหา (guard `isIntradayQuote` ยิงเพราะ cron รันกลางตลาดเปิด) วัดได้ตรง ๆ ว่า **0 ครั้ง** ที่เวลาใหม่ เทียบกับ 230+ ครั้ง/วันก่อนแก้ — เป็นหลักฐานตรงและเพียงพอ แม้จะไม่มีเคส "หุ้นไทยได้ราคาใหม่" ในรอบนี้โดยเฉพาะ (ผลข้างเคียงจากการ dispatch มือของผมเอง) เพราะสิ่งที่ #33 วัดคือ **guard ยิงกี่ครั้ง** ไม่ใช่ **ราคาขยับกี่ตัว** — แนะนำให้เจ้าของดูรอบ `schedule` ถัดไป (17 ก.ย.) เป็นการยืนยันซ้ำแบบไม่มีตัวแปรกวน แต่ไม่ใช่เงื่อนไขบังคับก่อนปิด item นี้

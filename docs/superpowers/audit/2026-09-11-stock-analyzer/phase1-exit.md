# เกณฑ์จบระยะ 1 — ผลวัดจริง 12 ก.ย. 2569

> วัดบน worktree `stock-analyzer-audit-9678cd` branch `claude/audit-p1-f-exit` @ `ee3de05c` (ส่วน A–E ครบในสาขานี้ · A–D merge main แล้วเป็น PR **#31 #32 #33 #34** · **#35** merged `3d028b75` (ส่วน E)) · เวลาไทย (UTC+7)
> เกณฑ์ 5 ข้อมาจาก spec §6 แถว **"1"**: `docs/superpowers/specs/2026-09-11-stock-analyzer-audit-design.md`
> ★ **ทุกตัวเลขในไฟล์นี้วัดใหม่บนสาขานี้** ไม่ได้ลอกจาก ledger — ตัวที่ต่างจาก ledger มีคำอธิบายกำกับทุกตัว

## 0. สรุป — ผ่าน 5/5

| # | เกณฑ์ (spec §6 แถว "1") | ค่าที่วัดได้ | ผล |
|---|---|---|---|
| 1 | gate พิมพ์ coverage ทุกไฟล์ | **908/908 บรรทัดมี `· ช่อง x/70`** · บรรทัดสรุปมี `ช่องต่ำสุด 46/70 (FANG)` · ต่ำสุด **46** · มัธยฐาน **58** · สูงสุด 64 | ✅ |
| 2 | NEITHER 18 → 0 (+1 แถวใหม่ f64 นอก 18 เดิม · deferred ระยะ 2 ตามข้อ A/B — manifest จึงรายงาน 1) | ช่องที่ไม่มี binding เหลือ **`f64` ตัวเดียว** (deferred ระยะ 2 โดยข้อ A/B) · 18 แถวเดิมได้เจ้าของครบ (`pair` 8 · `presence` 10) | ✅ |
| 3 | UNVERIFIED WRITE 3 → 0 | **f11 f12 f50 = `pair` ทั้งสามแถว** · cron `notes` "found:false" = **0/908** | ✅ |
| 4 | W16/W17/W19/W20 → E โดย cron ไม่ล้ม | sweep 12 ราคา **fails=0 · error 0 · warning 241 ทุกราคา** · quarantine ส่ง `patch-rejected` ไม่ throw · **cron จริง 12 ก.ย. รันบนโค้ดที่ยกเป็น E แล้วสำเร็จ** (897 ใบ · PONY เข้า quarantine 1 ใบ) | ✅ |
| 5 | คิว LLM ลด ≥50% | replay 8 snapshot (มีของจริง 6) — กฎเก่า **57** → กฎใหม่ **16** = **ลด 72%** · exit 0 | ✅ |

---

## 1. เกณฑ์ 1 — gate พิมพ์ coverage ทุกไฟล์

```bash
rtk proxy node test/check-reports.js > cr.txt
grep -c 'ช่อง [0-9]*/70' cr.txt                      # → 908
grep -o 'ช่อง [0-9]*/70' cr.txt | sort -n -k2 | uniq -c   # การกระจาย
```

> **หมายเหตุ suffix:** เป็น `ช่อง x/**70**` ไม่ใช่ `/68` — คำว่า "68 ช่อง" ในสเปก/แผนคือ **เลขแถวสูงสุด** (#1–#68) ไม่ใช่จำนวนแถว · manifest จริงมี 70 แถว เพราะ code-audit §1.1 แทรก #25b และ #63b (ดู `manifest-census.md` หัวไฟล์)

บรรทัดสรุปที่ได้ (verbatim):

```
สรุป: 908/908 ไฟล์ผ่าน • error 0 • warning 241 • ช่องต่ำสุด 46/70 (FANG)
```

**coverage ทั้งคลัง (n=908):** ต่ำสุด **46** · มัธยฐาน **58** · เฉลี่ย **57.45** · สูงสุด **64**

| ช่องที่อ่านได้ | ใบ | ช่องที่อ่านได้ | ใบ | ช่องที่อ่านได้ | ใบ |
|---|---|---|---|---|---|
| 46 | 2 | 53 | 39 | 60 | 112 |
| 47 | 1 | 54 | 52 | 61 | 89 |
| 48 | 2 | 55 | 63 | 62 | 32 |
| 49 | 8 | 56 | 74 | 63 | 8 |
| 50 | 14 | 57 | 113 | 64 | 7 |
| 51 | 16 | 58 | **135** | | |
| 52 | 19 | 59 | 122 | | |

**อ่านผล:** ไม่มีใบไหนที่ gate เดินผ่านโดยไม่ประกาศว่าอ่านได้กี่ช่อง ⇒ "ช่องที่ไม่มีใครอ่าน" กลายเป็นตัวเลขที่เห็นได้ทุกวัน ไม่ใช่จุดบอด · ช่วง 46–64 (ไม่ใช่ 70 ทุกใบ) เป็นเรื่องปกติ — 38 ใน 70 ช่องเป็น `required:false` (การ์ดปันผล/Beta/BVPS/เป้านักวิเคราะห์ ฯลฯ ที่รายงานหลายใบไม่มี) · ช่อง required ที่หายจะถูก **W21** ฟ้องแยกต่างหาก

---

## 2. เกณฑ์ 2 — NEITHER 18 → 0

```bash
rtk proxy node -e "const {FIELDS}=require('./tools/field-manifest.js');const n=FIELDS.filter(f=>f.binding==='deferred'||(!f.gate.length&&f.binding!=='pair'&&f.binding!=='presence'&&f.binding!=='cron'));console.log('ไม่มี binding:',n.map(f=>f.id))"
```

ผล (verbatim):

```
ไม่มี binding: [ 'f64' ]
total fields: 70
binding tally: {"cron":27,"pair":12,"presence":13,"gate":17,"deferred":1}
```

`f64` (ราคาในร้อยแก้ว) = **เลื่อนไประยะ 2 โดยเจตนา ตามข้อ A/B ของแผน** ไม่ใช่ช่องที่ตกหล่น — เป็นช่องเดียวที่เหลือ

**18 แถว NEITHER เดิม (code-audit §1.1 "ไม่มี cron เขียน ไม่มี gate ตรวจ") → binding ใหม่** (ตรวจซ้ำจาก `tools/field-manifest.js` ที่ HEAD ของสาขานี้ · ตรงกับ `manifest-census.md` ทุกแถว):

| id | ช่อง | binding ใหม่ | คู่เทียบ |
|---|---|---|---|
| f13 | footer "ข้อมูล ณ" | `presence` | — |
| f23 | การ์ดเป้านักวิเคราะห์ — ราคา | `presence` | — |
| f24 | เป้านักวิเคราะห์บน gauge scale | `pair` | → f23 (money) |
| f25 | rating นักวิเคราะห์ | `presence` | — |
| f25b | จำนวนสำนัก n= | `presence` | — |
| f32 | การ์ด BVPS | `presence` | — |
| f33 | การ์ด EPS (TTM) | `presence` | — |
| f41 | กรอบ 52 สัปดาห์ | `pair` | → f01 (contains · stale) |
| f46 | report-data.chart.fairLine | `pair` | → f44 (money) |
| f47 | legend "มูลค่าเหมาะสม $FV" | `pair` | → f44 (money) |
| f48 | การ์ด "โซนเริ่มทยอยสะสม < $FV" | `pair` | → f44 (**below** — ไม่ใช่ money · ดูหมายเหตุ) |
| f49 | ป้าย gauge mFair "เหมาะสม $FV" | `pair` | → f44 (money) |
| f54 | vcell กรอบ (FV_LOW–FV_HIGH) | `pair` | → f43 (range) |
| f55 | การ์ด "P/E เฉลี่ย ~N ปี" | `pair` | → f04 (years · stale) |
| f58 | การ์ดกำไรสุทธิ / YoY | `presence` | — |
| f60 | การ์ดอัตรากำไรขั้นต้น | `presence` | — |
| f61 | การ์ด Beta | `presence` | — |
| f63b | "จาก ATH −X%" ใน prose | `presence` | — |

รวม **`pair` 8 · `presence` 10 = 18/18** — ไม่เหลือแถวที่ไม่มีใครอ่าน

> **f48 ใช้ `below` ไม่ใช่ `money`** — วัดแล้วว่า "โซนเริ่มทยอยสะสม" เป็น**เพดาน** ไม่ใช่ค่าเดียวกับ FV (ผู้เขียนตั้งต่ำกว่า FV ตามส่วนเผื่อ MOS 5–20% ราว 11% ของคลัง) · ถ้าบังคับ "เท่า FV" จะยิง 103 ใบที่ไม่ใช่ของเสีย ⇒ คำตัดสิน controller 12 ก.ย. 69 (เหตุผลเต็ม + การกระจายสัดส่วนอยู่ใน `manifest-census.md` §"เรื่องที่วัดแล้วต่างจากที่แผนคาด" ข้อ 1)

---

## 3. เกณฑ์ 3 — UNVERIFIED WRITE 3 → 0

3 แถวที่ code-audit ชี้ว่า "cron เขียนได้ แต่ไม่มีใครตรวจว่าเขียนถูก" คือ **f11 · f12 · f50** — ตอนนี้ทั้งสามเป็น `pair` (มีคู่ให้เทียบค่า) และมี `notes` ของ cron เป็นชั้นที่สอง

```bash
rtk proxy node -e "const {FIELDS}=require('./tools/field-manifest.js');for(const id of ['f11','f12','f50']){const f=FIELDS.find(x=>x.id===id);console.log(JSON.stringify({id:f.id,binding:f.binding,gate:f.gate,pair:f.pair,required:f.required}))}"
```

```
{"id":"f11","binding":"pair","gate":[],"pair":{"with":"f10","how":"date"},"required":false}
{"id":"f12","binding":"pair","gate":[],"pair":{"with":"f10","how":"date"},"required":false}
{"id":"f50","binding":"pair","gate":[],"pair":{"with":"f01","how":"money"},"required":true}
```

| id | ช่อง | ตัวตรวจชั้น 1 (`pair` → W22) | ตัวตรวจชั้น 2 (cron `notes`) |
|---|---|---|---|
| f11 | วันที่ทวนในวงเล็บ (คนละศักราช) | เทียบวันที่กับ f10 (วันที่ราคา) | `update-prices.js:542` — อ่านกลับไม่ออก = `found:false` |
| f12 | disclaimer "ราคา ณ" ใน `.disc` | เทียบวันที่กับ f10 (ระดับเดือน) | `update-prices.js:571` — อ่านออกแต่เขียนกลับไม่ลง = `found:false` |
| f50 | ป้าย gauge mCur "ปัจจุบัน $px" | เทียบราคากับ f01 (.px header) | — (ราคาเป็นค่าเดียวกับ f01 ตรวจจบที่ `pair`) |

**cron `notes` "found:false" ทั้งคลัง = 0/908** — เดิน `patchReport()` (เส้นทางเดียวกับ cron · offline ไม่แตะ network ไม่เขียนไฟล์) ทุกใบด้วยราคาเดิม + วันที่วันนี้:

```
ไฟล์ที่เดิน patchReport: 908
ไฟล์ที่มี notes: 0
tally: {}
```

> ที่ notes = 0 วันนี้ **เพราะวันที่ disclaimer มีเจ้าของเดียวแล้ว** — `tools/price-date.js` เพิ่ม `findDiscPriceDate()` ให้ทั้ง**ตัวอ่าน** (f12) และ**ตัวเขียน** (cron) เรียกฟังก์ชันเดียวกัน ⇒ สาขา "อ่านออกแต่เขียนไม่ลง" 65 ใบเดิมหายไปเพราะกฎเดียวกันทั้งสองฝั่ง ไม่ใช่เพราะปิดตาไม่ตรวจ (รายละเอียด + กติกาสองชั้น/กฎ "ต้นช่วง" → `manifest-census.md` ข้อ 2)

### ข้อจำกัดที่ประกาศไว้ ไม่ใช่ "ผ่านแบบเงียบ"

สองขาข้างล่างมีโค้ดครบแต่ **ยิงไม่ได้บนคลังวันนี้** — ต้องอ่านเลข 0 ของมันว่า *ตรวจไม่ได้* ไม่ใช่ *สะอาด*:

| ขา | ทำไมยิงไม่ได้ | ปิดที่ไหนแล้ว |
|---|---|---|
| **f55 ↔ f04** ป้าย "P/E เฉลี่ย ~N ปี" เทียบจำนวนปีในกราฟ | `yearsInChart` อ่านได้เฉพาะ label กราฟที่เป็น **ปี 4 หลัก** — คลังทั้ง 908 ใบใช้ "ต.ค.25" (เดือนไทย + ปี 2 หลัก) ⇒ **0/908** | **ไม่ได้ปล่อยทิ้ง** — Task 24 ปิดด้วย `fyYears` (จำนวนคอลัมน์ FY ที่มี EPS จริง จากตาราง [3]) → `prep()` เขียนลง state + เตือนใน prompt · `postcheck.checkFyYears(f55, fyYears)` ฟ้องเมื่อป้ายเกินจริง (open-item #7 ปิด) · ขา manifest `f55↔f04` คงไว้เป็นข้อมูลเสริม ไม่ใช่ตัวตัดสิน |
| **กฎ `stock-meta.roe` ต้อง null เมื่อขาดทุน** | `ctx.baseEPS` มาจาก regex ที่ **ไม่จับเครื่องหมายลบ** ⇒ ใบที่เขียน "EPS ฐาน ~−X" (3 ใบ) ได้ `null` ไม่ใช่ค่าติดลบ ⇒ เงื่อนไข `baseEPS <= 0` เป็นจริงไม่ได้ ⇒ **0/908** | **ยังเปิด** — open-item #14 · จ่ายให้ Task 26 (เปลี่ยนตัวชี้เป็น `ctx.scenarios[].eps` หรือ `stock-meta.pe === null`) |

---

## 4. เกณฑ์ 4 — W16/W17/W19/W20 → E โดย cron ไม่ล้ม

### (ก) ยกระดับจริงแล้วในโค้ด

```bash
rtk proxy node -e "const {CHECKS}=require('./test/check-reports.js');const lv={};for(const c of CHECKS)lv[c.level]=(lv[c.level]||0)+1;console.log(CHECKS.length,JSON.stringify(lv));console.log('error:',CHECKS.filter(c=>c.level==='error').map(c=>c.id).join(' '))"
```

```
65 {"error":47,"warn":18}
error: E01 … E43 W16 W17 W19 W20
warn:  W01 W02 W03 W04 W05 W06 W07 W08 W09 W10 W12 W13 W14 W18 W15 W21 W22 W23
```

รหัส gate ทั้งหมด **65** = **47 ระดับ error** (E01–E43 + W16 W17 W19 W20 ที่ยังใช้ชื่อเดิมแต่เป็น error แล้ว) + **18 ระดับ warn** · ตรงกับที่ทุกไฟล์พิมพ์ `47/47 ผ่าน`

### (ข) sweep ราคาสังเคราะห์บนสาขา F (รวม A–E) — `sweep fails=0`

ต่างจาก Task 5 ที่วัดบนส่วน A อย่างเดียว · รันสองแบตช์ foreground (ข้อจำกัดเวลาต่อ call) สคริปต์ไม่แก้:

```bash
PRICES="0.5 0.7 0.85 0.95 1.0 1.05" SWEEP_DIR=<scratchpad>/sweep-p1 sh docs/superpowers/audit/2026-09-11-stock-analyzer/sweep.sh
PRICES="1.15 1.3 1.5 1.8 2.2 3.0"   SWEEP_DIR=<scratchpad>/sweep-p1 sh docs/superpowers/audit/2026-09-11-stock-analyzer/sweep.sh
git status --short     # ว่างทั้งสองรอบ
```

```
×0.5 ok · ×0.7 ok · ×0.85 ok · ×0.95 ok · ×1.0 ok · ×1.05 ok    → sweep fails=0
×1.15 ok · ×1.3 ok · ×1.5 ok · ×1.8 ok · ×2.2 ok · ×3.0 ok      → sweep fails=0
```

| ตัวคูณราคา | ผล gate | AAPL ช่อง | BBL ช่อง | ตัวคูณราคา | ผล gate | AAPL ช่อง | BBL ช่อง |
|---|---|---|---|---|---|---|---|
| ×0.5 | error 0 · warning 241 | 57/70 | 55/70 | ×1.15 | error 0 · warning 241 | 58/70 | 56/70 |
| ×0.7 | error 0 · warning 241 | 58/70 | 56/70 | ×1.3 | error 0 · warning 241 | 58/70 | 56/70 |
| ×0.85 | error 0 · warning 241 | 58/70 | 56/70 | ×1.5 | error 0 · warning 241 | 58/70 | 56/70 |
| ×0.95 | error 0 · warning 241 | 58/70 | 56/70 | ×1.8 | error 0 · warning 241 | 57/70 | 55/70 |
| **×1.0 (ราคาจริง)** | **error 0 · warning 241** | **58/70** | **56/70** | ×2.2 | error 0 · warning 241 | 57/70 | 55/70 |
| ×1.05 | error 0 · warning 241 | 58/70 | 56/70 | ×3.0 | error 0 · warning 241 | 53/70 | 51/70 |

**อ่านผล — ต่างจากระยะ 0 อย่างมีนัย:** ระยะ 0 ตัวเลข warning **ขยับ 149→150/151** ตามราคา (คลาส W06/W15 ของ AAPL/BBL เอง) ซึ่งเป็นเหตุผลที่ phase0-exit §6 สั่งให้รัน sweep ซ้ำหลังเลื่อน W→E · รอบนี้ **warning นิ่งที่ 241 ทุกราคา และ AAPL/BBL ไม่ยิง warning เลยสักราคาเดียว** เพราะสี่รหัสที่เลื่อนเป็น E มีตัวซ่อมของ cron คู่กันครบ (`patchDerived#6/#7/#8/#10`) และช่องสรุปที่เคยเป็น W06 กลายเป็น**คลังคำคงที่ที่ cron เขียนทั้งช่อง** (`summaryPlan` healer #11) ⇒ เส้นทาง patch ไม่ทิ้งความขัดแย้งไว้ให้ประตูเห็นอีก · สิ่งที่ยังขยับตามราคาคือ **coverage** (58→53 ที่ ×3.0) = ช่อง **optional** อ่านไม่ออกที่ราคาสุดขั้ว — **ไม่มีช่อง required หาย** (W21 ไม่ยิงบนสองใบนี้เลย)

### (ค) quarantine: gate ตกหลัง patch = `patch-rejected` ไม่ใช่ crash

`test/update-prices-test.js:961–985` (ผ่านใน verify ขั้นที่ 1 · 271 เคส):

```
gateAfterPatch: fixture ดี → ok
gateAfterPatch: ไฟล์ที่ patch แล้ว gate ตก → ok=false + รหัส (E15)
gateAfterPatch: detail มีรหัส + สั้นพอลง price-flags.json
gateAfterPatch: expandReport ระเบิด → EXPAND ไม่ throw
gateAfterPatch: W17 (ยกเป็น error) ที่ตกหลัง patch → ok=false + patch-rejected ไม่ throw
```

เคสสุดท้ายคือเคสที่ตรงกับเกณฑ์นี้เป๊ะ: **W17 ที่ตอนนี้เป็น error ต้องเข้า quarantine เหมือน E ตัวอื่น ไม่ใช่ทำให้รอบ cron ล้ม**

### (ง) ★ cron ของจริงรันบนโค้ดที่ยกเป็น E แล้ว — และไม่ล้ม (แก้ข้อสันนิษฐานของ brief)

brief ของ task นี้ระบุว่า cron ยังไม่เคยรันบนโค้ดส่วน A ("รอบ 12 ก.ย. `b17b70c1` เกิดก่อน merge PR #31 ~18 นาที") — **วัดแล้วไม่ใช่** ต้องบันทึกตามจริง:

```bash
git log -1 --format='author=%ad%ncommit=%cd%nparents=%p' --date=iso-strict b17b70c1
git merge-base --is-ancestor a4b91756 b17b70c1 && echo YES   # a4b91756 = merge PR #31
```

```
author=2026-09-12T04:48:19Z   commit=2026-09-12T04:48:19Z   parents=824ff8a7
YES
```

- `b17b70c1` (price refresh 897 symbols) มี **parent = `824ff8a7` = merge PR #32 (ส่วน B)** และ merge PR #31 (`a4b91756`, 07:35 น. ไทย) เป็น ancestor · author date = committer date ⇒ ไม่ได้ถูก rebase มาทีหลัง
- ตรวจโค้ดที่ main ถืออยู่ตอนนั้นโดยตรง: `git show 824ff8a7:.github/workflows/update-prices.yml` มี `npm run verify:cron` แล้ว · `git show 824ff8a7:test/check-reports.js` ประกาศ **W16 W17 W19 W20 เป็น `level:'error'`** แล้ว
- ผลของรอบนั้น: **patch + push สำเร็จ 897 ใบ** · คิวที่เขียนออกมา 9 แถว = `mos-sign-flip` 6 · `drift-gt-15pct` 2 · **`patch-rejected` 1** คือ **PONY** พร้อม detail `W17 (patch ทำให้ตก) — W17 ป้าย "จากจุดเข้า" = 6.63 แต่ราคาปัจจุบัน 6.64`

⇒ **นี่คือหลักฐานของจริง ไม่ใช่สังเคราะห์**: W17 ระดับ error ทำให้ไฟล์เดียวถูกกักกันรายไฟล์ และรอบ cron เดินต่อจนจบ — ตรงกับกลไกที่เกณฑ์นี้ต้องการเป๊ะ

**สิ่งที่ยัง*ไม่*ได้พิสูจน์ด้วยของจริง:** รอบนั้นถือโค้ดแค่ **ส่วน A + B** · ส่วน **C** (dead-band ±5 · `summaryPlan` · triage flip→PREPATCH · คิวตามอายุ · state ถาวร) **D** (gen-docs/docs-test) **E** (กับดัก vendor ใน prep) ยังไม่เคยผ่าน cron จริง — รอบแรกที่ครบทุกส่วนคือรอบถัดไปหลัง PR #35 ขึ้น main (ตารางเวลา `17 0 * * *` UTC = **07:17 น. ไทย** · สังเกตจริง 3 วันหลังสุด commit ลงเวลา 04:48–04:56 UTC = ~11:50 น. ไทย ⇒ **GitHub Actions หน่วงจากตารางราว 4.5 ชม. สม่ำเสมอ** ซึ่งเป็นข้อสังเกตใหม่ ควรตรวจสอบตอน follow-up) → §7

---

## 5. เกณฑ์ 5 — คิว LLM ต่อรอบลด ≥50% (replay)

`docs/superpowers/audit/2026-09-11-stock-analyzer/replay-queue.js` — replay `price-flags.json` **8 snapshot ล่าสุด** (ค้นจาก `--since=2026-08-01` แต่ที่เจอจริงคือ **5–12 ก.ย. 2569 — ช่วง 1 สัปดาห์**, ตรงกับที่ KPI ข้อนี้วัดเป็นรายสัปดาห์) จาก git history (offline ล้วน ไม่แตะ network) ผ่าน `tools/queue/triage.js` ตัวจริง:

- **กฎเก่า** = ทุกแถว LIGHT/FULL ที่ไม่ skip ส่ง LLM (`mos-sign-flip`→LIGHT · `drift-gt-*`→LIGHT · `suspect-split-or-data`/`bad-chart`→FULL)
- **กฎใหม่** = `T.llmList(T.triage(...))` — `mos-sign-flip` เป็น **PREPATCH** (ไม่ส่ง LLM) ยกเว้นยกเป็น LIGHT เมื่ออายุ footer > 90 วัน
- `footerAgeOf` อ่าน footer **ของ snapshot นั้น** (`git show <sha>:reports/<SYM>.html`) เทียบกับวันที่ของ snapshot · `earningsAfterOf` **ไม่มีข้อมูลย้อนหลัง** ⇒ วัดเฉพาะกฎ flip + การยกตามอายุ

```bash
rtk proxy node docs/superpowers/audit/2026-09-11-stock-analyzer/replay-queue.js ; echo "exit=$?"
```

| snapshot | flags | LLM (กฎเก่า) | LLM (กฎใหม่) | ลด |
|---|---|---|---|---|
| 2026-09-12 `b17b70c1` | 9 | 7 | 1 | 86% |
| 2026-09-11 `c963b78d` | 3 | 1 | 0 | 100% |
| 2026-09-10 `abca6132` | 4 | 3 | 0 | 100% |
| 2026-09-09 `3428cf2b` | 1 | 1 | 1 | 0% |
| 2026-09-09 `96ab5bbe` | 27 | 27 | 8 | 70% |
| 2026-09-05 `4fe8536e` | 21 | 18 | 6 | 67% |

```
รวม: กฎเก่า 57 · กฎใหม่ 16 · ลด 72% (เกณฑ์ ≥50%)
exit=0
```

- **อ่าน 8 snapshot จริง แต่ขึ้นตาราง 6** — `277dc9c3` (11 ก.ย.) และ `22565f2b` (9 ก.ย.) เป็น `[]` (คิวถูกเคลียร์หมดในรอบนั้น) สคริปต์ข้ามแถวว่างตามที่เขียนไว้ · ตรวจยืนยันด้วย `git show <sha>:price-flags.json` แล้วทั้งคู่
- แถวที่ลด 0% (`3428cf2b`) = คิว 1 แถวที่เป็น `suspect-split-or-data` ล้วน (APH split 2-for-1 · bucket FULL) ซึ่งกฎใหม่ไม่แตะโดยตั้งใจ — **ไม่ใช่ของเสีย** กฎนี้เปลี่ยนเฉพาะ `mos-sign-flip`
- **ข้อจำกัดที่ต้องประกาศ:** ปฏิทินงบ **ยังไม่เปิดใช้** (fallback · open-item #24) ⇒ การยก PREPATCH→LIGHT ด้วย "งบออกหลังวิเคราะห์" **ยังไม่ทำงาน** · ตัวเลข −72% จึงสะท้อน **flip→PREPATCH + การยกตามอายุ >90 วัน เท่านั้น** · เมื่อเปิดปฏิทินแล้วตัวเลขนี้จะ**ลดลง** (คิว LLM เพิ่มขึ้น) ตามจำนวนใบที่มีงบใหม่จริง — ซึ่งเป็นการเพิ่มที่ต้องการ ไม่ใช่การถอยหลัง
- **ข้อจำกัดอีกข้อ (cron skew, open-item #33):** cron ลงเวลาจริงอยู่ในช่วง SET เปิดตลาด ⇒ หุ้นไทย refresh ได้จริงแค่รอบวันเสาร์ ⇒ **snapshot วันธรรมดาในตารางข้างบนเอียงไปทางหุ้น US เป็นหลัก** ตัวเลขลด 72% จึงไม่ได้ถ่วงน้ำหนักหุ้นไทยเท่าที่ควร

**สคริปต์ต่างจาก brief 1 จุด (จำเป็น):** brief เขียน `--format=%h|%ad` แบบไม่ quote ซึ่ง `execSync` ส่งผ่าน shell ⇒ `|` กลายเป็น pipe จริง (`%ad: command not found`) · แก้เป็น `--format='%h|%ad'` เท่านั้น ตรรกะไม่เปลี่ยน

---

## 6. KPI — baseline → หลังระยะ 1 (spec §7)

| KPI | baseline (11–12 ก.ย. 69) | หลังระยะ 1 (12 ก.ย. 69) | เป้า spec |
|---|---|---|---|
| ช่อง NEITHER (ไม่มีใครอ่าน) | 18 | **0** (`f64` deferred ระยะ 2 ตามข้อ A/B) | 0 ✅ |
| ช่อง UNVERIFIED WRITE | 3 | **0** (f11 f12 f50 = `pair` · cron `notes` 0/908) | 0 ✅ |
| หุ้นที่ส่ง LLM ÷ หุ้นที่ติด flag | 57/65 = **88%** | **16/65 = 24.6%** (replay 6 snapshot · 65 = ผลรวมข้าม snapshot — flag ที่ค้างหลายวันถูกนับซ้ำ · เป็นตัวแทนต่อรอบ ไม่ใช่จำนวนหุ้นจริง) | ≤40% ✅ |
| coverage ที่ gate ประกาศ | ไม่มี | **908/908 ใบ** · ต่ำสุด 46/70 · มัธยฐาน 58/70 | ทุกไฟล์ ✅ |
| ขั้น `npm run verify` | 14 | **16** (+`docs-test` +`prep-stock-test`) · 9.13 วิ · `verify:cron` แยกอีก 5 ขั้น | — |
| รหัส E/W ใหม่ในระยะ 1 | — | **3 รหัส — W21 W22 W23 ทั้งสามเป็น warn** (+ W06 **เขียนใหม่** ใช้รหัสเดิม: ตรวจรูป+ตัวเลขของคลังคำที่ cron เขียน ไม่ผูก dead-band) · ไม่มีรหัส **E** ใหม่ — 4 ตัวที่เป็น error เพิ่มคือ W16/W17/W19/W20 ที่**เลื่อนระดับ** ไม่ใช่รหัสใหม่ | ≤1/เดือน ⚠ (3 ในเดือนเดียว — ทั้งหมดเป็น warn และเป็นผลของ manifest ที่แผนสั่งทำ) |
| warning เปิดอยู่ | 149 (ก่อน manifest) → 303 (ตอนเปิด W21–W23) | **241** — W23 109 · W15 96 · W22 24 · W21 11 · W05 1 · **W06 = 0** (W06 เก่า 51 ใบ → เขียนกฎใหม่ยิง 861 ใบ → กวาดครั้งเดียวเหลือ 0) | 0 ที่ระยะ 3 |
| `docs/open-items.md` | เปิด 19 · ปิด 4 | Task 25 (วัดตอนนั้น): เปิด 13 · ปิด 11 (+#24 ใหม่ · ปิดในระยะ 1: #4 #7 #8 #10 #13 #19 #22) → **Task 26 (รีวิวจบระยะ 1): เปิด 19 · ปิด 15** — ปิดเพิ่ม #2 #9 (Task 9, PR #32) · #23 (Task 4, PR #31) · เพิ่มใหม่ #25–#34 จากหนี้ที่มองเห็น + เศษที่ส่งต่อใน §7 ด้านล่าง → **final review Part F: เปิด 21 · ปิด 14** — คืน #14 (กฎ roe) กลับเป็นเปิด เพราะกลไก `checkPairs` ยิงไม่ได้จริงบนคลังวันนี้ (§3) ไม่ใช่ "ปิดบางส่วน" · เพิ่ม #35 (`forecastLine` epsFwd===0) จากเศษ Part E | — |

**KPI จาก spec §7 ที่ยังวัดไม่ได้ในระยะ 1** (spec §7 วัด "ทุกรอบเคลียร์คิว" — ระยะ 1 ยังไม่มีรอบเคลียร์คิวจริงบนโค้ดครบทุกส่วน A–E ผ่าน runbook `npm run queue`):

| KPI (spec §7) | สถานะ |
|---|---|
| ปัญหาเชิงสาระที่คนจับได้ต่อรอบ | ยังไม่ได้วัด — รอรอบเคลียร์คิวจริง |
| re-dispatch รอบ 2 / จำนวนหุ้น | ยังไม่ได้วัด — รอรอบเคลียร์คิวจริง |
| turn ของ controller ต่อหุ้นในรอบเคลียร์คิว | ยังไม่ได้วัด — รอรอบเคลียร์คิวจริง |
| วันที่ cron ล้ม / เดือน | **มีหลักฐานบางส่วนแล้ว** (ไม่ใช่ "ยังไม่ได้วัด" ล้วน): §4 พบ **0 รอบล้มตั้งแต่ PR #31 ขึ้น main** · รอบ `b17b70c1` (12 ก.ย.) patch+push สำเร็จ 897/908 ใบ มี **1 ใบเข้า quarantine** (PONY, `patch-rejected`) ไม่ทำให้ทั้งรอบล้ม — ยังไม่มีรอบที่ถือโค้ดครบ A–E (§7 ด้านล่าง) |
| รหัส E/W ใหม่ / เดือน (คลาส price-derived) | วัดแล้วในตารางบน (3 รหัส W21–W23 ในเดือนเดียว) |
| ช่อง NEITHER / UNVERIFIED WRITE ใน manifest | วัดแล้วในตารางบน (0 / 0) |
| commit แก้ระบบ : commit วิเคราะห์ | ยังไม่ได้วัด — รอรอบเคลียร์คิวจริง/รอบ cron บนโค้ดใหม่ |
| หุ้นที่ส่ง LLM / หุ้นที่ติด flag ต่อสัปดาห์ | วัดแล้วในตารางบน (replay 24.6%) |
| prose ราคาค้าง · warning เปิด · ใบอายุ >90 วัน | prose ราคาค้าง: **ยังไม่ได้วัด** (786 ใบเดิม = เพดานก่อนระยะ 1 ยังไม่กวาดซ้ำ) · warning เปิด: วัดแล้ว **241** (ตารางบน) · ใบอายุ >90 วัน: **ยังไม่ได้วัด** — รอรอบ cron บนโค้ดใหม่ |

**จำนวนเคสของแต่ละขั้น verify (วัดจาก log ของ `npm run verify` รอบนี้):**

| ขั้น | เคส | ขั้น | เคส |
|---|---|---|---|
| 1 update-prices-test | 271 | 9 self-test | **404** |
| 2 dead-ticker-test | 51 | 10 ohlc-test | ผ่าน |
| 3 tag-apply-test | 57 | 11 ta-engine-test | ผ่าน |
| 4 queue-test | 281 | 12 build | 908 ใบ |
| 5 docs-test | 38 | 13 build-test | 146 |
| 6 prep-stock-test | 115 | 14 engine-exec | 908 |
| 7 tags-test | 68 | 15 skeleton-test | 32 |
| 8 check-reports | 908 × 47 | 16 check-site | ผ่าน |

> **ตัวเลขที่ต่างจาก ledger (วัดใหม่ ไม่ลอก):** W21 13→**11** · W22 33→**24** · W23 108→**109** · warning 303→**241** · self-test 403→**404**
> เหตุ: (ก) `bea4bc2d` กวาดช่องสรุปทั้งคลัง ⇒ **W06 861→0** (ledger บันทึกไว้แล้ว) (ข) คลังผ่าน price refresh ของจริง `b17b70c1` (12 ก.ย.) ⇒ ขา **f12 ↔ f10 (วันที่ disclaimer) จาก 9 ใบ เหลือ 0** เพราะ cron ประทับ `.disc` ใหม่ให้ตรงกับวันที่ราคา (ตรวจตัวอย่าง CKP: `4 ก.ย.` → `11 ก.ย.` ที่ `b17b70c1`) · f49 2→1 · f54 2→1 · f48 1→0 ตามราคาที่ขยับ (ค) **W23 เป็นขาที่ขยับตามราคา** (ราคาหลุดกรอบ 52 สัปดาห์) จึง +1 หลัง refresh — นี่คือเหตุผลที่ phase0-exit สั่งวัด W-code ซ้ำทุกครั้งก่อน/หลังเลื่อนระดับ

---

## 7. ยังเปิด

### ต้องตามต่อทันที

- **★ รอบ cron แรกที่ถือโค้ดครบทุกส่วน (A–E)** — รอบ 12 ก.ย. (`b17b70c1`) ถือแค่ A+B (พิสูจน์แล้ว §4(ง)) · รอบแรกที่ครบคือรอบถัดไปหลัง **PR #35** ขึ้น main (ตาราง `17 0 * * *` UTC = 07:17 น. ไทย · **สังเกตจริงว่าหน่วงไปลงเวลา ~11:50 น. ไทย สม่ำเสมอ 3 วัน** — ควรตรวจว่าเป็นการหน่วงของ GitHub หรือเอกสารเวลาในโปรเจกต์ล้าสมัย) · **สิ่งที่ต้องดูในรอบนั้น:** dead-band ±5 กรองคิวได้จริงไหม · `summaryPlan` เขียนช่องสรุปครบทุกใบไหม · มี `patch-rejected` เพิ่มไหม
- **ปฏิทินงบยังเป็น fallback (open-item #24)** — วัดจริง 12 ก.ย. 69: มีวันที่ **719/908 = 79.2%** · **หุ้นไทย `.BK` แค่ 51/238 = 21%** · US 668/670 ⇒ เกินเกณฑ์ "ไม่มีวันที่ >20%" จึง **ไม่ commit `earnings-calendar.json` และไม่เปิด workflow** ใช้นโยบายอายุอย่างเดียวไปก่อน · ตัวสร้าง + สาย `earningsAfterOf` อยู่ในรีโปครบแล้ว · **ต้องหาแหล่งวันงบไทย** (ปฏิทิน SET / StockAnalysis earnings date) ก่อนเปิด · ผลข้างเคียงที่ต้องจำ: เกณฑ์ 5 วัดได้ −72% **เพราะขาปฏิทินยังไม่ทำงาน** (§5)
- **★ cron ลงเวลาจริงอยู่กลางช่วง SET เปิดตลาด (open-item #33)** — workflow เขียน `17 0 * * *` UTC แต่ GitHub สร้างจริงเวลา 04:40–04:48 UTC (5/5 วัน) ⇒ ลง ~11:45 น. ไทย ซึ่งอยู่ในช่วงตลาดหุ้นไทยเปิด ⇒ guard `isIntradayQuote` ข้ามหุ้นไทยทั้งช่วง (230 ครั้งในล็อก 11 ก.ย.) ⇒ **หุ้นไทย refresh ได้จริงเฉพาะรอบวันเสาร์** (653–674 ใบวันธรรมดา เทียบ 897 ใบวันเสาร์) ⇒ replay ของเกณฑ์ 5 เอียงไปทาง US · **เจ้าของต้องตัดสินใจ**: เลื่อน schedule ไปราว 20:30 UTC (ยังก่อน SET เปิดแม้บวกหน่วง 4.5 ชม.) แล้ววัดใหม่ — "รับสภาพ" ไม่ใช่ทางเลือกเพราะกระทบทั้งความสดของราคาหุ้นไทยและตัวเลข KPI เกณฑ์ 5

### หนี้ที่ระยะ 1 ทำให้ "มองเห็น" แล้วจ่ายให้ระยะ 3

- **W23 = 109 ใบ** (ราคาหลุดกรอบ 52 สัปดาห์ที่พิมพ์ไว้) — ขาที่**ขยับตามราคาทุกวัน** ⇒ ห้ามเอาไปเลื่อนเป็น E โดยไม่รัน sweep ซ้ำ
- **W22 = 24 ใบ** — f47 legend ↔ FV 16 · f24 เป้าบน gauge ↔ การ์ดเป้า 4 · f46 fairLine ↔ FV 2 · f49 mFair ↔ FV 1 · f54 vcell ↔ .fv-box 1 · **ขา f12 ↔ f10 (9 ใบ disclaimer ระดับเดือนที่ ledger บันทึกไว้) = 0 แล้ว** หลัง cron ประทับ `.disc` รอบ 12 ก.ย.
- **W21 = 11 ใบ / 16 ช่อง** — f54 vcell กรอบ 9 · f47 legend 3 · f49 mFair 2 · f17+f18 ช่องส่วนต่าง 2
- **W15 = 96 ใบ** (% ของราคาเป้าใน prose) = หนี้เก่าก่อนระยะ 1 ครองสัดส่วนใหญ่สุดของ warning ที่เหลือ
- **f64 (ราคาในร้อยแก้ว) = `deferred` ระยะ 2** ตามข้อ A/B — เป็นช่องเดียวที่ยังไม่มี binding

### เศษที่ส่งต่อให้ Task 26 (open-items) — ✅ Task 26 บันทึกเป็นแถวใน `docs/open-items.md` แล้วทุกข้อ

แถวอายุเสีย `model`/`prepAt` เมื่อรันข้ามวัน (**#26**) · `shipStock` postcheck guard ไม่ผูกขอบเขตรอบ (**#27**) · `roundStart` ยังไม่มีด้านกลับ (**#28**) · `earnings-calendar --out` ข้ามกติกา fallback ได้ (**#29**) · `gen-docs` เขียนไฟล์ไม่ atomic + marker โผล่ใน code fence ของเอกสาร gen (**#30**) · เส้นทาง `--th` ของ forecast ยังไม่ได้ทดสอบ (**#31**) · ความสอดคล้อง `median-multiples` ↔ `fyYears` (**#32**) · **open-item #14 (กฎ roe) ที่ §3 ระบุว่ายิงไม่ได้ — ยังเปิดอยู่จริง ไม่ใช่ปิดบางส่วน** (final review Part F แก้กลับจาก "ปิดเชิงกลไก" เดิม เพราะ `checkPairs` ยิงไม่ได้เลยสักใบจนกว่า `baseEPS` อ่านเครื่องหมายลบ — ระยะ 2) · cron schedule skew ~4.5 ชม. จาก §4(ง) (**#33** — วัดผลกระทบเพิ่มแล้ว: refresh หุ้นไทยได้จริงเฉพาะรอบวันเสาร์) · รอบ cron แรกที่ถือโค้ดครบ A–E ยังไม่เกิด (**#34** — ซ้ำกับหัวข้อ "ต้องตามต่อทันที" ด้านบน) · หนี้ W22/W23 ที่มองเห็นใหม่ (**#25**) · `forecastLine` มอง `epsFwd===0` เป็นค่าหาย (**#35** — เศษ Part E)

### เงื่อนไขก่อนเริ่มระยะ 2

- **freeze การวิเคราะห์ใหม่ 1–2 วันระหว่าง migrate** — ระยะ 2 แตะโครงไฟล์รายงาน (token/ร้อยแก้ว) ⇒ worker ที่เขียนไฟล์พร้อมกันจะชนกับตัวแปลง
- ก่อนเลื่อน W ตัวใดเป็น E ในระยะถัดไป **ต้องรัน `sweep.sh` ซ้ำทั้งก่อนและหลัง** (กฎที่ phase0-exit §6 ตั้งไว้และรอบนี้พิสูจน์ว่าจำเป็น — W23 ขยับตามราคาจริง)

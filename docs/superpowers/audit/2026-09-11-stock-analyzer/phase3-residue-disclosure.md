# เปิดเผย residue v1 หลังระยะ 3 (legacy sweep) — 25/908 ใบ · 15 ก.ย. 2569

> วัดบน worktree `stock-analyzer-audit-9678cd` สาขา `claude/audit-p3-a` · เวลาไทย (UTC+7)
> ★ **ทุกตัวเลขในไฟล์นี้อ่านจากไฟล์ `reports/<SYM>.html` จริงในวันที่ทำ** ไม่ได้ลอกจากสำมะโนรอบก่อน (ซึ่งค้างที่ 14 ก.ย.)
> รีรันคำสั่งเดียวได้ตัวเลขชุดเดิม: `node tools/migrate-v2.js --cron-diff --census docs/superpowers/audit/2026-09-11-stock-analyzer`
> สำมะโนเต็ม (ทุกใบ ทุกชนิด) = `migration-v2-census.{md,json}` โฟลเดอร์เดียวกัน · ไฟล์นี้เปิดเผย**เฉพาะ 25 ใบที่ยังเป็น v1**

## 0. สรุป

| ผล | ใบ |
|---|---|
| คลังทั้งหมด | 908 |
| เป็น v2 | **883** (ระยะ 2 ย้าย 865 · ระยะ 3 Task 2–5 ย้ายเพิ่ม 18) |
| **ยังเป็น v1 (residue)** | **25** |
| ↳ หมวด A — ตัวเลขในใบขัดกันเอง ตัดสินเชิงกลไม่ได้ | **21** |
| ↳ หมวด B — เหตุผลอื่น (ไม่ใช่ความกำกวมของ valuation) | **4** (ACN FANG LWLG MU) |

**ผลต่อ `W22` (open-item #36):** residue > 0 ⇒ **ไม่ยกระดับ `W22` เป็น error รอบนี้** ตามกติกาเดิมของ #36
(รอบที่ v1 = 0 ให้ใช้ `healer:'patchReport'` ไม่ใช่ `'build'` — ดู #36 ในรายละเอียด)

> ★ **กฎการอ่านไฟล์นี้:** ทั้ง 25 ใบ **ห้ามเชื่อค่าใดค่าหนึ่งโดยอัตโนมัติ** — migrator ปฏิเสธเพราะมันตัดสินไม่ได้ว่าค่าไหนคือค่าที่ผู้วิเคราะห์ตั้งใจ
> ตอน re-analyze ให้ **ตรวจแหล่งปฐมภูมิ** (งบ/สมมติฐาน FV ในใบนั้น) แล้วเขียนค่าเดียวกันลงทุกจุด ไม่ใช่เลือกข้างจากตารางนี้
> สิ่งที่ตารางนี้รับประกันคือ "มีสองค่าอยู่จริงในใบเดียวกัน" เท่านั้น ไม่ได้บอกว่าค่าไหนถูก

---

## หมวด A — ตัวเลขในใบขัดกันเอง (21 ใบ · ambiguous)

migrator ชั้น 1 ปฏิเสธเมื่อ "ค่าเดียวกันในใบเดียว" ต่างกันเกิน 0.5% เพราะการย้ายเป็น v2 ต้องเลือกค่า **ค่าเดียว**
ไปใส่ `report-data.values` แล้ว render ทุกจุดจากค่านั้น ⇒ การย้ายจะ **กลืนค่าหนึ่งทิ้งเงียบ ๆ** ถ้าไม่มีคนตัดสินก่อน

### A1 · ป้าย legend ของกราฟ ≠ Fair Value ของใบ (16 ใบ)

ค่า A = ตัวเลขในป้าย legend ใต้กราฟ (`<div class="legend">… มูลค่าเหมาะสม $X`) ซึ่งเป็นข้อความคงที่ที่ผู้เขียนพิมพ์เอง
ค่า B = FV ที่เหลือทั้งใบใช้ (ป้าย gauge `#mFair` · การ์ด `.vcell` "มูลค่าเหมาะสม" · `report-data.fv` · `chart.fairLine`)

| ใบ | ค่า A (legend) | ค่า B (ที่เหลือทั้งใบ) | ต่าง | ฝั่งที่โดดเดี่ยว |
|---|---|---|---|---|
| COHU | $53 | $56 · $56 · `fv`=56 · `fairLine`=56 | 5.4% | legend |
| CTAS | $188 | $190 · $190 · `fv`=190 · `fairLine`=190 | 1.1% | legend |
| DTM | $120.65 | $97.11 · $97.11 · `fv`=97.11 · `fairLine`=97.11 | **24.2%** | legend |
| DXCM | $74 | $80 · $80 · `fv`=80 · `fairLine`=80 | 7.5% | legend |
| FORM | $142 | $138 · $138 · `fv`=138 · `fairLine`=138 | 2.9% | legend |
| GNTX | $32.00 | $33.67 · $33.67 · `fv`=33.67 · `fairLine`=33.67 | 5.0% | legend |
| HMPRO | ฿6.9 | ฿7.54 · ฿7.54 · `fv`=7.54 · `fairLine`=7.54 | 8.5% | legend |
| LPLA | $334.08 | $288.31 · $288.31 · `fv`=288.31 · `fairLine`=288.31 | **15.9%** | legend |
| RELX | $38.00 | $44.00 · $44.00 · `fv`=44 · `fairLine`=44 | **13.6%** | legend |
| SHANG | ฿44.35 | ฿39.60 · ฿39.60 · `fv`=39.6 · `fairLine`=39.6 | **12.0%** | legend |
| SWK | $95.00 | $100.00 · $100 · `fv`=100 · `fairLine`=100 | 5.0% | legend |
| TER | $340 | $365 · $365 · `fv`=365 · `fairLine`=365 | 6.8% | legend |
| **TT** | $447 | $447 · $447 · `fv`=**443** · `fairLine`=**443** | 0.9% | **`report-data` (ไม่ใช่ legend)** |
| VLTO | $96.50 | $97.66 · $97.66 · `fv`=97.66 · `fairLine`=97.66 | 1.2% | legend |
| VRTX | $520 | $480 · $480 · `fv`=480 · `fairLine`=480 | 8.3% | legend |
| WFC | $87 | $89 · $89 · `fv`=89 · `fairLine`=89 | 2.2% | legend |

> ★ **TT กลับข้างกับอีก 15 ใบ** — ที่นั่นทุกจุดที่คนเห็น (legend · gauge · การ์ด) ตรงกันที่ $447 แต่ `report-data.fv`/`chart.fairLine` = $443
> ⇒ เส้น fair value ในกราฟถูกวาดที่ $443 ขณะที่ตัวหนังสือทั้งใบพูดว่า $447 · อย่าเหมารวมกับ 15 ใบบนว่า "แก้ legend ก็จบ"
> ★ DTM (24.2%) · LPLA (15.9%) · RELX (13.6%) · SHANG (12.0%) ต่างมากพอที่จะพลิกคำตัดสินถูก/แพงของใบนั้น — ต้องมีคนอ่านก่อนเสมอ

### A2 · ป้าย gauge `#mFair` ≠ Fair Value (1 ใบ)

| ใบ | ค่า A (`#mFair`) | ค่า B (การ์ด `.vcell` · `report-data.fv`) | ต่าง | ฝั่งที่โดดเดี่ยว |
|---|---|---|---|---|
| SAPPE | ฿35 | ฿34.20 · `fv`=34.2 · `fairLine`=34.2 | 2.3% | `#mFair` (ป้ายหัวเข็ม gauge) |

### A3 · `chart.fairLine` ≠ `report-data.fv` (2 ใบ — ทั้งคู่อยู่ใน JSON ก้อนเดียวกัน)

ทั้งสองค่าอยู่ใน `report-data` ของใบเดียวกัน ⇒ **เส้น fair value ในกราฟกับ FV ที่การ์ด/gauge ใช้ ชี้คนละที่มาตั้งแต่ v1**

| ใบ | ค่า A (`chart.fairLine` — เส้นในกราฟ) | ค่า B (`report-data.fv` · การ์ด · gauge) | ต่าง | ฝั่งที่โดดเดี่ยว |
|---|---|---|---|---|
| PNC | $220 | $201 (legend $201 · `#mFair` $201 · การ์ด $201) | 9.5% | `chart.fairLine` |
| TEAM | $115 | $133 (legend $133 · `#mFair` $133 · การ์ด $133) | **13.5%** | `chart.fairLine` |

> TEAM: $115 บังเอิญเท่ากับ **ขอบล่างของกรอบ FV** ที่การ์ดพิมพ์ไว้ (`$115–$149`) ⇒ น่าจะเป็นการหยิบขอบล่างมาวาดเส้น ไม่ใช่ FV กลาง — แต่ **เดาแทนไม่ได้** ต้องยืนยันตอน re-analyze

### A4 · กรอบ (ช่วง) FV สองที่ไม่ตรงกัน (2 ใบ)

FV กลางตรงกันทุกจุด แต่ **ช่วง** ที่การ์ด `.vcell` พิมพ์ กับช่วงในกล่อง `.fv-box` (หมวดมูลค่า) ไม่ตรงกัน

| ใบ | ค่า A (กรอบในการ์ด `.vcell`) | ค่า B (กรอบใน `.fv-box`) | FV กลาง (ตรงกัน) | หมายเหตุ |
|---|---|---|---|---|
| ALLE | $158 – $168 | $150 – $170 | $163 | ช่วงต่างกันทั้งสองขอบ (แคบกว่า/กว้างกว่า) |
| POET | $3 – $20 | $8.10 – $18.40 | $9.50 | **ช่วงต่างกันเกือบเท่าตัว** — $3–$20 คือกรอบสถานการณ์สุดขั้ว ขณะที่ $8.1–$18.4 แคบกว่ามาก |

> POET เป็นหุ้น pre-profit (เกณฑ์ "หุ้นยาก" ตาม CLAUDE.md §3.2) ⇒ ตอน re-analyze ต้องใช้ effort high และตัดสินว่ากรอบไหนคือกรอบที่วิธีประเมินให้จริง

---

## หมวด B — 4 ใบที่ **ไม่ใช่** ความกำกวมของ valuation

ทั้ง 4 ใบไม่มี "สองค่าที่ขัดกัน" ให้เจ้าของเลือก — คนละสาเหตุ คนละทางแก้ จึงแยกหมวด

| ใบ | เหตุผล residue (คำต่อคำจาก migrator) | คลาสจริง | ทางแก้ |
|---|---|---|---|
| **ACN** | `จำนวนช่อง %/ปี ไม่เท่าเดิม (v1 4 → v2 3)` | **เนื้อหาจริงจะหาย** — ช่อง `.ret` ของคอลัมน์ base เขียน `รวม ~ −14% (+ปันผล ~5%/ปี)` · `~5%/ปี` ตัวนี้เป็น **dividend yield ไม่ใช่ CAGR ของฉาก** (`SCN_DIV_YIELD_BEFORE` ใน `tools/derived-values.js` ข้ามให้ถูกแล้ว) ⇒ v2 จะ render เหลือ `−14%` เปล่า · คอลัมน์อื่นเสียคำขยาย `(ก่อนปันผล)` / `(+ปันผล)` ด้วยแต่ไม่มีตัวเลขให้ยามจับ | **แก้เนื้อรายงานตอน re-analyze** (ย้ายคำว่า "+ปันผล ~5%/ปี" ออกจากช่อง `.ret` ไปไว้ใน `<ul>` หรือ hint) — ห้ามผ่อนยาม ห้ามฝืนย้าย |
| **FANG** | `site mFair match ≠ 1 (0)` | **ตัวค้นหาหยุดจริง ไม่ใช่ความกำกวม** — ป้าย gauge ของใบนี้เขียนว่า `mid-cycle FV $172` **ไม่มีคำว่า "เหมาะสม"** ที่ยามของ site ใช้เป็นสมอ ⇒ จับได้ต้องเลิกใช้คำในป้ายเป็นยาม (เหลือแต่ `id="mFair"`) ซึ่งเปลี่ยนความหมายของ site ทั้งคลัง — นอกขอบเขตระยะ 3 | ปล่อยเป็น residue **โดยตั้งใจ** (Task 2 ตรึงไว้เป็นเทสแล้ว จะได้ไม่มีใครผ่อนเผลอ ๆ) · ปิดเองเมื่อ re-analyze เขียนป้ายด้วยถ้อยคำมาตรฐาน |
| **LWLG** | `cron-diff v1-unstable gate-warn:W22` · `×0.87 ราคา 4.55: warning v1 [W22] ≠ v2 []` — v1 W22 ป้าย gauge mCur "ปัจจุบัน $px" ↔ `.px` header: **4.6 ≠ 4.55** | **บั๊ก rounding ของกลไก cron patch ฝั่ง v1** (พบตอน Task 4 · ไม่มีในสำมะโนระยะ 2) — ที่บางจุดของกริดราคา v1 เขียน `mCur` เป็น 4.6 ขณะ `.px` เป็น 4.55 ⇒ v1 (สมมติ) ยิง W22 แต่ v2 ยิงไม่ได้เลยเพราะทั้งสองจุดเป็น **token เดียวกัน** ⇒ การย้ายเป็น v2 **แก้ปัญหานี้** แต่ cron-diff ปฏิเสธการเปลี่ยนพฤติกรรมทุกชนิดโดยอัตโนมัติ (migrator ตัดสินเองไม่ได้ว่าคำเตือนที่หายไปคือ "ซ่อนปัญหา" หรือ "แก้ปัญหา") | ต้องมีคนยืนยันว่า W22 ที่หายคือการแก้จริง แล้วย้ายด้วย `--v2-baseline` (เกณฑ์ "v2 เองเสถียร" ของ Task 5) — **งานของรอบถัดไป ไม่ใช่ Task 6** |
| **MU** | `cron-diff gate-warn:W24` · v2 ยิง `W24` เพิ่ม: อ่านวันที่วิเคราะห์จาก footer ไม่ได้ — `"ข้อมูล ณ 24 มิ.ย.–13 ส.ค. 2026"` | **footer ผิดรูป ไม่ใช่เรื่อง valuation** — `FOOTER_RE` ไม่รับช่วงวันที่ข้ามเดือน ⇒ `E44`/`proseTokensIfNew` ข้ามทั้งใบ · ใบนี้มี prose ผูกราคา 5 จุด | ผูกกับ **open-item #38** (ขยาย `FOOTER_RE` ต้องเป็น task ของตัวเอง เพราะเป็นตัวอ่านร่วมของ `preflight`/`preserve-dates`/`postcheck`) · หรือแก้ footer เป็นวันที่เดียวที่ parse ได้ตอน re-analyze — **retry หลัง #38** |

---

## หมวด C — สิ่งที่ migration **ทำหายไปแล้ว** ในคลัง v2 (ไม่ใช่ residue — เปิดเผยเพื่อไม่ให้ลืม)

หมวดนี้ไม่ใช่ใบที่ยังเป็น v1 แต่เป็น **หนี้ที่การย้ายรอบก่อนสร้างไว้** ต้องอ่านคู่กันเสมอ

| คลาส | ขอบเขต | บันทึกที่ |
|---|---|---|
| ช่อง `.ret` หมวด 6 เสียคำขยาย/จำนวนเงินระหว่างย้าย 865 ใบของระยะ 2 | **39 ใบ** เสียคำขยายที่ไม่มีตัวเลข (`(รวมปันผล)` `(capital gain)` `(ราคา)` …) + **6 ใบ** เสีย**จำนวนเงิน** (PLD TRI PEP FR ECL BMY) · หนักสุด: **SO** เสีย `(capital gain)` และ hint ไม่ได้บอกฐานเลย ⇒ อ่านไม่ออกแล้วว่า +3% รวมปันผลหรือไม่ · **PLD** เสียตัวเลขปันผล `~$13` ทั้งก้อน | **`docs/open-items.md` #42** (เปิดใหม่โดย Task 6) |
| ช่อง `.ret` เสียคำประกอบตอนย้ายด้วย `--v2-baseline` (ระยะ 3 Task 5) | **WWD** เสีย `(3ปี)` ×3 คอลัมน์ · **THCOM** เสียเครื่องหมายประมาณ `~` ทั้ง 3 คอลัมน์ (`−47.1% (~−19.1%/ปี)` → `−47% (−19%/ปี)`) — คลาสเดียวกับแถวบน | รวมอยู่ใน **#42** |
| THCOM: ค่าที่คนเห็นเปลี่ยนจริง ฿8.02 → ฿8.01 (0.12% · ต่ำกว่า `GAP_REL` จึงถูกยอมรับ) | 1 ใบ | สำมะโน `migration-v2-census.md` หัวข้อ "ค่าต่างที่มีอยู่ก่อน patch" (กลับมาแสดงแล้วหลัง Task 6 แก้ `summarizeCronDiff`) |
| คลาสสีของช่องสรุปไม่ตรงเครื่องหมาย MOS | **14 ใบ** สีเขียวบน MOS ติดลบ (AKAM CEG COHU DELL DXCM DY ELV ETN EXPE GLW MSFT MXL NOK SAP) · หนักสุด DELL (−31.9) · SAP (−14) · **มีมาก่อนระยะ 3 ไม่ใช่ของที่การย้ายสร้าง** (13/14 เป็น v2 อยู่ก่อนแล้ว) | **`docs/open-items.md` #43** (เปิดใหม่โดย Task 6) |

---

## ภาคผนวก — วิธีตรวจซ้ำ

```bash
# ตัวเลข residue + เหตุผลรายใบ (ไม่เขียนไฟล์รายงาน — dry-run เสมอถ้าไม่มี --write)
node tools/migrate-v2.js --cron-diff --census docs/superpowers/audit/2026-09-11-stock-analyzer

# ดูว่าฝั่ง v2 ของใบหนึ่งเสถียรไหมบนกริดราคาเดิม (ใช้ตัดสิน LWLG ในรอบถัดไป)
node tools/migrate-v2.js --v2-grid LWLG
```

> ★ `--write` ต้องมาคู่ `--cron-diff` เสมอ (ยามในโค้ด ไม่มีตัวเลือกข้าม) · `--v2-baseline` ต้องระบุชื่อใบ ห้ามกวาดทั้งคลัง

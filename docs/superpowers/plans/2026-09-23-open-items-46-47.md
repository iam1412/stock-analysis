# ของค้าง #46 + #47 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ปิด #46 (กล่อง "เป้านักวิเคราะห์ 12 ด." สี hardcode จาก skeleton) และ #47 (`.ret` content drift 6 ใบ) — ทั้งสองข้อพบระหว่างทำแผนก่อนหน้า (`2026-09-23-open-items-triage-fix.md`, merged เป็น PR #65 แล้ว)

**Architecture:** #46 = ขยายกลไกเดียวกับ #43 ที่เพิ่ง merge (CSS class + `mosBand`-style checker) แต่ **ขอบเขตแคบกว่าที่ #46 ตั้งใจไว้ตอนเปิด** (ดูข้อค้นพบ ★ ด้านล่าง — เนื้อข้อความของกล่องนี้ไม่สม่ำเสมอทั้งคลัง ต่างจาก #43) · #47 = **น่าจะปิดได้โดยไม่ต้องแก้โค้ด** (ดูข้อค้นพบ ★★ — หลักฐานที่สำรวจใหม่ขัดกับสมมติฐานเดิมของแถว #47 เอง)

**Tech Stack:** Node ≥20.19 zero-dep, `test/check-reports.js` gate, `npm run verify` 18 ขั้น

**Spec:** `docs/open-items.md` แถว #46, #47 (ต้นทาง) · CLAUDE.md §8/§10 (design token rule, ใช้กับ #46 เหมือน #43)

## Global Constraints

- ทำใน worktree แยก (ใหม่ ไม่ใช่ `open-items-triage-fix` เดิมที่ merge ไปแล้ว) — PR + merge เท่านั้น
- commit trailer: `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`
- แก้ `test/check-reports.js` ต้องมี self-test mutation ก่อน push
- แก้ `reports/*.html` เป็นชุด ต้องท่า `heal/edit → npm run build → node tools/preserve-dates.js → npm run build`
- ห้ามแก้ `dist/`, `reports.json` มือ

## ★ ข้อค้นพบจากการสำรวจคลังจริง (23 ก.ย. 69) — เปลี่ยนขอบเขตจากที่แถว #46 เขียนไว้ตอนเปิด

แถว #46 ตอนเปิดสันนิษฐานว่ากลไกเหมือน #43 เป๊ะ ("ใช้กลไกเดียวกับ #43 ที่เพิ่งแก้ แต่คนละ sign source") — **สันนิษฐานนี้ผิด**: สำรวจ 902/909 ใบที่มีกล่องนี้ พบเนื้อข้อความในวงเล็บ **ไม่สม่ำเสมอทั้งคลัง** (ต่างจาก #43 ที่ทุกใบใช้ token `{{rd:mos}}` เดียวกัน):

| กลุ่ม | จำนวน | ตัวอย่าง |
|---|---|---|
| (a) token `{{rd:analystPct}}` | 115 | `~{{rd:analystTgt}} ({{rd:analystPct}})` |
| (b) เปอร์เซ็นต์ literal | 8 | `(+12%)` |
| (c) คำเรตติ้งเชิงคุณภาพ ไม่มี % | 647 | `~$100 (Hold)` · `~$143 (Buy/Hold)` |
| (d) อื่นๆ ปนกัน | 133 | ไม่มีวงเล็บเลย · "ไม่มี consensus" · อ้างแหล่งข้อมูลแทนความเห็น (`(Yahoo, n=1)`) |

**กลุ่ม (c)+(d) = 780/902 (86%) ไม่มีเปอร์เซ็นต์ให้อ่านเป็นเครื่องหมายในข้อความที่แสดงอยู่เลย** แต่ยืนยันแล้วว่า `values.analystTgt`/`px` **มีอยู่จริงและคำนวณ `analystPct` ได้เสมอ** (ฟังก์ชันเดิมที่ `tools/report-values.js:151` ไม่สนใจว่าข้อความที่แสดงจะเป็นอะไร) ยกเว้น ~20 ใบที่ไม่มี `analystTgt` เลย (ไม่มี consensus)

**★ แก้ไขจาก advisor review รอบ 1 — ข้อเท็จจริงที่ผิดในร่างแรก:** `{{ANALYST_RATING}}` **ไม่ใช่ dead token** — เป็นช่องที่ **worker กรอกเองตอนวิเคราะห์** (คลาสเดียวกับ `{{VERDICT_BODY}}` ในกล่อง skeleton บรรทัดใกล้กัน) ทดสอบผิดตอนสำรวจ (เช็คแค่ว่า "มี tool ไหนเติมให้อัตโนมัติไหม" ไม่ใช่คำถามที่ถูก — ต้องดูว่า worker กรอกเองไหม) 647 ใบที่โชว์ "(Hold)"/"(Buy/Hold)" คือ**ผลของ worker กรอกช่องนี้จริงตามที่ตั้งใจไว้** ไม่ใช่ของค้าง

**ทางเลือกที่ต้องให้เจ้าของตัดสิน (ไม่ใช่ advisor — เป็นการเปลี่ยนดีไซน์ที่มองเห็นได้บนเกือบทุกหน้าเว็บ เจ้าของขอ involve ทุกครั้งที่มีการตัดสินใจดีไซน์ที่มองเห็นได้):**
- **(C) — แนะนำ: ทำให้กล่องนี้ "ไร้สี" เหมือนกล่อง "มูลค่าเหมาะสม" ข้างๆ กัน** (`reports/DELL.html:310` — `<div class="v">{{rd:fv}}` ไม่มี `style`/`class` สีเลย) เพราะ **สีปัจจุบันไม่มีข้อมูลอะไรอยู่แล้ว** (เขียวทุกใบเหมือนกันหมดไม่ว่าเป้าจะสูง/ต่ำกว่าราคา) การลบสีออกจึงไม่เสียข้อมูลอะไร และตัดปัญหา "สีแดงข้างคำว่า Hold" ทิ้งไปเลยโดยไม่ต้องแก้อะไรซับซ้อน — ไม่มี writer ใหม่ ไม่มี checker ใหม่ ไม่มี CSS ใหม่ แก้แค่ skeleton (ลบ `style=`) + sweep ลบ `style=` ออกจาก 867 ใบที่ยังมี (902 − ~35 ที่ไม่มี style อยู่แล้วในกลุ่ม (d))
- **(A) — ทางเลือกที่ใส่สีจริง**: เพิ่ม class จาก sign ของ `analystPct` ที่คำนวณได้เสมอ (ไม่ว่าข้อความจะโชว์ % หรือคำเรตติ้ง) แบบ **2-tier `pos/neg`** (ไม่ใช่ 3-tier แบบ `mosBand` — analyst-target upside ไม่มี doctrine "bad/ok/good" แบบ MOS zone, การยืม threshold ของ mosBand มาใช้จะเป็นการเดาโดยพลการ) ยังคงมีปัญหา "สีไม่มีตัวเลขอธิบาย" ในกลุ่ม (c)/(d) 780 ใบอยู่ (ไม่มีทางแก้ที่เบาแล้ว — **ตัดตัวเลือกเสริม tooltip/`title` ออกจากแผนแล้ว** เพราะเป็นการเพิ่มของตกแต่งใหม่ใน report files ขัด CLAUDE.md §10)
- **~~(B) มาตรฐานข้อความ~~ — ตัดออกจากแผนแล้ว หลุด scope นี้โดยสิ้นเชิง**: advisor ชี้ว่า `report-values.js:164` (`analystPct: (d) => DV.fmtMos(need(d.analystPct, 'analystTgt'))`) ใช้ `need()` ซึ่ง **throw เมื่อค่าเป็น null** — บังคับ token `{{rd:analystPct}}` ใน ~20 ใบที่ไม่มี `analystTgt` เลยจะทำให้ **build พังทันที** ไม่ใช่แค่ "งานใหญ่กว่า" อย่างที่ร่างแรกเข้าใจ — ตัดทิ้งเป็นตัวเลือกที่ทำไม่ได้ ไม่ใช่แค่ไม่แนะนำ

**★ แก้ไขจาก advisor review รอบ 2 — นับจำนวนจริงแล้ว (grep ตรง ไม่ใช่ประมาณ):** กล่องนี้ **ไม่ได้มีแค่ `#a5d6a7` เขียวเดียวทั้งคลัง** — สำรวจ regex `style="color:#[0-9a-f]{6}"` บนกล่องนี้ทุกใบ พบ **879 ใบมีสี hardcode รวม 18 เฉดต่างกัน**: `#a5d6a7` 846 ใบ (ส่วนใหญ่) · เฉดเขียวอื่น 13 ใบ (`#81c995` ฯลฯ) · เฉดส้ม/อำพัน 13 ใบ (`#ffcc80`/`#ffd180`/`#ffe082`/`#ffd28a`) · **เฉดแดง/ส้มแดง 3 ใบ** (`#ff8a80` × 2 · `#ffab91` × 1) · เฉดเทากลาง 2 ใบ (`#cab9a8`)

**ตรวจ 3 ใบสีแดงแล้วว่าเป็น "สัญญาณจริง" หรือ "สุ่มเลือกผิด" (คำถามที่ advisor ตั้งไว้ก่อนสรุป):** `AEHR.html`/`WDC.html` (`#ff8a80`) และ `GIS.html` (`#ffab91`) **ทั้ง 3 ใบ prose ในไฟล์เขียนเองว่าราคาปัจจุบัน "ต่ำกว่า" เป้านักวิเคราะห์** (เช่น GIS: "เป้าเฉลี่ย $37.67 เหนือราคาปัจจุบันเพียงเล็กน้อย ~+3.7%") ⇒ **`analystPct` เป็นบวก (upside มีอยู่) แต่กลับให้สีแดง** — ตรงข้ามกับที่สีแดงควรสื่อถ้าเป็นระบบสัญญาณจริง ⇒ **สีที่ "ดูเหมือนตั้งใจ" ก็ยังเป็นการเลือกมั่ว ไม่ใช่สัญญาณที่เชื่อถือได้** เหมือนกับ 876 ใบที่เหลือ — **นี่คือหลักฐานที่หนักแน่นขึ้นสำหรับ (C)**: ไม่มีใบไหนในคลังที่สีปัจจุบันสื่อความหมายถูกต้องเลยสักใบที่ตรวจดู ⇒ ลบสีออกทั้งหมดไม่เสียสัญญาณที่ใช้งานได้จริงแม้แต่ใบเดียว

**การ์ดที่ไม่มี `analystTgt` เลย (~20 ใบ):** ไม่มีเครื่องหมายให้ใส่สี — ถ้าเลือก (C) ไม่กระทบอะไร (ไม่มีสีอยู่แล้ว) · ถ้าเลือก (A) ต้องมี fallback (ไม่ใส่ class)

**สิ่งที่ตัดสินได้แน่นอนแล้วไม่ต้องรอเจ้าของ/advisor:**
- ลบ inline `style="color:#a5d6a7"` ออกจาก skeleton ทั้ง th/us (บรรทัด 316) — เป็น anti-pattern เดียวกับที่ #43 แก้แล้ว (ไม่ว่าจะเลือก C หรือ A ก็ต้องลบ style นี้ทั้งคู่)
- **`{{ANALYST_RATING}}` ไม่ลบ** (แก้จากร่างแรก) — เป็นช่องที่ worker กรอกเองตามปกติ เก็บไว้ตามเดิมถ้าเลือก (C); ถ้าเลือก (A) ก็ยังเก็บไว้เหมือนกัน (แค่เพิ่ม class ข้างๆ ไม่ใช่แทนที่)
- ไม่มี checker ใดตรวจกล่องนี้อยู่เลยตอนนี้ (W08 ตรวจแค่ prose มี citation งบ ไม่ใช่กล่องนี้ · E42 ตรวจการ์ดคนละที่ในหมวด 1) — ถ้าเลือก (C) ไม่ต้องเพิ่ม checker เลย (ไม่มีอะไรให้ตรวจ ไม่มีสี ไม่มีเครื่องหมายที่ต้องตรงกัน)

## ★★ ข้อค้นพบจากการสำรวจ #47 — สมมติฐานเดิมของแถวอาจผิด (ต้องยืนยันเพิ่มก่อนตัดสิน)

สำรวจ 6 ใบที่แถว #47 ระบุ (WELL NI LNT DTE EVRG PEP) **ในสภาพปัจจุบัน** (ไม่ใช่ diff ประวัติ git แบบที่แถว #47 ใช้ตอนเปิด):
- **ทั้ง 6 ใบ ทั้ง 3 คอลัมน์ฉาก (bear/base/bull) สอดคล้องกันเองในปัจจุบัน** ไม่มีความขัดแย้งภายในไฟล์ที่ผู้อ่านจะเห็น ณ วันนี้
- **WELL ที่แถว #47 เรียกว่า "label flip" ยืนยันด้วยเลขคณิตแล้วว่าเป็นการแก้ไขที่ถูกต้อง** (bear: (222−232.76)/232.76 = −4.62% ตรงกับป้าย `(ไม่รวม div)` แบบ capital-only เป๊ะ) — **รูปแบบเดียวกับที่ #42 ต้องแก้ MAA/CPT/AP** (ป้ายเดิมผิด ป้ายใหม่ถูก ไม่ใช่บั๊ก)
- **`scenarioPlan` (`tools/derived-values.js:521`) ซึ่งเป็นตัวเดียวที่ patch `.ret` บนใบ v1 แก้แค่ตัวเลข % ไม่เคยแตะข้อความคำขยายเลย** — ไม่มี code bug ร่วมที่อธิบายการเปลี่ยนคำขยายได้ สาเหตุคือ analyst เขียน prose ไม่สม่ำเสมอข้ามรอบ UPDATE จริงตามที่ #47 ตั้งสมมติฐานไว้ (ไม่ใช่บั๊ก patcher)
- **ยังไม่ได้ scan ทั้งคลังแบบ exhaustive** เพื่อยืนยันว่า 6 ใบนี้คือทั้งหมดจริง (ทำแค่ grep คร่าวๆ) — ต้องทำให้ครบก่อนสรุปปิด

**★ แก้ไขจาก advisor review รอบ 1 — บล็อกข้อสรุป "ปิดด้วยเอกสารอย่างเดียว":** "สอดคล้องกันเอง 3 คอลัมน์" ≠ "ถูกต้อง" — การสำรวจตรวจแค่ WELL ด้วยเลขคณิตจริง ส่วน **NI/LNT/DTE/EVRG/PEP ตรวจแค่ว่าเขียน "รวม"/"ไม่รวม" ตรงกันทั้ง 3 คอลัมน์เท่านั้น ไม่ได้ตรวจว่าคำว่า "รวม" นั้น**ตรงกับตัวเลขจริง**ไหม** — ตรงกับที่ CLAUDE.md §8 เตือนไว้ตรงๆ ("ฐาน 'รวมปันผล' ถอดจากตัวเลขที่โชว์เอง ห้ามเชื่อคำว่า...") ⇒ **สมมติฐานเดิมของ #47 ("bear cell เสียคำขยายตอนคำนวณใหม่") อาจยังเป็นบั๊กจริงที่ซ่อนอยู่**: ถ้าตอนคำนวณเลขใหม่ (ตอนเพิ่ม per-year clause) ฐานเปลี่ยนเป็น capital-only แต่ป้ายยังเขียน "รวม" ค้างอยู่ — "สอดคล้องกันเอง" จะ**ซ่อน**บั๊กนี้ไว้ ไม่ใช่ล้างบั๊ก ⇒ **ต้องทำเลขคณิตแบบ WELL กับทั้ง 6 ใบ ไม่ใช่แค่ตรวจความสอดคล้องของคำ** — นี่คือจุดตัดสินจริงว่า "ปิดด้วยเอกสาร" หรือ "ต้องแก้เหมือน #42"

**Scan scope เพิ่มจาก advisor:** รวมใบ v2 ด้วย (ไม่ใช่แค่ v1 ทั้ง 6 ใบ) — เทียบป้ายท้าย `.ret` กับ **`report-data.values.scnBasis.divIncluded`** (แก้ field path จากร่างก่อนหน้าที่เขียนผิดเป็น `values.scnN.scnBasis...` — ยืนยันจาก `tools/report-values.js:123` ที่อ่าน `b.divIncluded` จาก **basis object เดียวของทั้งใบ ไม่ใช่แยกต่อ scenario** ตรงกับที่เห็นจริงใน MAA.html: `{"years":3,"divIncluded":false,"perYear":null}` เป็น object เดียว) เป็น cheap contradiction check ครั้งเดียว ไม่ใช่การเปิด checker ถาวรใหม่ (คำตัดสิน "ไม่เพิ่ม checker" จากรอบ #42 ยังยืนตามเดิม เว้นแต่ scan นี้เจอ pattern เป็นระบบในใบ v2 จำนวนมาก)

---

### Task 1: #46 — แก้กล่องเป้านักวิเคราะห์ (เจ้าของเลือก C แล้ว 23 ก.ย. 69)

**✅ เจ้าของเลือก (C) — ตัดสีออกทั้งหมด** (23 ก.ย. 69, หลังเห็นหลักฐานว่า 3 ใบที่ดูเหมือนตั้งใจให้สีแดงก็ยังผิดทิศทาง) — sub-steps ของ (A) ด้านล่างเก็บไว้เป็นบันทึกทางเลือกที่ไม่ได้ใช้เท่านั้น

**Files (ถ้าเลือก C):**
- Modify: `_template/skeleton-{th,us}.html` (ลบ `style="color:#a5d6a7"` เท่านั้น — เก็บ `{{ANALYST_RATING}}` ไว้เหมือนเดิม)
- Modify: `reports/*.html` × **879 ใบ** ที่มี `style="color:#…"` บนกล่องนี้ (**ทุกเฉด ไม่ใช่แค่ `#a5d6a7`** — นับจริงแล้ว มี 18 เฉดต่างกัน ดูตารางด้านบน) — sweep ลบ attribute ทั้งหมด ไม่แตะข้อความ/`ANALYST_RATING`

**Files เพิ่มเติมถ้าเลือก (A) แทน:**
- Modify: `tools/derived-values.js` (writer ใหม่ ใช้ `.pos/.neg` ไม่ใช่ `mosBand`)
- Modify: `_template/dashboard.css` (reuse `.pos/.neg` ที่มีอยู่แล้ว `dashboard.css:87` — ไม่ต้องสร้าง class ใหม่)
- Modify: `test/check-reports.js` + `test/self-test.js` (checker ใหม่ W-code warn, fallback สำหรับ ~20 ใบไม่มี `analystTgt` = ไม่ตรวจ/ไม่ใส่ class)

**Interfaces:**
- Consumes: `analystPct` ที่มีอยู่แล้ว (`report-values.js:151`) — เฉพาะถ้าเลือก (A) เท่านั้น (C ไม่ต้องใช้เลย)

- [ ] **Step 0: AskUserQuestion ถามเจ้าของ C vs A** (แสดงตัวอย่าง markup ก่อน/หลังทั้งสองแบบให้เห็นภาพ) — ห้ามข้าม
- [ ] **Step 1 (ถ้า C): แก้ skeleton ทั้ง th/us** ลบเฉพาะ `style="color:#a5d6a7"` ออกจาก `<div class="v" style="color:#a5d6a7">~{{rd:analystTgt}} ({{ANALYST_RATING}})</div>` เหลือ `<div class="v">~{{rd:analystTgt}} ({{ANALYST_RATING}})</div>`
- [ ] **Step 2 (ถ้า C): sweep ลบ `style="color:#[0-9a-f]{6}"` (regex ทุกเฉด ไม่ใช่ hardcode แค่ `#a5d6a7`) ออกจากกล่อง "เป้านักวิเคราะห์" ทั้ง 879 ใบ** — script ง่ายๆ regex replace ไม่ใช่ writer ใน derived-values.js เพราะไม่มี logic ต้องคำนวณอะไรเลย (ต่างจาก #43) · เช็คหลังรันว่าเหลือ 0 ใบที่มี `style="color:` บนกล่องนี้
- [ ] **Step 3 (ถ้า C): รัน `heal → npm run build → node tools/preserve-dates.js → npm run build`**
- [ ] **Step 1-8 (ถ้า A แทน): ทำตาม sub-steps ของ A** — เขียนเทส `self-test.js` ที่ยัง fail → implement checker (warn, 2-tier pos/neg) → implement writer (`.pos/.neg` ไม่แตะข้อความ) → จัดการ ~20 ใบไม่มี `analystTgt` (ไม่ใส่ class) → `npm run test:self` ผ่าน → sweep 902 ใบ → skeleton (ลบ style, เก็บ ANALYST_RATING)
- [ ] **Step 4: รัน `npm run verify` เต็ม 18 ขั้น**
- [ ] **Step 5: commit แยกจาก Task 2 เสมอ** (advisor ยืนยัน: #46 เป็นการเปลี่ยนดีไซน์ ~870 หน้า ส่วน #47 เป็นแก้ความถูกต้องไม่กี่ใบ/ปิดด้วยเอกสาร — ต้อง revert แยกกันได้ ห้ามรวม commit) — batch commit เดียวสำหรับ #46 เอง อธิบาย deviation (ไม่ใช่ 1 หุ้น 1 commit) ใน body

---

### Task 2: #47 — ยืนยัน exhaustive scan แล้วตัดสินใจปิดแบบมี/ไม่มีแก้โค้ด

**Files:**
- Create: สคริปต์ scan ชั่วคราว (scratchpad หรือ `tools/scratch/`) — ไม่ commit ถ้าไม่ใช่เครื่องมือถาวร
- Modify: `docs/open-items.md` แถว #47 (ปิดพร้อมหลักฐาน)
- Modify: `reports/WELL.html` (ถ้า Step 2 พบว่ายังมีจุดขัดแย้งจริง — ไม่คาดว่าจะต้องแก้ตามข้อมูลที่มีตอนนี้)

- [ ] **Step 1: ทำเลขคณิตแบบ WELL กับทั้ง 6 ใบก่อน (บังคับ ไม่ใช่ทางเลือก — และต้องเสร็จก่อน Step 2 เสมอ)** — ต่อทุกคอลัมน์ฉาก (bear/base/bull) ของ NI/LNT/DTE/EVRG/PEP (WELL ทำแล้ว = ถูกต้อง): ดึง `tgt`/`div`/`px` จาก `<li>` ในคอลัมน์นั้น คำนวณทั้ง capital-only `(tgt−px)/px` และ dividend-inclusive `(tgt+div−px)/px` เทียบกับ % ที่แสดงจริง — ป้าย "รวม"/"ไม่รวม" ต้องตรงกับสูตรที่ตัวเลขจริงแมตช์ ถ้าไม่ตรง = ป้ายผิดจริง (บั๊กที่ #47 สงสัยไว้ตั้งแต่ต้น ไม่ใช่แค่ "ดูเหมือนขัด") · **ผลลัพธ์ของ Step นี้กำหนดว่า Step 2 (เขียน scan script) ต้องหาอะไร** — ห้ามเขียน script ก่อนรู้ผล Step 1 (advisor เตือนว่าจะเสียเวลาถ้าเขียนสคริปต์ก่อนรู้ว่ากำลังหาอะไรจริงๆ)
- [ ] **Step 2 (เริ่มหลัง Step 1 เสร็จเท่านั้น): เขียน script scan ทุกใบ `.ret` 3 คอลัมน์ต่อ report หาความขัดแย้งคำขยายภายในไฟล์เดียวกัน** (เกณฑ์: คอลัมน์หนึ่งมีคำว่า "รวม"/"ไม่รวม"/basis word อีกคอลัมน์ไม่มีเลยหรือขัดกัน) รันทั้ง 909 ใบ (v1) **+ เทียบป้ายกับ `scnBasis.divIncluded` สำหรับใบ v2** ตามที่ advisor แนะนำ — ให้ครบกว่าที่สำรวจคร่าวๆ ไปแล้ว
- [ ] **Step 3: อ่านผล scan** — ทุกใบที่ scan เจอ (v1 หรือ v2) ต้องผ่านเลขคณิตแบบ Step 1 ก่อนตัดสินว่า "ป้ายผิดจริง" หรือ "ดูขัดแต่จริงๆ ไม่ขัด"
- [ ] **Step 4: แก้ป้ายที่ผิดจริงทั้งหมด** (6 ใบเดิม ถ้า Step 1 เจอปัญหา + ใบใหม่จาก Step 2-3 ถ้ามี) เหมือนแบบ #42 (กู้คืน/แก้ป้ายให้ตรงเลขคณิต) ตามท่า `heal → build → preserve-dates → build`
- [ ] **Step 5: ถ้า Step 1 + scan ยืนยันว่าไม่มีป้ายผิดเหลืออยู่จริง (ทั้งก่อนและหลัง Step 4)** → ปิด #47 ด้วยเอกสาร ใน `docs/open-items.md` พร้อมอ้างผลเลขคณิต + scan เป็นหลักฐาน (จำนวนใบที่ตรวจ/พบ/แก้ — ไม่ใช่แค่ "สอดคล้องกันเอง")
- [ ] **Step 6: รัน `npm run verify` ยืนยันไม่มี regression** (ถ้ามีการแก้ reports/ ใน Step 4)

---

## Self-Review

- **Spec coverage:** #46 (Task 1), #47 (Task 2) ครบทั้ง 2 ข้อ ✅
- **Placeholder scan:** Task 1 Step 0 รอเจ้าของตัดสิน C/A ก่อนเขียนโค้ดจริง — ตั้งใจ (เป็นการตัดสินใจดีไซน์ที่มองเห็นได้ ไม่ใช่คำถามเทคนิคที่ควรถาม advisor) · Task 2 Step 1 (เลขคณิต 6 ใบ) เป็น step บังคับก่อน scan ตามที่ advisor ยืนยันว่าจำเป็นเพื่อแยก "ปิดด้วยเอกสาร" กับ "ต้องแก้เหมือน #42" — ไม่ใช่ placeholder
- **Type/interface consistency:** Task 1 ตัดตัวเลือก B ที่ advisor พิสูจน์แล้วว่า build พัง (`need()` throw) ออกจากแผนทั้งหมด — ไม่เหลือ step ไหนอ้างถึง B อีก
- **แก้ตามคำตัดสิน advisor รอบ 1 ครบ:** (1) เพิ่มตัวเลือก C + ตัด B ✅ (2) แก้ข้อเท็จจริงเรื่อง `ANALYST_RATING` ไม่ใช่ dead token ✅ (3) เพิ่ม arithmetic check บังคับใน Task 2 ก่อนสรุปปิด ✅ (4) ตัด tooltip/title ออกจากตัวเลือก A ✅ — เหลือ **process note เรื่อง worktree** ที่ต้องจัดการก่อนเริ่ม execute (ดูข้อความหลัง AskUserQuestion)

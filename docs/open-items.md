# Open items — ของค้างของโปรเจกต์ (ย้ายจาก memory 11 ก.ย. 2569 · docs-audit §6)

> memory เป็นของ session ไม่ใช่ของโปรเจกต์ — ข้อเสนอที่อยู่แต่ใน memory เคยหาย (29 ส.ค. → ระเบิด 9 ก.ย.) · ไฟล์นี้คือที่เดียวที่บันทึกของค้าง · ปิดแล้ว = ย้ายไปตาราง "ปิดแล้ว" พร้อม commit/PR · เพิ่มใหม่ = 1 แถว ไม่เขียนย่อหน้า

## เปิดอยู่

| # | รายการ | ที่มา | ปิดใน |
|---|---|---|---|
| 1 | `%/ปี` ในประโยคสรุปหมวด 6 เป็น prose — ไม่มี warning/ตัวซ่อม · **ยังเปิดอยู่หลังจบระยะ 2** (Task 18 ทบทวนแล้ว): ระยะ 2 ทำให้**ตารางฉาก**หมวด 6 เป็น token (`{{rd:scNret}}` ซึ่งรวม `%/ปี` ในช่อง) แต่ **ประโยคสรุปที่เป็นร้อยแก้วไม่ถูกแตะ** — `RV.PROSE_BOUND` ของ `E44` มี 6 แพทเทิร์น (`mosZone` · `mosZoneRev` · `px` · `fv` · `mos` · `tgt`) **ไม่มีแพทเทิร์นของ `%/ปี`** ⇒ ทั้ง `E44` และ healer `proseTokens` มองไม่เห็นจุดนี้ · ★★ **ระยะ 3 Task 9 วัดคลังแล้ว — ruling: ยังไม่เปิด (`PROSE_BOUND` คง 6 แพทเทิร์น) · #1 ยังเปิดอยู่** (หลักฐานรันซ้ำได้: `docs/superpowers/audit/2026-09-11-stock-analyzer/probe-peryear.js` ดัน pattern ผู้สมัครเข้า `RV.PROSE_BOUND` ที่ export มาแล้วเรียก `RV.proseBoundHits` ตัวจริงทั้ง 908 ใบ — ไม่ต้องแก้ไฟล์จริง) · **เหตุผล 3 ข้อ เรียงตามน้ำหนัก**: (ก) ★ **ค่าที่จะผูกเป็นฟังก์ชันของราคา** ⇒ เกณฑ์กระพริบทุกรอบ cron — วัดจริงโดยขยับ `values.px`: ผู้สมัครที่มีป้าย ยิง 3 จุด แต่ขยับราคาแค่ **±2% จุดที่ยิงเปลี่ยน 3/3** (หายหมด เกิดใหม่ 1) · ผู้สมัครที่ไม่มีป้าย 12 จุด → เปลี่ยน 11/12 · **control: 6 แพทเทิร์นเดิม 5,234 จุด เปลี่ยน 0 จุดที่ ±2% และ ±5%** (ตรงกับที่ `tools/report-values.js` ประกาศไว้เองว่า `fv`/`mos20`/`mos30`/`analystTgt` ไม่ขยับตามราคา) ⇒ แพทเทิร์นนี้จะเป็นตัวแรกที่ผิดกติกา W18 (ใบที่ผ่านเมื่อวานตกวันนี้โดยไม่มีใครแก้อะไร) (ข) **false positive สูง** — แบบ "ค่าตรงเป๊ะอย่างเดียว" (ตรงตามที่ข้อ #1 เสนอ) ยิง **12 จุดทั้งคลัง อ่านครบทุกจุด (census ไม่ใช่สุ่ม) = false positive 9 จุด**: `ABT`(EPS adj โต) `BNS`(กำไรโต) `TSLA`(กำไรโต) `MCK`×2(EPS โต · บายแบ็กลดหุ้น) `FER`×2(g ของ DDM) `FNV`×2(GEO โต) — จริงแค่ 3 (`NTES` `TD` `TU`) · เติมป้าย "ผลตอบแทน/total return/CAGR/ทบต้น" แล้วเหลือ 3 จุดจริงล้วน **แต่ต่ำกว่าเกณฑ์ยืนยัน 15–20 จุดของ brief มาก** และชั้นป้ายเองรั่วอยู่ (พบเคสที่ป้ายผ่านแต่ scenario ไม่มี `perYear` นิยามไว้เลย — `scnBasis.perYear === null` ทำให้ไม่มีทางแมตช์ได้ตั้งแต่ต้น ไม่ใช่แค่ "ค่าไม่ชน") (ค) **ไม่มี token ให้ชี้** — `{{rd:scNret}}` render เป็นรูปประกอบ "+41% (+12.1%/ปี)" ไม่มี token ของเศษ `%/ปี` เดี่ยว ๆ ⇒ ถ้าเปิดต้องเพิ่ม token ใหม่เข้า schema v2 (นอกขอบเขต task) · ผู้สมัครที่จับ**รูปประกอบทั้งก้อน**ตรงกับที่ token render ยิง **0 จุดทั้งคลัง** ⇒ เปิดไปก็ไม่มีผล · **ตรึงคำตัดสินไว้เป็นเทสแล้ว**: `test/report-values-test.js` บล็อก (6) ยืนยัน (1) ชุดจุดที่ `PROSE_BOUND` ยิงต้องไม่ขยับเมื่อราคาขยับ +2% (2) ประโยค `%/ปี` ที่ตรงกับ perYear ของฉากเป๊ะต้องเงียบ — พิสูจน์ด้วย mutation (ดัน pattern ผู้สมัครเข้าอาร์เรย์ → เทสแดง 2 บรรทัด) · **ทางปิดที่แนะนำสำหรับรอบหน้าไม่ใช่ checker**: ให้ประโยคสรุปอ้าง `{{rd:scNret}}` (token ที่มีอยู่แล้ว ครอบทั้ง total และ %/ปี) เป็นกติกาการเขียนใน SKILL 5A แทนการเดาความหมายจากคำในย่อหน้า · **หมายเหตุจาก task review**: ตัวเลขบริบท "1,064 จุด/549 ใบ" ไม่รวม 43 ใบที่ยังเป็น v1 ตอนวัด (probe ข้ามไป) · E44 ยิง 0/908 ในวันที่วัด (ทุกใบยังอยู่ก่อน `PROSE_TOKEN_SINCE`) ⇒ FP-rate 9/12 ที่วัดได้เป็นอัตราที่จะเกิดกับใบ**ใหม่**ในอนาคต ไม่ใช่ความเสียหายที่เกิดแล้วตอนนี้ · ข้อ (ก) ที่ว่า "ค่าที่ผูกราคากระพริบ" เป็นคุณสมบัติของดีไซน์ "ค่าตรงเป๊ะ" (may-point-to-many) เท่านั้น — ดีไซน์ "owning" (ป้าย+หน่วยยิงไม่ดูค่า) วัดแยกต่างหากได้ 76 จุด/69 ใบ churn 0 ที่ ±2% (price-stable) แต่ไม่ผ่านด้วยเหตุผล (ข)/(ค) เหมือนกัน — รอบหน้าที่จะพิจารณาใหม่ควรเริ่มจากดีไซน์ owning + token ใหม่ ไม่ใช่ดีไซน์เดิมที่ตกรอบนี้ | price-derived-staleness:113 | ระยะ 3 (คู่กับ `f64` ที่ deferred ตามข้อ A/B) — ถ้าจะปิดต้องเพิ่มแพทเทิร์นเข้า `PROSE_BOUND` + วัดคลังก่อนเปิด (บทเรียน W18: เกณฑ์ที่กระพริบตามราคาใช้เป็น error ไม่ได้) |
| 5 | W05 มองไม่เห็น "สองขาตระกูลเดียว" (WWD) | price-derived-staleness:174 | ระยะ 3 WS7 fix-on-touch — ไม่ได้แก้ในระยะ 1 (manifest/WS9(a) ที่ปิดแล้วไม่ครอบคลุมเคสนี้ ดู phase1-exit §7) |
| 6 | การ์ด EV/Sales · EV/EBITDA · DCF ไม่มี checker | data-source-traps:258 | ระยะ 3 WS7 fix-on-touch — ไม่อยู่ใน 18 แถว NEITHER ที่ปิดระยะ 1 (ดู phase1-exit §2/§7) |
| 11 | 6 ใบสองขาตระกูลเดียว ยังไม่แก้ (กฎยอมรับแล้ว 22 ก.ย. — fix-on-touch เฉพาะ UI, ดู RULE-TENSION-001 `docs/decisions.md` §8) | fv-family-weighting:22 | ระยะ 3 WS7 fix-on-touch (ลิสต์ในไฟล์ memory) |
| 14 | `stock-meta.roe` ต้อง null เมื่อขาดทุน (OKJ) — กฎ `checkPairs` (f57↔f40) มีอยู่แล้วตั้งแต่ Task 9 PR #32 | bulk-stock-analysis-workflow:81 | **แก้บางส่วนแล้วโดยอ้อม (ระยะ 2 ส่วน E — Task 16 ตรวจ 14 ก.ย. 69)**: ไฟล์ v2 (884/908 ณ 15 ก.ย. 69 · เดิม 865/908 ตอนตรวจ) ให้ `ctx.baseEPS` = `report-data.values.baseEps` ตรง ๆ (`tools/report-values.js`) ซึ่งเป็นเลข JSON ที่คงเครื่องหมายลบครบ ⇒ กฎ f57 ยิงถูกต้องแล้วสำหรับ v2 โดยไม่ต้องแก้อะไรเพิ่ม (ยืนยันด้วยโค้ด — ยังไม่พบใบ v2 ที่ `values.baseEps < 0` จริงในคลังตอนนี้ให้ดูยิง) · **ยัง blocked จริงสำหรับ 24 ใบ v1 ที่เหลือ** (ตอนตรวจเป็น 43 ใบ — ระยะ 3 กวาดลงเหลือ 24 · ดู #36): `V1_READ.baseEPS` (`test/check-reports.js:221`) ยังใช้ regex `/EPS ฐาน…([0-9.]+)/` ที่ไม่จับเครื่องหมายลบหน้าเลข ⇒ EPS ฐานติดลบในไฟล์ v1 จะอ่านเป็นค่าบวกเสมอ ไม่ต้อง promote ก่อน — ปิดเต็มเมื่อ v1 = 0 (ดู #36) หรือแก้ regex ให้จับ `-`/`−` นำหน้า |
| 15 | คำขอคำศัพท์ 49 รายการใน `tags.json.requests` รอเจ้าของ | tag-system-2026-08:19 | เจ้าของรีวิว |
| 16 | `stock-ai.dotent.workers.dev` ยังเปิด (`workers_dev=true` ห้ามลบ) traffic ไม่ cache | workers-dev-cache-inert:21 | ไม่ทำ (บันทึกไว้) |
| 17 | ไม่มี `x-cache` header — วินิจฉัย cache จากเวลาอย่างเดียว | workers-dev-cache-inert:20 | ไม่ทำ (บันทึกไว้) |
| 24 | **ปฏิทินงบ: Yahoo ให้วันที่ <80%** — วัดจริง 12 ก.ย. 2569 รอบเต็ม 908 ใบ: มีวันที่ 719 (79.2%) · **TH `.BK` แค่ 51/238 (21%)** · US 668/670 ⇒ ไม่มีวันที่ 20.8% เกินเกณฑ์ 20% ⇒ ไม่ commit `earnings-calendar.json` / ไม่เปิด workflow (ตัวสร้าง `tools/earnings-calendar.js` + สาย `earningsAfterOf` ใน preflight อยู่ในรีโปแล้ว ใช้นโยบายอายุอย่างเดียวไปก่อน) — ต้องหาแหล่งวันงบหุ้นไทย (ปฏิทินหลักทรัพย์ SET / StockAnalysis earnings date) มาเสริมก่อนเปิด · **ตอนเปิดใช้ให้รู้ไว้:** ตัวสร้างมีกติกา carry-forward แล้ว (รอบที่ยิงล้ม = "ถามไม่ได้" → ยก `next` เดิมมาต่อ ไม่ล้างทิ้ง · ยิงสำเร็จแต่ไม่มีวันที่ = "คำตอบจริง" → ล้าง) และเกณฑ์ 20% คิดจาก `ไม่มีวันที่ / (ทั้งหมด − ยิงล้ม)` (`EC.nodateRatio`) | Task 17 (WS6 ข้อ 2) | ระยะ 3 WS7 (คู่กับ #20 EPS ค้างตามปฏิทินงบ) |
| 25 | **หนี้ที่ระยะ 1 ทำให้ "มองเห็น" แล้ว** — W22 ยิง 24 ใบ (f47 legend↔FV 16 · f24 เป้า gauge↔การ์ด 4 · f46 fairLine↔FV 2 · f49 mFair↔FV 1 · f54 vcell↔.fv-box 1) · W23 ยิง 109 ใบ (ราคาหลุดกรอบ 52 สัปดาห์ที่พิมพ์ไว้ — ขยับตามราคาทุกวัน ห้ามเลื่อนเป็น E โดยไม่รัน sweep ซ้ำ) | phase1-exit §7 | ระยะ 3 fix-on-touch (WS7) |
| 29 | `earnings-calendar.js --out` ข้าม gate `--write` และ guard `--limit` ได้ (สงบนิ่งอยู่ตราบที่ปฏิทินงบยังปิดใช้งาน — คู่กับ #24) | phase1-exit §7 | งานเปิดใช้ปฏิทินงบ (ตอนปิด #24) |
| 36 | **W22 → error รอ v1 = 0** — ★ **นับใหม่หลังระยะ 3 Task 2–6 + Task 14 (15 ก.ย. 2569): v1 เหลือ 24/908 ใบ ⇒ residue > 0 ⇒ ยังไม่ยกระดับ `W22`/`level`/`healer` รอบนี้เช่นกัน** · 24 ใบคือ `ACN ALLE COHU CTAS DTM DXCM FANG FORM GNTX HMPRO LPLA LWLG PNC POET RELX SAPPE SHANG SWK TEAM TER TT VLTO VRTX WFC` — **เปิดเผยค่าที่ขัดกันรายใบไว้แล้วที่ `docs/superpowers/audit/2026-09-11-stock-analyzer/phase3-residue-disclosure.md`** (หมวด A = 21 ใบ ambiguous ตัดสินเชิงกลไม่ได้ · หมวด B = 3 ใบคนละคลาส: ACN เนื้อหาจริงจะหาย · FANG ป้าย gauge ไม่มีคำว่า "เหมาะสม" (ตรึงเป็นเทสโดยตั้งใจ) · LWLG บั๊ก rounding ของ cron patch ฝั่ง v1 ที่ v2 แก้ให้ รอคนยืนยันแล้วย้ายด้วย `--v2-baseline`) · ★ **MU ย้ายแล้วใน Task 14 fix round (25 → 24)** — ตอน Task 6 ยังติด `cron-diff gate-warn:W24` เพราะ `FOOTER_RE` อ่านช่วงวันที่ข้ามเดือนไม่ได้ แต่ **Task 8 (`5e4c668c`) แก้ `FOOTER_RE` ให้รับช่วงข้ามเดือนแล้ว** ⇒ ตัวบล็อกหายไปเอง (merge หลัง Task 6 จึงไม่มีใครกลับไป migrate ซ้ำ) · รอบนี้ `--cron-diff` ผ่าน 61/61 จุด ต่างแค่รูป · สำมะโนตรงกับไฟล์จริงแล้ว (`--census` รอบ 15 ก.ย. — ก่อนหน้านี้ค้างที่ 43 เพราะ Task 2–5 ย้าย 18 ใบโดยไม่ได้สั่ง `--census` และ `mergeCensus` คงแถว residue เดิมไว้ · แก้แล้วใน Task 6) · **ประโยคเดิมด้านล่างเก็บไว้เป็นบันทึกของรอบ 14 ก.ย.** — นับคลังหลัง merge ส่วน E (14 ก.ย. 2569): v1 ยังเหลือ **43/908** ใบ ⇒ ตาม Task 16 brief ("ถ้า v1 > 0 ไม่แตะ level") **ไม่ยกระดับ** `W22`/`level`/`healer` รอบนี้ · residue 43 ใบต่อชนิด (จาก `docs/superpowers/audit/2026-09-11-stock-analyzer/migration-v2-census.md` + `migration-v2-census.json`): legend≠FV >0.5% 16 (COHU CTAS DTM DXCM FORM GNTX HMPRO LPLA RELX SHANG SWK TER TT VLTO VRTX WFC) · จำนวนช่อง %/ปี ไม่เท่าเดิม 5 (ACN JMT RS SCGD SNNP) · ค่าไม่ตรงชั้น 1 (f36/f50) 4 (AMKR LWLG MTI TMAN) · site vcellTgt match ≠1 3 (BABA STX VRT) · site legend match ≠1 3 (FANG MPC MU) · site vcellFv match ≠1 2 (AEM BTG) · กรอบ FV สองที่ไม่ตรงกัน (vcell↔fv-box) 2 (ALLE POET) · FV ไม่ตรงกันเอง (chart.fairLine↔report-data.fv) 2 (PNC TEAM) · cron-diff v1-unstable gate-warn:W22 2 (PTG THCOM) · cron-diff v1-unstable meta:dividendYield,visible 1 (BNY) · site summary match ≠1 1 (MXL) · mFair≠FV >0.5% 1 (SAPPE) · cron-diff v1-unstable visible 1 (WWD) · **W21 คงเป็น warn ทั้งสองกรณี** (`healer: null` — เหตุผลเดิมจาก Task 13: f67 เป็น `owner:'worker'` ไม่มีฟังก์ชันซ่อมอัตโนมัติเทียบเท่า `mirrorStockMetaV2`) · ★ **แก้ข้อความ brief ของ Task 16 เอง**: brief เขียนว่ารอบที่ v1=0 ให้ตั้ง `healer:'build'` แต่ `task-13-report.md` หัวข้อ "ข้อสังเกตส่งต่อ (แก้ไขจากรอบแรก — ให้ Task 16)" **แก้ไขค่านี้แล้ว**: ต้องใช้ `healer:'patchReport'` (มีอยู่แล้วใน `HEALERS`) เพราะ `'build'` ถูกถอดออกจาก `HEALERS` ทั้งหมดใน Task 13 (ไม่มี check ใดในคลังเข้าเงื่อนไข "เสียเฉพาะเพราะ HTML render ค้างจาก JSON โดยไม่มีฟังก์ชันซ่อม") — รอบถัดที่ v1 = 0 ต้องใช้ `'patchReport'` ไม่ใช่ `'build'` · **หมายเหตุเพิ่ม (open-items-triage-fix 23 ก.ย. 69, แทนการเปิด checker ใหม่)**: จุดบอด `tools/migrate-v2.js:901` ที่ทำให้คำขยาย `.ret` หมวด 6 หายเงียบตอน migrate (ดูรายละเอียดเต็มที่แถว #42 ที่ปิดแล้ว) ยังมีผลกับ 24 ใบ v1 residue ข้างต้นทุกใบที่ยังไม่ถูก migrate — ใครก็ตามที่ migrate ใบเหล่านี้ในอนาคตต้องตรวจมือว่า `.ret` ไม่ถูกทำหายเงียบ ๆ เหมือนที่ #42 ต้องกู้คืนย้อนหลัง · **อัปเดต 23 ก.ย. 69 (census ซ้ำ + advisor consult)**: รีรัน `node tools/migrate-v2.js --cron-diff --census` พบว่า**สมมติฐานเดิมของหมวด A (21 ใบ "ตัวเลขขัดกันเอง ตัดสินเชิงกลไม่ได้") ตายแล้ว** — แคมเปญ 908 ใบ (21–22 ก.ย.) เขียนเนื้อหาของ 20/24 ใบใหม่ทั้งหมด (ยืนยันด้วย `git log --follow -- reports/ACN.html`: commit `0b656ac00` "analyze: update ACN — UPDATE" 21 ก.ย. 69) แต่**ยังคงเขียนเป็น v1 format เดิม ไม่ได้สลับไปใช้ v2 skeleton** (SKILL 5B ไม่บังคับ v2 บน UPDATE ของใบเดิม — ช่องว่างที่ทำให้ residue เกิดซ้ำได้แม้เนื้อหาสดแล้ว) ⇒ สิ่งที่บล็อกการย้ายตอนนี้เปลี่ยนจาก "ค่าขัดกัน" เป็น "E44 prose ผูกราคา" ล้วน (migrator เองมี gate เข้มกว่า `check-reports.js` E44 เพราะ E44 เงียบบนไฟล์ v1 เสมอ — เพิ่งพบว่าเป็นช่องว่างจริง ไม่ใช่บั๊ก) · **พิสูจน์ด้วยมือกับ ACN แล้ว**: procedure ที่ใช้ได้จริง = extract ผ่าน `MV.extractValues/tokenise/buildRd` (exported ทั้งหมด) → เขียนไฟล์ candidate (v2) → tokenize prose ที่เหลือด้วยมือ (สั้นแค่ 3–4 จุด/ใบ) → `build → preserve-dates → build` → `npm test` — **แต่ ACN เจอ content loss จริงระหว่างทาง**: `.ret` ของหมวด 6 มี `(+ปันผล ~3.6%/ปี)` ฝังอยู่ (dividend yield ไม่ใช่ CAGR ของฉาก) ที่ `buildRd` ไม่รักษาไว้ ตรงกับที่ `phase3-residue-disclosure.md` หมวด B เตือนไว้ล่วงหน้าแล้วว่า ACN เป็นเคสพิเศษ ⇒ **revert ACN กลับ ไม่ commit** · เช็ค `.ret` ของอีก 19 ใบด้วยมือ (grep annotation shape): **RELX มีความเสี่ยงเดียวกัน** (`(~$39.12 รวมปันผล)` ฝังจำนวนเงินสะสมอยู่ใน `.ret`) · อีก 18 ใบ (ALLE COHU CTAS DTM DXCM FORM GNTX HMPRO LPLA LWLG PNC POET SHANG SWK TEAM TER VLTO VRTX) annotation อยู่ในรูปที่ migrator รู้จักอยู่แล้ว (`%/ปี` เปล่า/`(3 ปี)`/`(รวมปันผล)`/`(ไม่รวมปันผล)` — ตรงกับ `HINT_NOTE_TAIL_RE`/`clauseNote` ที่โค้ดมีกลไกรองรับ) **น่าจะย้ายได้แบบกลไกล้วน แต่ยังไม่ได้รัน `verifyPair` ยืนยันทีละใบจริง** (ตรวจแค่ด้วยตาจาก regex — ยังไม่นับเป็นพิสูจน์) · **สรุปสถานะ 24 ใบเดิม ณ 23 ก.ย. 69**: ~~SAPPE/TT/WFC ยังกำกวมจริง~~ **แก้แล้ว 23 ก.ย. 69** — ตรวจแหล่งปฐมภูมิ (ตัวเลข valuation methods ในใบเดียวกัน) แล้วพบว่าไม่กำกวมจริง มีจุดเดียวที่ค้าง/พิมพ์ผิดต่อใบ: **SAPPE** gauge `#mFair` ค้างที่ ฿35 (ของเดิมก่อนปรับ) prose ในใบเองบอกตรง ๆ ว่า "Fair Value ปรับลงเหลือ ฿34.20" → แก้ label ให้ตรง `report-data.fv` · **TT** กลับข้าง — `report-data.fv`/`chart.fairLine`/`gauge.fair` ค้างที่ 443 ทั้งที่ทุกจุดที่คนเห็น (legend/fv-box/gauge label/vcell/prose/`stock-meta.fairValue`) พูดตรงกันที่ 447 ซึ่งตรงกับค่าเฉลี่ย 3 วิธี ($439.23+$472.00+$430.30)/3=$447.18 พอดี → แก้ JSON ให้ตรง 447 (ไม่แตะ 3 วิธี) + แก้กรอบ fv-box `$427–$472`→`$430–$472` ให้ตรง min/max จริงของ 3 วิธี · **WFC** legend เขียน $87 (คือขอบล่างของกรอบ $87–$93 ไม่ใช่ค่าเฉลี่ย) ทั้งที่ทุกจุดอื่นพูดตรงกันที่ $89 → แก้ legend commits: SAPPE `f96a7e333` · TT `f5c407324` · WFC `e6756a124` · FANG ยังเป็น residue ตั้งใจ (ป้าย gauge) · ACN + RELX ต้องย้ายคำอธิบายปันผลออกจาก `.ret` ก่อนย้าย (งานตัดสินใจเรื่อง placement ไม่ใช่ค่า) · 18 ใบที่เหลือ **น่าจะ**เป็นงานกลไกล้วน รอ `verifyPair` ยืนยันทีละใบก่อนลงมือจริง | Task 16 · ระยะ 3 Task 6 (`test/check-reports.js` count · `migration-v2-census.md` · `phase3-residue-disclosure.md` · `task-13-report.md`) · อัปเดต census 23 ก.ย. 69 · SAPPE/TT/WFC ปิด 23 ก.ย. 69 | **อัปเดต 23 ก.ย. 69 (รอบสอง — migrate จริง)**: รัน `verifyPair`-equivalent (`checkStripped`/`scnPyCountGuard`) ตรงกับ 18 ใบที่เหลือ (ไม่ใช่ regex-eyeball แล้ว) — ทั้ง 18 ได้ `null`/`null` (ไม่มี content stripped ไม่มี %/ปี นับเพี้ยน) ⇒ ปลอดภัยจริงตามที่คาด · migrate ครบ: extract candidate (v2) ผ่าน `MV.extractValues/tokenise/buildRd` (exported) → splice `{{rd:…}}` ทับทุกจุดที่ `RV.proseBoundHits` ชี้ (ตำแหน่ง exact ไม่ใช่ text-search กันชนกรณีเลขซ้ำ) → build → preserve-dates → build → `npm test` **commit `e1435d285`** (ALLE COHU CTAS DTM DXCM FORM GNTX HMPRO LPLA LWLG PNC POET SHANG SWK TEAM TER VLTO VRTX — 18 ใบ) — ยืนยันด้วยมือ 1 ใบ (ALLE) ก่อนรันแบตช์ที่เหลือ: เลข `.ret` เปลี่ยนเพราะคำนวณสดจาก `values.scenarios`+px ปัจจุบัน (ราคาที่ขยับตั้งแต่วิเคราะห์) ไม่ใช่ข้อมูลหาย · SHANG ได้ W21 ใหม่ (vcell กรอบใช้คำ "กรอบเต็ม" ที่ regex ไม่รู้จัก — คำเดิมจาก v1 ก่อน migrate ไม่ใช่ของใหม่ ปล่อยไว้ตาม "ตัดสินไม่ได้ = ไม่แตะ") · **SAPPE/TT/WFC migrate ต่อได้ทันทีหลังแก้ #36 เสร็จ** (เดิม migrate-v2 ปฏิเสธเพราะค่าขัดกันเอง — พอแก้แล้วเหลือแค่ E44 เหมือน 18 ใบข้างบน) **commit `a28da3a53`** · **v1 residue สุดท้าย 23 ก.ย. 69 (รอบสาม) = 1 ใบ**: **ACN แก้แล้ว** — ตรวจเลขคณิตจริงพบว่า base/bull label "(+ปันผล ~3.6%/ปี)"/"(+ปันผล)" ผิด (capital-only ตรงกับตัวเลขที่โชว์ทั้ง 3 คอลัมน์ทุกจุด ไม่ใช่ dividend-inclusive ตามที่ป้ายอ้าง — คนละสาเหตุจากที่ residue doc เดาไว้ว่า "เนื้อหาจริงจะหาย ต้องย้ายไปไว้ที่อื่น") ⇒ **แก้ป้ายให้ตรงเลขคณิตแทนการย้าย** (เหมือน COR/WELL/SAPPE) commit `b6c20864e` แล้ว migrate commit `50740677c` · **RELX ไม่ต้องแก้อะไรเลย** — รัน `checkStripped`/`scnPyCountGuard` ตรง ๆ (ไม่ใช่ eyeball แบบที่เคยตั้งข้อสงสัยไว้ผิด) ได้ `null`/`null` ทั้งคู่ ⇒ ปลอดภัยแบบเดียวกับ 18+3 ใบก่อนหน้า (ตัวเลข "(~$39.12 รวมปันผล)" ที่หายตอน migrate เป็นการลดทอนที่ยอมรับได้ เหมือน "(3 ปี)" ที่หายจากใบอื่น ไม่ใช่ content loss จริง) migrate commit `50740677c` · ****อัปเดต 23 ก.ย. 69 (รอบสี่ — ปิดครบ) — v1 residue = 0**: FANG แก้แล้วเหมือนกัน — ตรวจแล้วพบว่า "mid-cycle FV $169" ที่ป้าย gauge เป็นแค่จุดเดียวที่ไม่สอดคล้องกับที่เหลือทั้งใบ (legend/fv-box/vcell ใช้ "เหมาะสม ... mid-cycle" กันหมดอยู่แล้ว) ⇒ แก้ป้ายให้ตรงรูปแบบเดียวกัน ("เหมาะสม mid-cycle $169") ไม่ใช่การเลิกใช้คำเป็นยาม (option ที่ residue doc ปฏิเสธไว้ 15 ก.ย. — นั่นเป็นคนละทางแก้) · ระหว่างทางเจอราคาเก่า $192.42 ติดอยู่กับ "(~14%)" ที่จะเพี้ยนถ้า migrate ตรงๆ (px จะขยับเป็น $184.50 อัตโนมัติแต่ % ไม่ขยับตาม) ⇒ แก้ "(~14%)" → "(~9%)" ให้ตรงเลขคณิตที่ px ปัจจุบันก่อน migrate (2 จุดที่คล้ายกันที่ใช้คำว่า "ราคา" ไม่ใช่ "ราคาปัจจุบัน" ไม่ถูกแตะเพราะ tokenizer จับไม่ได้อยู่ดี ค้างเป็น W15-class ตามปกติ ไม่ใช่ปัญหาใหม่) commit `d4579dea4` — ยืนยัน `checkStripped`/`scnPyCountGuard` = null ก่อน migrate เหมือนทุกใบก่อนหน้า · **v1 residue = 0 ใบ จากทั้งหมด 24 ใบเริ่มต้น (15 ก.ย. 69)** | Task 16 · ระยะ 3 Task 6 · อัปเดต census 23 ก.ย. 69 · migrate ครบ 24/24 ใบ 23 ก.ย. 69 | **2 งานต่อเนื่องที่เพิ่งถูกปลดล็อก แต่ยังไม่ได้ทำ (แยกเป็น task ของตัวเอง ไม่รวมเข้า migration commit ตาม advisor)**: (1) ยกระดับ `W22` เป็น error + `healer:'patchReport'` (ตามที่ #36 เขียนกำกับไว้เองว่า "รอบที่ v1=0 ต้องใช้ patchReport ไม่ใช่ build") ต้องมี `test/self-test.js` เคสใหม่คู่กัน (2) `#14`'s `V1_READ.baseEPS` regex กลายเป็น dead code แล้ว (ตามที่ #14 เขียนไว้เองว่า "ปิดเต็มเมื่อ v1=0") — ลบได้ ไม่ต้องแก้ regex · **งานใหม่ที่ยังไม่เปิด (แค่บันทึกไว้ ยังไม่เริ่ม)**: cleanup ทาง v1 ทั้งโค้ด (`V1_READ` ใน check-reports.js, เส้นทาง v1 ใน update-prices.js ฯลฯ) ยังมีอยู่แม้ report ทุกใบเป็น v2 หมดแล้ว — ควรเปิดเป็นแถวใหม่แยกต่างหากเมื่อจะลงมือจริง ไม่ใช่ทำเงียบๆ · **อัปเดต 23 ก.ย. 69 (gap-close บางส่วน — ปิด SKILL 5B gap โดยไม่ลบโค้ด v1)**: เจ้าของสั่งปิดแค่ "ช่องโหว่จริง" ไม่ทำ full removal (ตัวเลือกที่เลือกจาก 3 ตัวเลือกที่เสนอ — full removal ต้อง re-base `test/self-test.js` ~500+ เคสจาก v1 fixture ไป v2 ถือว่าใหญ่เกินสำหรับรอบนี้) ⇒ `test/check-reports.js buildCtx()`: ไฟล์ที่ไม่ประกาศ `report-data.v===2` เลย (ไม่ใช่แค่ v2 ที่ schema เสีย) ตอนนี้ตั้ง `v2Err` แล้วยก error `V2SCHEMA` เหมือนกัน (เดิมเงียบ ถอยไปอ่านแบบ v1 ไม่มี error เลย) — ปิดช่องที่ worker เขียนทับใบเดิมเป็น v1 ระหว่าง UPDATE แล้ว gate ไม่รู้ตัว (SKILL 5B) · `tools/migrate-v2.js`: กรอง `V2SCHEMA` ออกจาก 2 จุดที่เทียบ/พรีเช็ค v1 กับ gate (`migrateOne`'s `gate0` บรรทัด ~970 · `cronGate()` ที่ใช้ใน `--cron-diff`) เพราะตัวย้ายรับ v1 เป็น input โดยนิยาม ไม่ควรถูกบล็อกด้วยสัญญาณ "นี่คือ v1" — ยังจับ error อื่นของ v1 source ตามปกติ (เช่น E01) · **`update-prices.js` ไม่ต้องแก้เลย** — พิสูจน์แล้วว่ากลไก quarantine เดิม (`gateAfterPatch` → `patch-rejected`, บรรทัด ~1111-1121) ครอบคลุมอยู่แล้วโดยอัตโนมัติ: v1 report ที่ patch ผ่าน path เดิมจะโดน `V2SCHEMA` ใน `gateAfterPatch` เสมอ ⇒ ไม่เขียนไฟล์ + freeze `patch-rejected` โดยไม่ต้องเพิ่ม routing ใหม่ (ยืนยันด้วย `node -e` จริง: `UP.patchReport(v1Fixture,...)` แล้ว `gateAfterPatch` คืน `ok:false, codes:['V2SCHEMA', ...]`) · self-test.js: เพิ่ม `v1Base` (= `expandReport(FX.BBL())`) แยกจาก `base` (ย้ายเป็น `expandReport(FX.BBL_V2())`) — ทุกเคสที่ทดสอบพฤติกรรม "อ่านค่าจาก literal HTML" (E16/E23/E34/E35/E36/W04 · manifest f45/f46/f47/f49 · W17 inference path) ยืนบน `v1Base` ต่อไป (เส้นทางนั้นยังอยู่จริง ไม่ได้ลบ) ส่วนเคสทั่วไปย้ายไป `v1Base`→v2 แล้วพิสูจน์ว่ายังผ่านครบ (550/550, +3 เคสใหม่จาก 547 — 2 เคสยืนยันการปฏิเสธ v1 + 1 เคสพิสูจน์ว่า v2 ที่มี `scnBasis` ประกาศแล้วฟ้องความไม่สอดคล้องตรง ๆ แทนที่จะเงียบแบบ v1 inference path) · `npm run verify` 909/909 error 0 warning 94 (เท่าเดิม) ครบทั้ง 18 ขั้น รวม `test/v2-path-test.js`/`test/migrate-v2-test.js` (436/436) และ `test/update-prices-test.js` (515/515 — 1 จุดต้องสลับ fixture จาก v1→v2 เพราะทดสอบ "ไฟล์ดีต้องผ่าน gate" ซึ่ง v1 ไม่ใช่ "ไฟล์ดี" อีกต่อไปแล้วโดยนิยาม) · เหลือ: **(1)** `V1_READ` โค้ดจริงใน check-reports.js ยังอยู่ (ตั้งใจ ไม่ใช่ลืม) **(2)** docs หลายจุด (`docs/price-refresh.md`/`docs/templates.md`/`CLAUDE.md`/`stock-controller/SKILL.md`) ยังพูดถึง "24 ใบยังเป็น v1" ซึ่ง stale ตั้งแต่ PR #70 (v1=0) แล้ว ไม่ใช่ของใหม่จากรอบนี้ — ยังไม่แก้ (นอกขอบเขตที่เลือกรอบนี้) **(3)** งาน full-removal (ตัวเลือกที่ 2 ที่เจ้าของไม่เลือก) ยังเปิดเป็นตัวเลือกในอนาคตถ้าต้องการลบโค้ดจริง — ต้องผ่านการ re-base self-test ก่อน |
| 37 | **E44 = fix-on-touch ที่บังคับด้วย gate** (ระยะ 2 ส่วน F · Task 17 · ปรับตัวเลขตาม review fix round 1) — ใบเก่าทุกใบจะติด E44 **ครั้งแรกที่ถูก re-analyze** เพราะทุกโหมดต้องขยับ footer เป็นวันนี้ (กติกาใน SKILL 5B/5C · postcheck บังคับให้เฉพาะเส้นทาง queue ไม่ใช่ทั้ง gate) ⇒ worker ต้องแปลง prose ผูกราคาเป็น {{rd:…}} ในรอบนั้น (คลัง 14 ก.ย. 2569: **5,293 จุดใน 907 ใบ ~5.7 จุด/ใบ**) · ★★ **ต้องแยกสองตัวเลขนี้ออกจากกัน**: healer แทนจุดที่ literal ตรงทุก byte ได้ **1,534 จุด/550 ใบ (~30% ของจุด)** แต่ **`--heal-derived --write` เขียนไฟล์ได้จริงแค่ 27/550 ใบ (~5%)** เพราะมันเขียนต่อเมื่อ `gateAfterPatch` ของไฟล์นั้นผ่าน (all-or-nothing รายไฟล์ — safety property ที่ healer ทุกตัวพึ่งอยู่ **ห้ามแก้**) และไฟล์ส่วนใหญ่มีจุดที่ heal ไม่ได้ปนอยู่ ⇒ ลำดับที่ใช้ได้จริง = **dry-run ดูรายการ → แก้มือให้ครบ → `--write` ปิดท้าย** (SKILL 5A/5B เขียนตามนี้แล้ว) · อีก ~26% ของจุดต่างแค่รูปแบบ ($129 vs $129.00 · ~19% vs +19%) และ ~44% เป็นเลขค้างจริงซึ่ง**ต้องให้คนอ่านประโยคแล้วเขียนใหม่** · **ยังไม่มี loose-mode healer โดยตั้งใจ** (เขียนเลขในย่อหน้าใหม่อัตโนมัติ เช่น $297 → $332.27 ทำให้ประโยครอบ ๆ ผิดความหมายเงียบ ๆ — reviewer สุ่มอ่าน 8 จุดยืนยันว่าประโยครอบ ๆ ค้างด้วย) · ถ้าภาระเวฟสูงเกินรับ ทางเลือกคือเลื่อน `RV.PROSE_TOKEN_SINCE` ออกไป (แก้ค่าเดียว) ไม่ใช่ผ่อนเกณฑ์ · หมายเหตุ: ใบ post-SINCE ที่ prose มี literal ค้าง (ไม่ควรเกิด เพราะ gate บล็อกตั้งแต่ push) จะโดน cron กัก `patch-rejected` รายไฟล์ทุกวันจนกว่าคนจะแก้ — พฤติกรรมเดียวกับ PONY/W17 (#34) | Task 17 (`tools/report-values.js` · วัดด้วย `RV.proseBoundHits` ทั้งคลัง · review fix round 1) | ระยะ 3 — ทบทวนหลังเวฟ re-analysis แรกหลัง 14 ก.ย. 2569 |
| 39 | **E44 ชั้น (ข) — เลขที่บังเอิญเท่ากับค่าฐาน** (Task 17 fix round 1 · finding 3 · **ยังไม่มีเคสจริงในคลัง**) — ป้ายที่ชี้ได้หลายค่า (`Fair Value` ของฉาก · `โซน MOS 20%` ของ peer) ถ้าเลขบังเอิญเท่ากับ `report-data.fv`/`mos20`/`mos30` เป๊ะ E44 จะฟ้องและ healer จะแปลงเป็น token ⇒ เลขของฉาก/peer จะวิ่งตามค่าฐานตลอดไป · reviewer ค้นทั้งคลัง: 122 จุดเข้าข่าย **อ่านแล้วเป็นค่าฐานแท้ทุกจุด ⇒ 0 เคสจริง** · **ไม่แก้ด้วยโค้ดรอบนี้โดยตั้งใจ**: สัญญาณที่จะแยก "ของฉาก" ออกจาก "ของฐาน" ต้องอ่านความหมายประโยค — stop-list คำว่า ฉาก/กรณี/peer จะทั้งแคบเกินจนไม่มีผล (0 เคส) และเสี่ยงปิดปาก hit จริงในประโยคที่มีคำเหล่านั้นปนอยู่ · เตือนไว้ใน SKILL 5A แทน ("เขียนเลขให้ต่าง หรือย้ายออกนอก `<p>`/`<li>`") · ทบทวนใหม่ถ้าเจอเคสจริงใบแรก | Task 17 fix round 1 (`tools/report-values.js` `PROSE_BOUND` ชั้น (ข)) | ระยะ 3 |
| 41 | **`V2TOKENS` ไม่ถูกนับในเลข "N/48" ของไฟล์** (Task 18 · residual ที่เปิดเผยไว้) — เป็น pseudo-error นอกตาราง `CHECKS` แบบเดียวกับ `EXPAND`/`V2SCHEMA` ⇒ ไฟล์ที่ติด `V2TOKENS` จะพิมพ์ `errPass` ต่ำกว่าที่ป้าย `N/48` สื่อ 1 หน่วย · เลือกแบบนี้เพราะยกเป็น E-code จริงต้องประกาศ healer ซึ่งไม่มี (ช่องที่กลับเป็น literal ต้องให้คนเขียน token คืน) และจะขยับจำนวน 48/19 ที่ `gen-docs` เป็นเจ้าของ | Task 18 (`test/check-reports.js` · precedent Task 10 `V2SCHEMA`) | เมื่อรีโปตัดสินใจนับ pseudo-error ในตัวหารเดียวกัน (ทั้ง 3 ตัวพร้อมกัน) |
| 44 | **`summary` ("ส่วนต่างจากราคา") ยังอยู่นอก `V2TOKENS`** (ระยะ 3 Task 12) — เป็นช่องเดียวที่เหลือของ `REQUIRED_SITES` (migrator, 14 ช่อง) ที่ยังไม่เข้า `RV.REQUIRED_TOKEN_SITES` (13 ช่อง) · **วัดคลังแล้ว: ผ่าน 883/883 ใบ v2 + skeleton th/us + fixtures v2 ทั้ง 7 ใบ ⇒ "เพิ่มได้ทันทีโดยไม่มีใบตก"** — ที่ไม่เพิ่มไม่ใช่เพราะคลังไม่พร้อม แต่เพราะ**ผิดเกณฑ์คัดเข้ารายการ**: ช่องนี้**มีตัวเขียน** (`summaryPlan` = `patchDerived` #11 · ระยะ 1 ข้อ D) ⇒ ไม่เข้าเงื่อนไข "ไม่มีตัวซ่อมไหนเอื้อมถึง ⇒ ค้างถาวร" ที่รายการนี้ยืนอยู่ · และการยกเป็น error จะเปลี่ยนสภาพที่ cron ซ่อมเองได้ (literal ที่ค่าถูกต้อง) ให้กลายเป็น **`patch-rejected` กักไฟล์รายวัน** จนกว่าคนจะมาเขียน token คืน — แลกไม่คุ้ม · ★ ทบทวนใหม่เมื่อ: (ก) รีโปตัดสินใจว่า "สำเนาต่อค่า = 1" ต้องบังคับแม้ช่องที่มี healer หรือ (ข) `summaryPlan` ถูกถอด/เปลี่ยนเจ้าของ · **ตรึงคำตัดสินไว้เป็นเทสแล้ว** (`test/self-test.js` — assert ว่า `summary` ไม่อยู่ในรายการ + รายการต้องเป็นสับเซตของ `MG.REQUIRED_SITES`) | ระยะ 3 Task 12 (`tools/report-values.js` · `test/self-test.js`) | รอบที่ตัดสินข้อ (ก)/(ข) ข้างบน |
| 48 | **hint header หมวด 6 ขัดกับตัวเลขที่โชว์จริง — คนละคลาสจาก #47 (ที่ปิดแล้ว)** (พบระหว่างสแกนปิด #47, 23 ก.ย. 69 — Task 2 ของแผน `open-items-46-47`): สแกน `.ret`/hint ทั้ง 909 ใบด้วย regex หยาบ พบ 67 ใบต้องสงสัย — หลังตรวจเลขคณิตจริงทุกใบ (`(tgt−px)/px` capital-only เทียบ `(tgt+div−px)/px` dividend-inclusive ที่ current px + back-solve implied entry price ต่อคอลัมน์เพื่อแยก "สูตรผิด" จาก "entry ค้าง"): **ส่วนใหญ่ไม่ใช่บั๊ก — บัญชี 67 ใบครบทุกใบ ไม่มีใบตกหล่น**: 9 ใบเป็น dual-labeled cell ที่โชว์ทั้งสองตัวเลขอยู่แล้ว (HLT/T/SFLEX/SISB/TEAMG/APO/MRK/NKE/TRI — self-documenting, parser หยาบจับเบอร์แรกผิด) · 7 ใบ (CB/EQT/FRT/STE/TKC/UNP/VGI) ใช้ methodology "ปันผลฝังใน tgt" (`tgt = metric×multiple + ปันผลสะสม` — ยืนยันด้วยสูตรใน prose ของแต่ละใบเอง ตรงเลขเป๊ะ) ทำให้ `(tgt−px)/px` ตรงกับตัวเลขพอดีแม้ hint เขียน "รวมปันผล" ถูกอยู่แล้ว (ปันผลรวมอยู่ใน tgt ไม่ได้บวกแยก) · 1 ใบ (`JKHY.html`) มีปันผลจริง (`dps=2.44`, `dividendYield≈1.61%`) แต่ตารางฉากหมวด 6 ไม่มีแถวปันผลให้บวก — capital-only ตรงกับตัวเลขที่โชว์เป๊ะ (−14.2%/+39.2%/+87.4% เทียบ −14%/+39%/+87% ที่แสดง) จึงยืนยันว่า "ไม่รวมปันผล" ถูกแล้ว ไม่ใช่เพราะหุ้นไม่มีปันผล · **28 ใบ** entry price ที่ implied จากตัวเลขตรงทิศทางกับที่ hint อ้าง (ไม่ใช่ "~15" อย่างที่เคยเขียนคร่าว ๆ — นับจริงแล้วคือ 28: 21 ใบห่างจาก px ปัจจุบัน ≤3% ปกติของ scenario ที่เขียนไว้ไม่กี่วันก่อน, 3 ใบ (`NDSN.html`/`PTTEP.html`/`SCB.html`) ตรงกับ px ปัจจุบันพอดีแทบไม่มี gap, 4 ใบมี gap ใหญ่กว่า 3% — ดูรายการแยกด้านล่าง) · **19 ใบเหลือที่สูตรใดก็ไม่ตรงเนียน ๆ (ไม่เข้า 4 กลุ่มข้างบน) — ตรวจเลขคณิตมือจริงแค่ 3/19 ใบ** (`ADVICE.html`/`AAI.html`/`PRINC.html` — สุ่มยืนยันว่าไม่ใช่บั๊ก เป็นข้อจำกัดของ parser หยาบที่ไม่รู้จักรูปแบบ per-year-only เช่น `ADVICE.html` โชว์แค่ "%/ปี" เฉย ๆ ไม่มี % รวม 3 ปีให้จับ — ถอด CAGR ของ capital-only กลับมาตรงกับตัวเลขที่โชว์เป๊ะและตรงกับ hint "ไม่รวมปันผล") **อีก ~16 ใบที่เหลือยังไม่ได้ตรวจมือทีละใบ — ไม่ได้ยืนยันว่าสะอาด เป็นแค่ประเมินความเสี่ยงต่ำตามรูปแบบที่พบในใบอื่น** (บันทึกช่องว่างนี้ไว้ตรง ๆ แทนที่จะกลบเงียบ) · **แก้แล้ว 1 ใบ**: `COR.html` — hint + prose (3 จุด: hint หมวด 6 · ย่อหน้าอธิบาย · กล่องกลยุทธ์หมวด 8) เขียน "ไม่รวมปันผล" ทั้งที่ 3/3 คอลัมน์ตรงกับ dividend-inclusive แม่นกว่า capital-only ที่ current px $316.49 อย่างชัดเจน (bear ห่าง 0.32pp เทียบ 2.05pp · base 0.34pp เทียบ 2.71pp · bull 0.11pp เทียบ 2.42pp — เฉลี่ยแม่นกว่า ~9 เท่า) และ `tgt` เองพิสูจน์แล้วว่าไม่ฝังปันผล (`EPS×P/E` ตรงเป๊ะทั้ง 3 คอลัมน์) จึงต้องบวกปันผลแยกจริงตามที่ตัวเลขแสดง — แก้ป้ายให้ตรงเลขคณิต เหมือนแบบ #42 (ไม่แตะ tgt/div/`.ret`) · commit `2723b84a1` ("fix: correct COR.html dividend-basis label in scenario table") · **อัปเดต 23 ก.ย. 69 (หลัง advisor consult) — ทั้ง DVN.html และ POOL.html แก้แล้ว ไม่ใช่ escalate ค้างอีกต่อไป**: `DVN.html` — footer "ข้อมูล ณ 21 ก.ย. 2569" (2 วัน อยู่ในแคมเปญ 908 ใบ ไม่ใช่ price-drift เก่า) ⇒ `scenarioPlan(DVN)` คืน null จริง ("สามคอลัมน์ไม่สอดคล้องกันเอง") ไม่ใช่แค่ "ตัดสินไม่ได้" — เป็นความไม่สอดคล้องสดที่ cron ซ่อมเองไม่ได้ `tgt` พิสูจน์แล้วว่าเป็น EPS×P/E ล้วน (ไม่ฝังปันผล) ตรงกับ hint "รวมปันผล fixed" ⇒ คำนวณ `.ret` ใหม่บนฐาน dividend-inclusive ที่ px ปัจจุบัน (−43%→−39% · +5%→+7% · +48%→+51%) โดยไม่แตะ `tgt`/`div` — ยืนยันแล้วว่า `scenarioPlan(DVN)` คืนค่าไม่ null หลังแก้ (`conv:'div'`, entry $47.46 ห่าง px 0.28%) ⇒ cron เป็นเจ้าของตารางฉากใบนี้ต่อจากนี้ commit `40bf271b1` · `POOL.html` — โครงสร้าง `tgt` เผยว่า base/bull ฝังปันผลสะสมไว้ใน `tgt` เอง (base 13.69×24≈328.56+~15.44≈344 · bull 15.27×28≈427.56+~16.44≈444) แต่ bear ไม่ฝัง (179≈9.92×18=178.56 ไม่บวกปันผล ~$15 เลย) — ความไม่สอดคล้องตอนสร้างราคาเป้าเอง ไม่ใช่ entry ค้าง ⇒ แก้ด้วยป้ายกำกับต่อคอลัมน์เหมือนแบบ #42 (ไม่แตะ `tgt`): เติม "(ไม่รวมปันผล)" ที่ `.ret` ของ Bear เท่านั้น commit `de935e309` · `scenarioPlan(POOL)` ยังคืน null หลังแก้ (ฐานผสมข้ามคอลัมน์เป็นโครงสร้าง ไม่ใช่ป้ายผิด) — **เหลือเป็นงานของรอบ re-analyze ถัดไป: ให้ analyst ตัดสินว่าจะขยับ `tgt` ของ Bear ให้ฝังปันผลเหมือน base/bull หรือไม่** (ไม่ใช่ของค้างประเภท "แก้ป้ายแล้วจบ" อีกต่อไป — บันทึกไว้ให้คนที่ re-analyze POOL เห็น) · **หลักฐาน stale scenario-entry ที่เด่น** (นอกขอบเขต #47/#48 นี้ เป็นคลาส `price-derived-staleness` ที่มีอยู่แล้ว — บันทึกไว้เพราะพบระหว่างสแกนนี้): `VMC.html` implied entry ~$312 เทียบ px ปัจจุบัน $246.2 (ห่าง **26.8%**) · `SCC.html` ~9.8% · `BTS.html` ~9.4% · `RKLB.html` ~7.7% · `DVN.html` ~5.7% · **W17 (`tools/derived-values.js` `scenarioPlan`) ยืนยันอิสระว่า `.ret`-cell เองไม่พัง (0/909 error ทั้งคลัง) แต่ W17 เองมี blind spot ที่ยอมรับแล้วในโค้ด (~126 ใบ "ตัดสินไม่ได้" → เงียบ ไม่ error ไม่ใช่ "ผ่านแล้วถูก") — COR เป็นหนึ่งใน 126 ใบนั้น (`scenarioPlan` คืน `null`, เหตุผล "ตัดสินสมมติฐานปันผลไม่ได้") เพราะ (ก) spread-vote ของ integer-rounded display ชนกัน (spP=0.84% vs spD=0.55% — ไม่ต่างพอจะฟันธง ตรงกับที่โค้ดเตือนไว้เองเรื่องเลขจำนวนเต็มปัดจนแยกไม่ออก) (ข) hint-vote ใช้ไม่ได้เพราะ hint มี token `{{rd:px}}` ไม่ใช่ตัวเลขดิบ — ยืนยันด้วยมือแทนโดยใช้ px จริง: dividend-inclusive implied entry เฉลี่ย $316.65 ห่าง px แค่ 0.05% (ตรงเกณฑ์ `near≤1% && far≥max(1.5%,near×3)` ของโค้ดเอง) ⇒ ตัดสินได้จริงว่าเป็น `div` ไม่ใช่ `plain`** · **checker gap ที่ยืนยันแล้ว**: ไม่มี W-code เทียบคำ "รวม/ไม่รวมปันผล" ในข้อความ hint กับ `scnBasis.divIncluded` (v2) หรือกับผลเลขคณิตจริง (v1) — v2 ตรวจแล้วว่า 745 ใบมี `scnBasis`, 108 ใบมี hint แบบ literal (ไม่ใช่ token) แต่ **0/108 ขัดกับ `divIncluded`** (v2 สะอาด เพราะ scnNote token บังคับอยู่แล้วในเคสส่วนใหญ่) — ช่องว่างเป็นของ v1/legacy-hint เท่านั้น | scan ระหว่างปิด #47 (`.superpowers/sdd/2026-09-23-open-items-46-47/task-2-report.md`) | **อัปเดต 23 ก.ย. 69 (advisor consult) — ปิด 2/3 ข้อ**: (1) DVN/POOL แก้แล้ว ดูรายละเอียดในคอลัมน์ก่อนหน้า (2) **ไม่เปิด W-code ใหม่รอบนี้ — ruling: fix-on-touch** (v2 745 ใบมี `scnBasis` สะอาด 0/108 อยู่แล้ว ช่องโหว่เป็นของ v1/legacy เท่านั้น ซึ่งจะแคบลงเองเมื่องาน "24 ใบ v1 residue" (open-items #14/#36) migrate ครบ — ทบทวนใหม่หลังงานนั้นจบ) (3) **VMC/SCC/BTS/RKLB ไม่ใช่ผู้สมัคร price-flags** — cron patch ราคาทีละก้าวเล็กทุกวัน ไม่มีวันกระโดดเกิน 15% ในครั้งเดียว และ UPDATE-LIGHT ไม่แตะตารางฉากอยู่แล้ว ⇒ ไม่มีกลไกไหนจะ flag ใบพวกนี้ได้ตามปกติ ทั้ง 4 ใบเป็นตัวอย่างของ **W17-silent class** (`scenarioPlan` คืน `null` แบบเงียบไม่ error — โค้ดเองประเมินไว้ ~126 ใบทั้งคลัง ไม่ใช่แค่ 4 ใบนี้) — **ไม่ special-case แค่ 4 ใบ** ต้องมองเป็นส่วนหนึ่งของ backlog ที่ใหญ่กว่า (ดู #1 ในตารางนี้ — คลาสเดียวกัน) รอ fix-on-touch ตอน re-analyze แต่ละใบตามปกติ |
| 53 | **v3 Plan 2 must-do — ทบทวนเกณฑ์ corpus 97% ที่ P6/P7** — `test/v3/tokens-corpus.test.js` ผ่านเมื่อ `compute()` รับ v2 value set ≥97% (ไม่ใช่ 100%) เพื่อไม่ให้ v2 ใบใหม่แปลก ๆ ใบเดียวบล็อกการ push ของ v2 · วัดหลัง rebase บน `aa9f5a414` (v2 = 909/909): รับ **909/909** · ปฏิเสธ 0 · เทียบ token 19,275 จุด mismatch 0 ⇒ ตอน migration (P6/P7) ตัดสินว่าจะยกเป็น 100% หรือคงไว้ | Report v3 Plan 1 (24 ก.ย. 69 · `docs/superpowers/plans/2026-09-24-report-v3-plan1-core-render.md` · `docs/decisions.md` §10 "Report v3") | v3 Plan 2 · P6/P7 |
| 54 | **v3 Plan 2 must-do — การ์ดปันผลพิมพ์มือ 1 ตำแหน่ง 35 ใบ ทำเป็น 2 ตำแหน่งตอน migrate v3 เท่านั้น** — `TOKENS.yield` เป็น 2 ตำแหน่งแล้ว (`docs/decisions.md` §10 "Report v3") แต่ 35 ใบที่พิมพ์ literal 1 ตำแหน่งไม่แตะใน v2 (ห้ามแก้มือ/ห้าม sweep รายงาน) ⇒ มาตรฐานเดียวเกิดเองตอน migrate เพราะ v3 render การ์ดจาก formatter กลาง · ตรวจตอน migration ว่าทั้ง 35 ใบออกมา 2 ตำแหน่ง | Report v3 Plan 1 (24 ก.ย. 69 · `docs/superpowers/plans/2026-09-24-report-v3-plan1-core-render.md` · `docs/decisions.md` §10 "Report v3") | v3 migration (P6/P7) |
| 55 | **v3 Plan 3 (migrate) — v2 เก็บ forward EPS ไว้ในช่อง `eps` ~19 ใบ** — FER: `stock-meta` P/E 57.4 มาจาก EPS fwd 0.98 · v3 แยก `eps` (TTM 0.97) กับ `epsForward` ⇒ P/E บน index ขยับเป็น 58.04 ตอน migrate · migrate-v3 ต้องจำแนกฐาน EPS จาก `.mdesc`/การ์ด (fwd vs TTM) แล้วใส่ช่องให้ถูก · ใบที่ P/E index ขยับ → ถัง VALUE-DRIFT (ไม่ใช่ CLEAN) | Task 0 FER-real (`docs/superpowers/specs/2026-09-24-FER-v3-compare.md`) 24 ก.ย. 69 | v3 Plan 3 (P6) |
| 56 | **v3 Plan 3 (migrate) — prose เก่าลอกตัวเลขผูกราคาที่ค้างแล้ว** — EQIX: prose บอกผลตอบแทนฉาก ~+38%/~+4%/~+57% แต่ช่องที่ compute ในหน้าเดียวกันเป็น +33%/+0.2%/+51% · P/AFFO 26.6x · P/E 65.7x ไม่ตรงราคาปัจจุบัน ⇒ ยืนยันความจำเป็นของ W31 + ถัง VALUE-DRIFT · migrate-v3 ต้องแทน literal ที่ตรงค่า view ด้วย token และนับที่เหลือ (spec §10.1) | Task 0 EQIX-real (`docs/superpowers/specs/2026-09-24-EQIX-v3-compare.md`) 24 ก.ย. 69 | v3 Plan 3 (P6) |
| 57 | **v3 Plan 3 — รายการ literal ค้างในใบจริงที่ต้องแทนด้วย token ตอน migrate** — ZTS-real W31=34 (ราคาสด เช่น "$74.83") · BBL-real `notes.epsFy` "TTM ~฿22.0" (ไม่มี token `{{eps}}`) · FER-real mcap note "≈ €34.2B ที่ EURUSD 1.15566" (fx เป็นช่อง structured แล้ว → token ได้) · FER gauge prose "(~44%)" ส่วนต่าง FV กับเป้านักวิเคราะห์ (ไม่มี token — เสนอ `analyst.vsFv`) ⇒ migrate-v3 ต้องมี token ชุดนี้หรือส่งเข้าถัง VALUE-DRIFT | Plan 2a Task 5/7/8/10/12 reviews 24 ก.ย. 69 | v3 Plan 3 (P6) |
| 58 | **v3 — ชั้น 0 ที่ยังไม่อยู่ใน check-v3**: "rf ต้องตรงสกุลกระแสเงินสด" และ "ขาที่ใช้ (r,g) ชุดเดียวกันโดยไม่ประกาศ family = วิธีเดียว" (W32 ใน Plan 2a ครอบแค่ |MOS|>40%) ⇒ เพิ่ม W-code ใน Plan 2b หรือ 3 พร้อม self-test | Plan 2a Task 12 review 24 ก.ย. 69 | v3 Plan 2b/3 |
| 59 | **v3 — คำถามระดับเจ้าของ/advisor: หน่วยเงินบาทใน `RV.fmtBig`** พิมพ์ "หมื่นล้าน/แสนล้าน" บนการ์ด (13/11 หน้าใน dist) ขณะที่ prose ทั้งคลังใช้ "ล้าน" 1,590× / "พันล้าน" 236× ⇒ หน้าเดียวกันผสม "฿4.60 หมื่นล้าน" กับ "฿9,003 ล้าน" · เป็น formatter ทั้งเว็บ (แตะ mcap ทุกหน้า TH) ไม่ใช่บั๊ก v3 → advisor ตัดสินตอน Plan 3 | Plan 2a Task 8 review 24 ก.ย. 69 | v3 Plan 3 |
| 60 | **v3 Plan 3 migration guidance (สี/tone)**: `tone:'neu'` = คลาส `.neu` เดิม (เหลือง) ไม่ใช่สีเรียบ · การ์ด `capital` (CET1/CAR) ตั้งค่า `pos` ตายตัว — ธนาคารทุนอ่อนต้องใส่ `tone:'neg'` เอง · การ์ด revenue ค่าตั้งต้น `neu` (FER เปลี่ยนสีจาก v2) ⇒ migrate-v3 ต้อง map คลาส v2 → tone ต่อการ์ด | Plan 2a Task 4/8/10 reviews 24 ก.ย. 69 | v3 Plan 3 (P6) |
| 62 | **v3 — ราคาใบ v3 แช่แข็งจน Plan 3 (P5) · เส้นตายแข็ง** — cron `update-prices.js` อ่านแต่ `.html` ⇒ ใบ v3 ที่ publish ใน Plan 2c ไม่ได้ราคาใหม่รายวัน · W09 ที่ 45 วัน · **E27 ที่ 120 วันทำให้ `npm run verify` ล้มทั้ง repo** ⇒ (1) Plan 2b: `update-prices` ต้อง exit ≠0 บน symbol v3 ("v3 cron = Plan 3") + `reportExists` รวม `.json` — ห้ามเงียบ (วันนี้ `--write --force <v3>` no-op exit 0) · (2) **เส้นตาย P5: แข็ง = market.priceDate ของใบ exit + 120 วัน (E27) · เป้า = +45 วัน (W09) — วัดจาก check-v3 24 ก.ย. 69** · ห้ามรวม P5 เข้า 2b/2c · **วันจริง (2c-ii):** `market.priceDate` OGE = 2026-09-23 · ICC = 2026-09-23 ⇒ **เส้นตายแข็ง 2027-01-21 (E27) · เป้า 2026-11-07 (W09)** | Plan 2b Task 0 + advisor 24 ก.ย. 69 | v3 Plan 3 (P5) — **สาย v3 ของ cron ลงใน Plan 3** (branch `feat/report-v3-plan3`) · ปิดเมื่อ cron ตามตาราง 3 รอบสะอาดหลัง merge (ไม่มี `patch-rejected` บนใบ v3 · `updated` ของ OGE/ICC ไม่ขยับ · W09 หาย — spec §11 P5 (f)) |
| 63 | **ชื่อไฟล์รายงานใช้ namespace เดียวข้ามตลาด** — TH `PM` ชนกับ US `PM` (Philip Morris) ⇒ หุ้นไทยที่ ticker ซ้ำกับหุ้น US ทำรายงานไม่ได้ · ต่ำ priority — ตัดสินตอน cutover (P7) หรือเมื่อเจอเคสจริง | Plan 2b Task 0 24 ก.ย. 69 | — |
| 64 | **v3 NEW ทำไม่ได้กับหุ้นไทยที่ StockAnalysis ไม่ครอบคลุม** — sidecar (`tools/queue/sidecar.js`) fail-closed เมื่อ `fetch-fundamentals` ให้ `errors.fin`/`errors.stats` ⇒ `report.js init` ปฏิเสธ · เคสจริง **M-CHAI** 24 ก.ย. 69 (`quote/bkk/M-CHAI` redirect symbol-lookup · search 404) ทั้งที่ Yahoo/set.or.th มีข้อมูล ⇒ ต้องมีแหล่งงบสำรองในเส้น sidecar (งบ SET/IR ตาม `docs/data-source-fallbacks.md` §3 หรือ `--fundamentals-from <json>` ที่ controller กรอกจากงบต้นฉบับ) ก่อน migrate ใบ TH ที่ SA ไม่มี (P6/P7) · ตรวจจำนวนใบ TH ที่ SA ไม่ครอบคลุมก่อนวางแผน cutover | Plan 2c-ii Task 2 24 ก.ย. 69 | v3 Plan 3+ (ก่อน P6) |
| 65 | **v3 gate: E17 (≥2 ขา FV) + hint "ถ่วงน้ำหนัก N วิธี" นับขาที่ `fvWeights` = 0 เป็นขา FV** — ขาที่ไม่นับใน FV ต้องเป็น `role: "context"` (สเปก §3.6 แถว I) แต่ `check-v3` และ render ยังนับขาน้ำหนัก 0 ⇒ ใบที่มีขา FV จริงขาเดียวผ่าน gate เงียบ ๆ (เคส ICC ร่างแรก 24 ก.ย. 69 จับได้โดย final review ไม่ใช่ gate) · แก้: E17 นับเฉพาะขา role fv ที่น้ำหนัก >0 · hint นับเท่ากัน · เพิ่ม W ใหม่เมื่อ `fvWeights` มี 0 ("ใช้ role context แทน") + self-test · **ข้อขัดกัน 0.4c ↔ E17 (advisor 24 ก.ย. 69)**: เมื่อขาตระกูลที่สองที่ซื่อสัตย์ห่างขาตลาด >2× (holding ที่ ROE < r เช่น ICC — g ที่สอดคล้องกับ retention ให้ ≈ ฿10.4 = DDM ที่ g เดียวกัน) 0.4c ผลักไป context แล้ว E17 ปฏิเสธ ⇒ ICC ต้องใช้ g = 0 (เพดานของตระกูล rg) เป็นขา rg เดียวที่ save ได้ พร้อมเปิดเผยในใบ · **เจ้าของต้องตัดสินกติกาทั่วไป** | Plan 2c-ii final review 24 ก.ย. 69 | v3 Plan 3 (gate) |

## ปิดแล้ว (ระยะ 0)

| # | รายการ | ปิดโดย |
|---|---|---|
| 3 | controller ต้อง diff เป้า/52wk/ปันผล/P-BV กับ prep ก่อน dispatch — อยู่แต่ใน memory | `npm run queue -- prep` (Task 12) |
| 12 | MEMORY.md บอกว่าเคส MC ใน quality-gate.md ผิด ทั้งที่แก้แล้ว | Task 19 Step 2 (memory) |
| 18 | เส้นทาง `analyze-wave` (Workflow) inject CLAUDE.md ไหม · default model ทางนี้ | Task 20 probe 12 ก.ย. 2569 (เจ้าของสั่ง · args ไม่ส่ง `model` · effort low): (1) "You are powered by the model named Sonnet 5" — ค่านี้มาจาก script pin `waveModel = args.model \|\| 'sonnet'` (`analyze-wave.js:20`) ไม่ใช่ default ของ harness ⇒ เส้นทางนี้**แน่นอน**ต่างจาก Agent tool (2) **ใช่** inject CLAUDE.md — worker เห็น "## 1. โครงสร้างโฟลเดอร์" (3) **ใช่** มี MEMORY.md index ~30 บรรทัด ⇒ role-scope `[controller]` ใน CLAUDE.md ใช้กับ worker ทั้งสองเส้นทาง |
| 21 | fixture ของ self-test/update-prices-test ผูกกับ BBL/AAPL จริง | Task 1–2 (PR #A) |

## ปิดแล้ว (ระยะ 1)

| # | รายการ | ปิดโดย |
|---|---|---|
| 2 | ป้าย marker gauge "เหมาะสม" ไม่มี gate (ICLR) | Task 9 (PR #32): field `f49` (ป้าย gauge mFair) ได้ binding `pair` → f44 (money) ⇒ ยิงเป็น **W22** เมื่อไม่ตรง |
| 4 | median-multiples ส่งค่าผิด 2 คลาส (สุดขั้ว · ผสมสกุล) — sanity ใน runbook แล้ว แต่ยังไม่มี fixture test ของตัวมันเอง | Task 23: `oneSymbol(spec, th, deps)` ฉีด `fetchFinPage`/`monthlyCloses`/`statementCurrency`/`finRow` ผ่าน `deps` (default = ของจริง) + `test/median-multiples-test.js` (offline, เรียกจาก `test/prep-stock-test.js`) ครอบ TTM/EPS≤0/outlier สุดขั้ว (เคส AU 2,074x)/ผสมสกุล (เคส CP)/MIN_POINTS |
| 7 | E41 ข้ามป้าย "P/E เฉลี่ย ~N ปี" — ป้ายอ้างช่วงยาวกว่าอายุหุ้น (GABLE) | Task 24: `parseVendor` นับ `fyYears` จากคอลัมน์ตัวเลขจริงบนแถว `EPS(dil)` ของตาราง [3] (หัก TTM เมื่อหัวตารางมี) → `prep()` บันทึกลง state + เติมบรรทัดเตือนใน prompt worker · `postcheck.checkFyYears(f55, fyYears)` ฟ้องเมื่อป้ายการ์ดเกินจริง — ★ นี่คือทางปิด #7 จริง แยกจาก manifest pair `f55↔f04` (`how:'years'`) ซึ่ง**ยังไม่ยิงใบไหนเลย (latent)** เพราะกราฟราคาไม่มีป้ายปี 4 หลักให้เทียบ — ขา manifest นั้นปล่อยไว้เป็นข้อมูลเสริมเฉย ๆ ไม่ใช่ตัวตัดสิน |
| 8 | `test:prep` ไม่อยู่ใน verify | Task 23: แทรก `node test/prep-stock-test.js` เป็นขั้นที่ 16 ของ `npm run verify` (หลัง `docs-test`) + `STEP_LABELS`/`.githooks/pre-push`/docs ผ่าน `tools/gen-docs.js` |
| 9 | bad-chart ผสมฐาน gate มองไม่เห็น (E36 ผิดพร้อมกัน) | Task 9 (PR #32): field `f41` (กรอบ 52 สัปดาห์) ได้ binding `pair` → f01 (contains · stale) ⇒ ยิงเป็น **W23** |
| 10 | ห้ามวาง shares ถัวเฉลี่ยลง prompt (CAMT) | Task 24: โค้ด (`SHARES_LABEL`/`SHARES_NOTE` ใน `printFinancialTable`) มีอยู่แล้วตั้งแต่ 17 ส.ค. 69 แต่ไม่มีเทสคุม — เพิ่ม `test/prep-stock-test.js` ยืนยัน `F.SHARES_LABEL === 'Shares(wAvgDil)'` และจับ stdout ของ `printFinancialTable` (export เพิ่มจาก `tools/fetch-fundamentals.js`) ว่าพิมพ์ `SHARES_NOTE` จริงเมื่อตาราง [3] มีแถว shares (fixture `amataFin`) และไม่พิมพ์เมื่อไม่มี |
| 13 | BBL ช่องสรุป "+2.1% (เกือบเต็มมูลค่า)" เครื่องหมาย/คำขัดกัน | ปิดเชิงกลไก (Task 11) · คลังกวาดแล้ว (Task 12 · 861 ใบ · W06 = 0) |
| 19 | W06 ยิง 553/908 ก่อน 17 ส.ค. — เดิมต้องให้คนแก้เพราะ cron ไม่แตะคำ | ปิดเชิงกลไก (Task 11) · คลังกวาดแล้ว (Task 12 · 861 ใบ · W06 = 0) |
| 22 | คิว price-flags เป็น snapshot — pre-patch ด้วย --force ล้าง flag ก่อนวิเคราะห์ ทำให้สถานะรอบอยู่แค่ใน .queue/state.json (per-machine) — ต้องการคิวถาวรใน repo | Task 15: state ย้ายไป `<checkout หลัก>/.queue` (`resolveQueueDir` ใน `tools/queue/state.js` ใช้ `git rev-parse --git-common-dir`) — ข้าม worktree ✓ ข้ามเครื่อง ✗ (ถ้าใช้หลายเครื่องต้องย้าย state เข้า repo) |
| 23 | lockfile ไม่มี heartbeat — holder ที่ทำงานเกิน 10 นาทีจะถูกยึด lock และตอนปล่อยจะลบ lock ของคนใหม่ (ยังไม่เกิดเพราะไม่มี holder ทำงานยาวใต้ lock) | Task 4 (PR #31): `tools/lockfile.js` — holder async ได้ heartbeat จน settle แล้วค่อยปล่อย lock (holder sync ยังห้ามยาวตามเดิม) |

## ปิดแล้ว (ระยะ 3)

| # | รายการ | ปิดโดย |
|---|---|---|
| 40 | **cron รอบแรกบนคลัง v2 ยังไม่ได้ออกแรง write path** | **ระยะ 3 Task 13a**: run [`34930505064`](https://github.com/iam1412/stock-analysis/actions/runs/34930505064) (15 ก.ย. 2569 04:52–05:00 UTC = 11:52–12:00 น. ไทย, commit `135eeab3`) ราคาขยับจริงบนคลัง v2 883/908 ใบ — เขียนแล้ว 658 · ไม่เปลี่ยน 4 · ข้ามตลาดเปิด 234 · freeze 12 · **`patch-rejected`/`patch-failed` = 0 ทั้งคู่** · gate หลัง patch 908/908 error 0 · สุ่ม `git show` 5 ใบ (AMKR AAPL WMT BNY WWD) ยืนยันแตะแค่ `report-data.values`/`stock-meta`/การ์ด derived ไม่แตะ prose/EPS/FV เลย ⇒ write path ทำงานถูกต้องจริงบนราคาที่ขยับจริง ไม่ใช่แค่ offline `--cron-diff` อีกต่อไป — รายละเอียดเต็ม `docs/superpowers/audit/2026-09-11-stock-analyzer/phase3-exit.md` §1 |
| 34 | รอบ cron แรกที่ถือโค้ดครบทุกส่วน A–E (PONY เคยเข้า quarantine ด้วย W17) | **ระยะ 3 Task 13a**: run เดียวกับ #40 — **PONY patch สำเร็จ (`✓ PONY 6.63→6.71`)** ไม่ติด quarantine/`patch-rejected` อีกต่อไป (แก้ไปแล้วก่อนหน้านี้) · `patch-rejected` ทั้งรอบ = 0 ⇒ ไม่มีใบไหนติด REJECTED bucket ให้ตรวจ |
| 33 | cron schedule เดิม `17 0 * * *` UTC รันจริงกลางช่วง SET เปิดตลาด — guard `isIntradayQuote` ข้ามหุ้นไทย 230+ ครั้ง/วัน | **ระยะ 3 Task 7 (`905267c4`, merge PR #45) + Task 13b**: เปลี่ยน config เป็น `0 21 * * *` UTC — run แรกที่เวลาใหม่ [`35035634931`](https://github.com/iam1412/stock-analysis/actions/runs/35035634931) (created `23:26:03Z` = 06:26 น. ไทย 16 ก.ย., commit `9e226dc3`) skew วัดจริง **2 ชม. 26 นาที** (ในกรอบ safe ceiling 5.78 ชม.) · **"ข้าม — ตลาดยังเปิด" = 0 ครั้งทั้งรอบ** (เทียบ 230+/วันก่อนแก้) · patch-rejected/failed = 0 · gate 908/908 error 0 · หมายเหตุ: หุ้นไทย 0 ใบอยู่ใน "อัปเดต 657" ของรอบนี้ เพราะ workflow_dispatch มือของผู้ดูแลเมื่อวัน 15 ก.ย. 18:20 น. (ตลาดปิดแล้วเช่นกัน) ได้ patch ราคาปิดวันเดียวกันไปก่อนแล้ว (พิสูจน์ด้วย hash PTT เท่ากันเป๊ะข้ามสอง commit) — ไม่ใช่การถูกข้าม แค่ไม่มีราคาใหม่ให้เขียนตอนนั้น · **แก่นของปัญหา (จำนวนครั้งที่ guard ยิง) วัดได้ตรง = 0 ⇒ ปิดได้** รายละเอียดเต็ม `docs/superpowers/audit/2026-09-11-stock-analyzer/phase3-exit.md` §2 |
| 31 | เส้นทาง `--th` ของ `/forecast/` ยังไม่เคยทดสอบกับของจริง | **ระยะ 3 Task 11 (`b89927b2`)**: probe จริง `node tools/fetch-fundamentals.js <SYM> --th` กับหุ้นไทย 3 ตัว (AOT/PTT/KTC) — เส้นทาง `quote/bkk/<SYM>/forecast/__data.json` ทำงานปกติทุกตัว exit 0 · บรรทัด `[2c]` แมตช์ FY ได้ถูกต้อง · **AOT ยิง ⚠ จริง (true positive ของกับดัก [2c] ไม่ใช่ transient ตามที่รายงานแรกเข้าใจผิด)** — `currentFY` (ตาราง [3]) กับ `fc.lastClosedFY` ตรงกันที่ FY2025 (ไม่มี lag) แต่ Yahoo `forwardEps` 2.07 อ้างอิง FY2027e (FY+2, ยืนยันด้วย Yahoo fwdPE 29.4 = 61÷2.07) ⇒ กับดักทำงานถูกต้องตามที่ออกแบบไว้ (จับ forward EPS ที่ไม่ใช่ปีถัดไปจริง) ⇒ ไม่พบบั๊ก ไม่ต้องแก้โค้ด |
| 35 | `forecastLine` มองว่า `epsFwd === 0` = ไม่มีค่า (`tools/fetch-fundamentals.js`) — ถ้า forward estimate จริงเป็น 0 จะพิมพ์ผิดว่า "ไม่มี epsFwd" | **ระยะ 3 Task 11 (`b89927b2`)**: เปลี่ยนเงื่อนไขจาก `!Number.isFinite(v) \|\| v === 0` เป็น `v == null` — `asNum()` คืน `null` อยู่แล้วเฉพาะตอน parse ไม่ได้/vendor ไม่ส่งค่ามา (ต่างจาก 0 ที่เป็นตัวเลขจริงและ `Number.isFinite` ผ่านอยู่แล้ว) ⇒ sentinel ถูกต้องโดยไม่ต้องเพิ่ม field ใหม่ · เทส TDD ใน `test/prep-stock-test.js` (แดงก่อนแก้ ยืนยันด้วยรันจริง) ครอบทั้ง 2 กรณี: `epsFwd` undefined/ไม่มีคีย์ → "ไม่มี epsFwd" (ไม่เปลี่ยน) · `epsFwd: 0` จริง → เทียบ FY ตามปกติ ("ไม่ตรง FY ไหน" เพราะ 0 ห่างทุกปีเกิน tolerance ไม่ใช่ข้อความ "ไม่มีค่า") |
| 38 | **footer "ข้อมูล ณ" ที่ `FOOTER_RE` อ่านไม่ได้ = ประตูวันที่ของ E44 fail-open** — 12 ใบ (11 v2 + `MU` ที่เป็น v1) | **ระยะ 3 Task 8**: `FOOTER_RE` รับเพิ่ม 2 รูป — **เดือนล้วน** (`มกราคม 2569` · `เม.ย. 2569`) → **วันที่ 1 ของเดือน** (ruling: ระมัดระวัง = ดูเก่ากว่าจริง ไม่ใช่ใหม่กว่า ⇒ ไม่มี false-fresh ที่จะทำให้ dedup 7 วัน / staleness 90 วัน ข้ามใบที่ควรเข้าคิว) · **ช่วงข้ามเดือน/ข้ามปี** (`31 ก.ค. – 2 ส.ค. 2026` · `24 มิ.ย.–13 ส.ค. 2026`) → **วันแรก** ตรงกับกติกาช่วงวัน–วันเดิม (ปีที่พิมพ์เป็นของวันสุดท้าย ⇒ เดือนแรก > เดือนสุดท้าย = ถอยปี) · ทางเดิมอยู่ **ทางแรกของ alternation ทุก byte** + รูปใหม่ผูกกับ**คลังชื่อเดือนจริง** (ไม่ใช่ `[ก-๙.]+`) ⇒ สำรวจคลัง 908 ใบก่อน/หลัง: **unchanged 896 · newly parsed 12 · lost 0 · changed 0** · **W24 ยิง 11 → 0** และ E44 ตัดสินได้จริง (ทุกใบ < `PROSE_TOKEN_SINCE` ⇒ เงียบเพราะ "ใบเก่า") · **ผลต่อคิว (preflight `ageQueue`)**: `TKN` 288d · `AMATAV` 257d · `ILM` 226d · `SABINA` 167d · `CHD`/`CI`/`PPG` 106d เข้าคิวอายุ (>90 วัน) — 5 แถวแรกของรอบเปลี่ยนจาก `SNA BAM` เป็น `SNA TKN AMATAV BAM ILM` · `AMZN` 46d · `MU` 83d · `FNF`/`MFEC`/`TOST` 76d ยังไม่เข้าคิว · แถว flag จริง 7 แถวไม่ขยับเลย · ★ `tools/preserve-dates.js` **ไม่ได้ใช้** `FOOTER_RE` (เทียบข้อความ footer ดิบกับ HEAD) ⇒ ไม่ถูกกระทบ |
| 26 | queue: แถวอายุสังเคราะห์ (synthetic age) เสีย `model`/`prepAt`/`postcheck` เมื่อรัน `preflight` ข้ามวันซ้ำภายในรอบเดียว | **ระยะ 3 Task 10 (`792c46fd`)**: `tools/queue/preflight.js` เพิ่ม `synthAge(prior, today)` — แถวอายุที่เคยจัดเป็น `'age-gt-90d'` มาก่อน + ยังไม่ `shippedAt` ⇒ ใช้ `flaggedAt` เดิมต่อเนื่อง (ไม่ประทับ `today` ทุกครั้งที่ `preflight` รัน) ⇒ `isNewFlag()` ไม่เห็นว่า flag "เปลี่ยน" ⇒ `model`/`prepAt`/`postcheck` ของรอบเดิมไม่ถูกล้างทิ้งอีก · `preflight()` ต้องโหลด state (`S.load()`) **ก่อน** `plan()` แล้วส่ง `priorStocks: s.stocks` เข้าไป (เดิมโหลดทีหลัง) · เคสที่เคย ship ไปแล้วแต่ยังโผล่ใน `ageQueue` อีก (ship ล้ม/footer ยังไม่ขยับ) ยังคงได้ `flaggedAt = today` ตามเดิม (รอบใหม่จริง ไม่ใช่ของค้าง) · เทส `test/queue-test.js` บล็อก 6c (reproduce แล้วแก้ — ยืนยัน fail ก่อน fix, pass หลัง fix) |
| 27 | queue: guard `postcheck` ของ `shipStock` ไม่ผูกขอบเขตรอบ — `postcheck:'pass'` ค้างจากรอบก่อนทำให้ `ship <SYM>` commit ได้โดยไม่ต้องสั่ง `ship <SYM>` ชัดเจนใหม่ | **ระยะ 3 Task 10 (`792c46fd`)**: `tools/queue/ship.js` แยก guard ออกเป็นฟังก์ชันบริสุทธิ์ `postcheckGuard(sym, rec, startedAt)` แล้วให้ `shipStock` เรียกแทนการเช็ค `rec.postcheck === 'pass'` ตรง ๆ — เพิ่มเงื่อนไข `S.inRound(rec, startedAt)` (กลไกเดียวกับที่ `status()`/`closeIssueIfNoLlmRows()` ใช้อยู่แล้วแต่ `shipStock` ไม่เคยเรียก) ⇒ postcheck ที่ผ่านตั้งแต่รอบก่อน (แถวยังไม่ทัน ship ตอนนั้น แล้วรอบใหม่มาทับ `startedAt`) ถูกปฏิเสธพร้อมข้อความ "ค้างจากรอบก่อน" ต้องสั่ง postcheck ใหม่หรือ `--force` · **fix round 1 (รีวิว F1)**: guard ยอมรับอีกทางหนึ่งเมื่อ `rec.postcheckAt >= startedAt` (postcheck ถูกสั่งในรอบนี้จริง) — จำเป็นเพราะ `postcheck` ไม่เคยแตะ `flaggedAt` ⇒ แถวที่ flag เก่าแต่เพิ่ง postcheck วันนี้ (พบบ่อยหลัง #26 ที่แถวอายุสังเคราะห์คง `flaggedAt` เดิม) จะแก้ตามข้อความไม่ได้เลย เหลือแค่ `--force` · เทส `test/queue-test.js` บล็อก 19d ส่วนขยาย (รวมเคส postcheckAt สด → ผ่าน / postcheckAt เก่า → ยังปฏิเสธ) |
| 28 | queue: `roundStart` ยังผูก `flaggedAt > prev` — flag ใหม่จริงที่ลงวันที่ก่อนรอบเริ่ม (เช่นค้างจากรอบก่อน) จะไม่เปิดรอบใหม่ (ตั้งใจแบบระวังไว้ก่อน สังเกตผ่านข้อความ "ค้างจากรอบก่อน") | **ระยะ 3 Task 10 (`792c46fd` · ruling: ไม่แก้พฤติกรรม)** — เปลี่ยนพฤติกรรม `roundStart` มีความเสี่ยงเปิดรอบซ้ำผิดจังหวะ ต้นทุนสูงกว่าประโยชน์ที่ยังไม่มีเคสจริงมายืนยัน ⇒ คงเงื่อนไข `flaggedAt > prev` ไว้ตามเดิม · แก้แค่ **ข้อความ** ให้ชัดขึ้นแทน: `tools/queue/ship.js` `status()` เปลี่ยนบรรทัด `ค้างจากรอบก่อน N: …` เป็น `ค้างจากรอบก่อน — ยังไม่นับเป็น flag ใหม่ของรอบนี้ (N): …` · เทส `test/queue-test.js` บล็อก 19g อัปเดตตาม |
| 30 | `gen-docs.js` เขียน 5 เป้าหมายแบบไม่ atomic · marker ที่อยู่ใน code fence ของเอกสาร render ออกมาเป็น HTML comment ที่มองเห็นได้ (รับสภาพ) | **ระยะ 3 Task 10 (`792c46fd`)**: `tools/gen-docs.js` เพิ่ม `atomicWrite(file, content)` (เขียนไฟล์ `.pid.random.tmp` แล้ว `fs.renameSync` ทับ — `rename` เป็น syscall เดียวที่ atomic บน filesystem เดียวกัน) แทน `fs.writeFileSync` ตรง ๆ ในลูปเขียน `TARGETS` ทั้ง 5 ไฟล์ ⇒ process ถูกขัดจังหวะกลางเขียนไม่ทำให้ไฟล์ค้างครึ่งเดียวอีก (ผู้อ่านเห็นได้แค่เนื้อเก่าเต็มไฟล์หรือเนื้อใหม่เต็มไฟล์) · **fix round 1 (รีวิว F2)**: `chmodSync(tmp, statSync(file).mode & 0o777)` ก่อน `rename` — `.githooks/pre-push` เป็น TARGETS ตัวหนึ่งและเป็น `100755`, rename ทับด้วย tmp ที่ mode ปริยายจะทำให้ exec bit หาย แล้ว git เลิกรัน hook โดยไม่เตือน (เทส 0755 ใน (ก-5)) · เทส `test/docs-test.js` (ก-5) มอนกี้แพตช์ `fs.writeFileSync` ให้ throw กลางเขียนไฟล์ชั่วคราวแล้วยืนยันไฟล์เดิมไม่ถูกแตะเลย · **ส่วน marker ใน code fence ที่ render เป็น HTML comment มองเห็นได้ — ruling เดิมคงไว้ ไม่แก้** (ยอมรับสภาพตามที่ระบุไว้แต่แรก) |
| 32 | `tools/median-multiples.js` นับทุกคอลัมน์ FY ส่วน `fyYears` (Task 24) นับเฉพาะ FY ที่มี EPS(dil) จริง — สองเครื่องมือขัดกันได้บนหุ้นตัวเดียวกัน | **ระยะ 3 Task 10 (`792c46fd`)**: `tools/fetch-fundamentals.js` แยก `fyEpsCountFromArrays(dk, eps)` ออกจาก `fyEpsCount(fin)` เดิม (ตัวเดียวกัน export เพิ่ม) แล้ว `tools/median-multiples.js` เรียกฟังก์ชันนี้ตรง ๆ บนอาร์เรย์ `dk`/`eps` ที่ตัวเองดึงมาแล้ว (ไม่มีลอจิกนับ FY ของตัวเองอีก ⇒ สองเครื่องมือดริฟท์กันไม่ได้อีกโดยโครงสร้าง) — ผล `oneSymbol()` มี field ใหม่ `fyYears` พิมพ์ในบรรทัด `FY ที่มี EPS(dil) จริง: N` ของ `report()` ให้ตรงกับบรรทัดเดียวกันที่ `printFinancialTable` พิมพ์ (เทียบกันได้ตรง ๆ) · `rows` ในรายงานยังคงแสดงทุกคอลัมน์ FY ที่ `datekey` parse ได้ตามเดิม (ให้ worker เห็นเหตุผลที่ปีนั้นถูกข้าม เช่น "ไม่มี EPS") — เปลี่ยนแค่ตัวเลขสรุปที่ใช้เทียบ ไม่ใช่การแสดงผลรายบรรทัด · เทส `test/median-multiples-test.js` เคส FY คอลัมน์ว่าง (`epsDiluted: [...,'-',...]`) ยืนยัน `fyYears` ตรงกับ `fyEpsCountFromArrays` เป๊ะ |

## ปิดแล้ว (open-items-triage-fix 23 ก.ย. 69)

> แผน `docs/superpowers/plans/2026-09-23-open-items-triage-fix.md` (advisor-reviewed 2 รอบก่อนเริ่ม, subagent-driven-development ต่อ task) — worktree `open-items-triage-fix` ยังไม่ merge ตอนเขียนบรรทัดนี้ (คอมมิตอ้างในนี้อยู่บน branch นั้น รอ PR)

| # | รายการ | ปิดโดย |
|---|---|---|
| 43 | คลาสสีของช่องสรุป "ส่วนต่างจากราคา" ไม่ตรงเครื่องหมาย MOS — 14 ใบ | **Task 1**: ย้ายจาก inline hex (`style="color:#…"` ค้างตายตัวตั้งแต่วิเคราะห์ ไม่เคย sync ตาม cron) เป็น CSS class `.v.bad/.ok/.good` reuse `mosBand()` เดียวกับกล่อง `.mos-verdict` ใหญ่ (ตัดสินหลัง advisor review 2 รอบ: ตัวเลือกแรก "ให้ cron เขียน hex สดทุกรอบ" ขัด CLAUDE.md §10 จึงเปลี่ยนเป็น class) · แก้ skeleton ทั้ง th/us ด้วย (ไม่ใช่แค่ 14 ใบเดิม) กัน regression ในใบใหม่ · checker ใหม่ `W26` (warn ไม่ใช่ error — บรรทัดฐานเดียวกับ W06) · sweep ทั้งคลัง 909 ใบ (`heal → build → preserve-dates → build`, 0/909 `updated` ขยับ) — commits `99a84b24a` (fix) + `833acf2ad` (sweep) · task review: Approved, 0 Critical/Important (1 fix round: เติม `Co-Authored-By` trailer ที่หายจาก 2 commit แรก) |
| 42 | ช่อง `.ret` หมวด 6 เสียคำขยาย/จำนวนเงินตอนย้าย v1→v2 (เดิมประเมิน 45 ใบ) | **Task 2**: ตรวจซ้ำก่อนแก้พบว่า**เสียจริงแค่ 24/45 ใบ** (อีก 21 ใบ `migrate-v2.js` ข้าม `.ret` ไปเลยตอน migrate ไม่มีอะไรหาย — ยืนยันด้วย `git show <migrate-commit>`) กู้คืน 23 ใบจาก git history + วาง token-adjacent ใน `.ret` (พิสูจน์กับ SO ก่อนทำใบอื่นตามแผน) · **3 ใบ (MAA/CPT/AP) ได้ป้ายแก้ใหม่ `(ไม่รวมปันผล)` แทนป้ายเดิมที่ผิด** (ยืนยันด้วยเลขคณิต 9/9 จุดตรงกับ capital-only ไม่ตรง dividend-inclusive — ทั้ง controller และ reviewer ตรวจซ้ำอิสระกัน) · `THCOM` กู้คืนไม่ได้ (เครื่องหมาย `~` ที่ token แสดงไม่ได้ — ค้างเป็น gap ที่ยอมรับ) · commit `d2fea9f61` (23 ไฟล์ + reports.json) · task review: Approved, 0 Critical/Important, 0 fix round · Group B (21 ใบที่ไม่เสียจาก migration) เจอปัญหาคนละชนิด 2 คลาส → เปิดเป็น #47 แทน |
| 20 | ~23 ใบ EPS ค้าง >2% (HSY SNNP COP NUE CF …) | **Task 3 (23 ก.ย. 69)**: ตรวจ 5 ใบที่ระบุชื่อไว้ (HSY SNNP COP NUE CF) ผ่าน `node tools/queue.js prep <SYM>` สดใหม่ — **HSY/SNNP/COP/NUE ผ่าน EPS screen แล้ว (Δ 0.0–0.4%) ไม่ค้างอีกต่อไป** (แก้ไปแล้วระหว่างแคมเปญ 908 ใบ 21–22 ก.ย. หรือ UPDATE รอบอื่นก่อนหน้านี้) · **CF ขึ้น "44.2% ⚠" แต่ตรวจไฟล์จริงแล้วเป็น false positive ของ prep-stock ชนิด adj-vs-GAAP** (ตรงกับกับดักที่บันทึกไว้ใน memory `data-source-traps.md §6d`): รายงาน CF ปัจจุบัน (footer 21 ก.ย. 69 — วิเคราะห์เมื่อ 2 วันก่อน) ใช้ EPS mid-cycle normalized $7.50 เป็นขา FV โดยตั้งใจ (หุ้นวัฏจักรปุ๋ยไนโตรเจน) ส่วน EPS TTM GAAP $13.44 ใช้เป็นบริบทเท่านั้น — ตรงกับที่ `docs/quality-gate.md` §0.4b กำหนดไว้พอดี ไม่ใช่ของค้าง · **ปรึกษา advisor ผ่าน courier ก่อนพบว่าเป็น false positive** (ตามกติกา CLAUDE.md §3.2 สำหรับหุ้นยาก/วัฏจักร — คำแนะนำที่ได้ยังมีประโยชน์เผื่อทบทวน mid-cycle assumption รอบหน้า) · **อัตราค้างจริงของกลุ่มเสี่ยงที่สุ่มตรวจ = 0/5 ⇒ ตามกฎขยายขอบเขตของแผน ไม่ขยายไปสแกนทั้ง 908 ใบ** — ถือเป็นสัญญาณว่าแคมเปญ 21–22 ก.ย. ปิดของค้างนี้ไปแล้วในทางปฏิบัติ ไม่ใช่กรณีที่ต้องสแกนเพิ่ม · **ขอบเขตที่ตรวจจริงแคบกว่าที่แผนกำหนดไว้**: Task 3 Step 0 (owner ตัดสิน scope B) แบ่งเป็น 2 ส่วน — (ก) ~23 ใบที่เอ่ยชื่อไว้ (ข) หุ้นที่เพิ่งประกาศงบใหม่แต่ยังไม่สะท้อนในรายงาน (earnings-calendar arm) — รอบนี้ตรวจจริงแค่ 5 ใน ~23 ใบที่เอ่ยชื่อ (ไม่ครบ ~23 ใบ) และ**ไม่ได้แตะแขนงปฏิทินงบ (ข) เลย** · ความเสี่ยงของช่องว่างนี้ต่ำ เพราะแคมเปญ 908 ใบ (21–22 ก.ย. 69) เพิ่งวิเคราะห์ทั้งคลังใหม่หมดก่อนรอบนี้ไม่นาน ⇒ ทุกใบมีวันที่ footer ≤2 วัน ทำให้เคสงบใหม่จริงตอนนี้แทบเป็นไปไม่ได้ — แต่นี่คือช่องว่างที่บันทึกไว้ ไม่ใช่ชุดที่ตรวจแล้วว่างเปล่า รอบหน้าที่สืบเรื่องความค้างแบบ #20 ควรตรวจให้กว้างกว่านี้ |

## ปิดแล้ว (open-items-46-47 23 ก.ย. 69)

> แผน `docs/superpowers/plans/2026-09-23-open-items-46-47.md` (advisor-reviewed ก่อนเริ่ม) — worktree `open-items-46-47`

| # | รายการ | ปิดโดย |
|---|---|---|
| 46 | **กล่อง "เป้านักวิเคราะห์ 12 ด." เขียวตายตัวจาก skeleton เอง** (`_template/skeleton-{th,us}.html`, inline `style="color:#…"` hardcode) | **Task 1**: เจ้าของเลือก **ตัวเลือก C — ตัดสีออกทั้งหมด** (ไม่ใช่ (A) `.v.bad/.ok/.good` แบบ #43 หรือ (B) บังคับ token ทุกใบ — (B) ตกเพราะ `need()` throw บนใบที่ไม่มี consensus) เพราะกล่องนี้ไม่มีเครื่องหมาย pos/neg ที่แน่นอนให้ผูกสี (ต่างจาก MOS ของ #43) ⇒ ถอด `style="color:#…"` ออกจากกล่อง "เป้านักวิเคราะห์" ทั้งสอง skeleton + สแกนคลัง 908 ใบ (909 − CPW.html ที่ใช้ `style="color:var(--muted)"` ซึ่งเป็น CSS variable ที่ใช้ร่วม 908+ จุดทั่วคลัง ไม่ใช่ hex ตายตัวชนิดเดียวกับที่ตัดออก — ยกเว้นไว้โดยตั้งใจ) · sweep รอบแรก matched ด้วยข้อความป้ายไทย พลาด 17 ใบที่ป้ายถูกเขียนใหม่/กล่องถูกใช้เนื้อหาอื่นแทน — fix round 1 เปลี่ยนเป็น DOM-position matching (vcell ตัวที่ 3 ใน `.vgrid` ต่อจาก MOS vcell) แก้ครบทั้ง 908 ใบ · commit `297900686` (amend ครั้งเดียวระหว่าง fix round 1) · task review รอบ 2 (re-review): ผ่าน ตรวจ diff ทั้ง 908 ใบไม่ใช่สุ่ม + สุ่มตรวจ regression 15 ใบเดิม — clean · ไม่เพิ่ม checker ใหม่ (ตามแผน — ไม่มีเครื่องหมายให้ตรวจ) · **ผลข้างเคียงที่รู้ตัว**: ไม่มี guard กันสีกลับมาถ้า worker เขียน `style` ใหม่ในอนาคต (fix-on-touch หากเกิดซ้ำ) |
| 47 | `.ret` หมวด 6 มี content drift 2 คลาสที่ไม่ใช่ migrate-v2 blind spot (WELL label flip · NI/LNT/DTE/EVRG/PEP คำขยายหาย) | **Task 2**: ทำเลขคณิตแบบ WELL (`(tgt−px)/px` capital-only เทียบ `(tgt+div−px)/px` dividend-inclusive เทียบ % ที่โชว์จริง) ครบทั้ง 6 ใบ 18/18 คอลัมน์ (bear/base/bull × WELL/NI/LNT/DTE/EVRG/PEP = 6×3) — **ทุกคอลัมน์ป้าย "รวม"/"ไม่รวม" ตรงกับสูตรที่ตัวเลขจริงแมตช์ 100%** ไม่มีจุดขัดแย้งเหลืออยู่ในไฟล์ปัจจุบันสักจุด: **WELL** label flip `(รวม div)`→`(ไม่รวม div)` ยืนยันเป็น**การแก้ (correction)** ไม่ใช่บั๊ก (capital-only แม่นกว่า dividend-inclusive ทั้ง 3 คอลัมน์ — ตรงกับที่ investigation ตอนวางแผนสรุปไว้แล้ว) · **NI/LNT/EVRG/PEP** ทุกคอลัมน์ dividend-inclusive ตรงกับป้าย "รวมปันผล" ที่ hint ประกาศ แม่นถึงทศนิยม (0.0–0.5pp) · **DTE** ทุกคอลัมน์ capital-only ตรงกับป้าย "ไม่รวมปันผล" ที่ hint ประกาศ (คำว่า "รวม" ในเซลล์ `.ret` หมายถึง "รวม 3 ปี" (cumulative) ไม่ใช่ "รวมปันผล" — กับดักคำพ้องที่ยืนยันแล้วว่าไม่ใช่บั๊ก) — **คำขยายที่ #47 สงสัยว่าหายไปจาก NI/LNT/DTE/EVRG (bear-scenario) และจำนวนเงินที่หายจาก PEP ไม่ปรากฏอยู่ในไฟล์ปัจจุบันเลย** (คาดว่าถูกแก้ไปแล้วในรอบวิเคราะห์ถัดจากที่ #47 บันทึกไว้ 23 ก.ย. 69 หรือคำอธิบายเดิมอ้างอิงคอมมิตประวัติที่ไม่ตรงกับ HEAD ปัจจุบัน — ไม่ได้ตรวจ git history ย้อนหลังเพิ่มเพราะไฟล์ปัจจุบันคือของจริงที่คนอ่านเห็น) · **สแกน exhaustive ทั้ง 909 ใบ** หาความขัดแย้งแบบเดียวกัน (`.ret`-cell เอง — WELL-style ป้ายในเซลล์ที่ขัดกับตัวเลข ไม่ใช่ป้าย hint) ด้วยสคริปต์ scratch (ไม่ commit) — **`.ret`-cell เอง: 0/909 ใบขัดแย้งกัน** ยืนยันด้วย 2 ทาง: (1) เลขคณิตตรงของแต่ละคอลัมน์ (2) `npm run verify` ทั้งคลัง 909/909 ผ่าน error 0 (W17 = error level ตรวจ `.ret`/tgt ตรงกับราคาจริงหรือไม่ ถ้าพังจะ error ไม่ใช่ warn) · **v2 745 ใบมี `scnBasis`**: 108 ใบมี hint แบบ literal (ไม่ใช่ token `{{rd:scnNote}}`) — **0/108 ขัดกับ `scnBasis.divIncluded`** · **ปิด #47 ตามหลักฐานนี้ — คำถามเดิมของ #47 (`.ret`-cell/hint ขัดกันเอง) ไม่พบเลยทั้งคลัง** ★ ระหว่างสแกนพบปัญหา**คนละคลาส** (hint-**header** text ขัดกับตัวเลข ไม่ใช่ `.ret`-cell เอง — ตัวอย่างจริง: `COR.html`) → ไม่ปนกับ #47 ตามคำแนะนำ advisor เปิดเป็น **#48** แทน (แก้ COR แล้ว + เปิดค้าง DVN/POOL ที่กำกวมจริง + บันทึกหลักฐาน stale-entry VMC/SCC/BTS/RKLB) |

## ปิดแล้ว (docs catch-up 23 ก.ย. 69)

| # | รายการ | ปิดโดย |
|---|---|---|
| 45 | **แคมเปญวิเคราะห์ใหม่ 908 ใบ (21–22 ก.ย. 2569): ทะเบียนบั๊ก 62 รหัส + แผนแก้ไข** — advisor วินิจฉัยว่า **ต้องแก้จริง 3 งาน** (W11 กฎ LIGHT/FULL · W1 patchDerived · W9 เอกสารแหล่งข้อมูลสำรอง) ที่เหลือไม่ต้องแก้/รอเจ้าของ | **เนื้องานปิดครบแล้วตั้งแต่ 22 ก.ย. 69** (W1 = PR #56 · W11 = PR #57 · W9 = PR #58 · docs-catchup = PR #59 · footer พ.ศ. = PR #61 · GULF ship (SOTP) = analyze commit 22 ก.ย. · GULF register close = PR #63 · คำตัดสินเจ้าของทั้ง 3 เรื่องปิดครบแล้ว: RULE-TENSION-001 = เจ้าของเลือกไม่แก้โค้ด 22 ก.ย. — PDYN เติมบรรทัดความไวด้านบน `9043242d` · COCOCO พบว่าบรรทัดความไวมีแค่ด้านขึ้น ไม่ใช้เป็นเคสอ้างอิงจนกว่าจะแก้ — ดู `docs/decisions.md` §8) — **แถวนี้แค่ค้างอยู่ใน "เปิดอยู่" เพราะไม่มีใครย้าย ไม่ใช่ของค้างจริง** พบระหว่างทบทวนของค้างทั้งหมด 23 ก.ย. 69 (แผน `docs/campaign-2026-09-fix-plan.md` PR #64 คือ commit ที่ปิดเนื้องานจริง — บรรทัดนี้เป็นแค่การย้ายให้เอกสารตรง) |

## ปิดแล้ว (Report v3 Plan 2a/2b 24 ก.ย. 69)

| # | รายการ | ปิดโดย |
|---|---|---|
| 50 | escape `<` ใน JSON ของ `<script type="application/json">` + allowlist สี/`gridFmt`/`dataFmt` ใน schema | Plan 2a: `jsonScript()` ใน `_template/v3/render.js` แปลง `<` → `\u003c` · `S.validate` ใช้ `SV.colorOK`/`SV.GRID_FMT_OK`/`SV.DATA_FMT_OK` (`tools/safe-values.js`) ชุดเดียวกับ build |
| 51 | `{{lit:…}}` + `meta.litReasons` (spec §4) | Plan 2a: token `{{lit:…}}` + `meta.litReasons` ในสคีมา (1 เหตุผลต่อ 1 ข้อความ · เหตุผลกำพร้า = error) + `W31 prose-lit` |
| 52 | `report.js save` ต้องรัน compute + รวม error ทุกข้อในครั้งเดียว | Plan 2b: `C.semanticErrors()` เก็บจุด throw ของ compute ครบก่อน compute · `checkDoc` คืน E51 พร้อม `details` ทีละ path · `node tools/report.js save` พิมพ์ครบในครั้งเดียว (`test/v3/report-cli.test.js`) |

## ปิดแล้ว (Report v3 Plan 2c-i 24 ก.ย. 69)

| # | รายการ | ปิดโดย |
|---|---|---|
| 49 | **v3 Plan 2 must-do — ลบ tripwire + ให้ scanner ที่อ่านแต่ `.html` อ่าน `reports/*.json` ด้วย** — ตอนนี้ `test/v3/no-json-reports.test.js` บล็อกไม่ให้มี `reports/*.json` เลย เพราะเครื่องมือเหล่านี้สแกนแค่ `.html` (ไฟล์ `.json` จะหลุด gate/cron/คิวเงียบ ๆ): `test/check-reports.js:1018` (ลูป `checkFile`) · `tools/update-prices.js:888` + `:961` (readdir 2 จุด) · `tools/queue/preflight.js:27` (`readReport`) + `:33` (`listReportsFS`) · `tools/analysis-age.js:27` · `test/tags-test.js:166` · `tools/dead-ticker-canary.js:184` · `tools/spotcheck.js:90` · `tools/earnings-calendar.js:120`/`:122` · `.github/workflows/update-prices.yml:74` (นับ `reports/*.html` ที่ staged) — ลบ tripwire ได้**หลัง**ทุกจุดอ่าน `.json` แล้วเท่านั้น (เลขบรรทัด ณ rebase บน `aa9f5a414`) | **Plan 2c-i (tripwire removed)** — scanner อ่าน `.json` ผ่าน `tools/report-source.js` แล้ว (Plan 2b · PR #74) · `test/v3/no-json-reports.test.js` ถอดเป็น commit โครงสร้างสุดท้ายก่อนใบ v3 ใบแรก (Plan 2c-ii) · ที่ยังค้าง = `update-prices.yml` นับ `.json` + `update-prices.js` main/healDerived readdir → P5 (#61/#62) · `docs/decisions.md` §10 "Report v3 Plan 2c-i" · **residue ปิดใน Plan 3 (P5)**: `update-prices.js` main + `healDerived` วน `RS.list` (`test/v3/cron.test.js` ตรึงว่าไม่เหลือ filter `.html` ล้วน) · `update-prices.yml` นับ `reports/*.json` |

## ปิดแล้ว (Report v3 Plan 3 · P5 24 ก.ย. 69)

| # | รายการ | ปิดโดย |
|---|---|---|
| 61 | **v3 cron path (Plan 3) — `verify:cron` ต้องรวม `check-v3`** ไม่งั้น E50/E51/E52 ถูกข้ามใน gate ของ cron จนกว่า `ci-verify` หลัง push จะจับได้ | **Plan 3 Task 4**: `verify:cron` = check-reports → check-v3 → build → build-test → engine-exec → check-site (`package.json`) · queue-test `0b` ตรึงลำดับ · marker gen-docs · + cron gate ใบ v3 ก่อนเขียนต่อไฟล์ (`planV3` + `IO.writeMarket`) · `docs/decisions.md` §10 "Report v3 Plan 3 (P5)" |

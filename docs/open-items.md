# Open items — ของค้างของโปรเจกต์ (ย้ายจาก memory 11 ก.ย. 2569 · docs-audit §6)

> memory เป็นของ session ไม่ใช่ของโปรเจกต์ — ข้อเสนอที่อยู่แต่ใน memory เคยหาย (29 ส.ค. → ระเบิด 9 ก.ย.) · ไฟล์นี้คือที่เดียวที่บันทึกของค้าง · ปิดแล้ว = ย้ายไปตาราง "ปิดแล้ว" พร้อม commit/PR · เพิ่มใหม่ = 1 แถว ไม่เขียนย่อหน้า

## เปิดอยู่

| # | รายการ | ที่มา | ปิดใน |
|---|---|---|---|
| 1 | `%/ปี` ในประโยคสรุปหมวด 6 เป็น prose — ไม่มี warning/ตัวซ่อม · **ยังเปิดอยู่หลังจบระยะ 2** (Task 18 ทบทวนแล้ว): ระยะ 2 ทำให้**ตารางฉาก**หมวด 6 เป็น token (`{{rd:scNret}}` ซึ่งรวม `%/ปี` ในช่อง) แต่ **ประโยคสรุปที่เป็นร้อยแก้วไม่ถูกแตะ** — `RV.PROSE_BOUND` ของ `E44` มี 6 แพทเทิร์น (`mosZone` · `mosZoneRev` · `px` · `fv` · `mos` · `tgt`) **ไม่มีแพทเทิร์นของ `%/ปี`** ⇒ ทั้ง `E44` และ healer `proseTokens` มองไม่เห็นจุดนี้ | price-derived-staleness:113 | ระยะ 3 (คู่กับ `f64` ที่ deferred ตามข้อ A/B) — ถ้าจะปิดต้องเพิ่มแพทเทิร์นเข้า `PROSE_BOUND` + วัดคลังก่อนเปิด (บทเรียน W18: เกณฑ์ที่กระพริบตามราคาใช้เป็น error ไม่ได้) |
| 5 | W05 มองไม่เห็น "สองขาตระกูลเดียว" (WWD) | price-derived-staleness:174 | ระยะ 3 WS7 fix-on-touch — ไม่ได้แก้ในระยะ 1 (manifest/WS9(a) ที่ปิดแล้วไม่ครอบคลุมเคสนี้ ดู phase1-exit §7) |
| 6 | การ์ด EV/Sales · EV/EBITDA · DCF ไม่มี checker | data-source-traps:258 | ระยะ 3 WS7 fix-on-touch — ไม่อยู่ใน 18 แถว NEITHER ที่ปิดระยะ 1 (ดู phase1-exit §2/§7) |
| 11 | 6 ใบสองขาตระกูลเดียว ยังไม่แก้ | fv-family-weighting:22 | ระยะ 3 WS7 fix-on-touch (ลิสต์ในไฟล์ memory) |
| 14 | `stock-meta.roe` ต้อง null เมื่อขาดทุน (OKJ) — กฎ `checkPairs` (f57↔f40) มีอยู่แล้วตั้งแต่ Task 9 PR #32 | bulk-stock-analysis-workflow:81 | **แก้บางส่วนแล้วโดยอ้อม (ระยะ 2 ส่วน E — Task 16 ตรวจ 14 ก.ย. 69)**: ไฟล์ v2 (865/908) ให้ `ctx.baseEPS` = `report-data.values.baseEps` ตรง ๆ (`tools/report-values.js`) ซึ่งเป็นเลข JSON ที่คงเครื่องหมายลบครบ ⇒ กฎ f57 ยิงถูกต้องแล้วสำหรับ v2 โดยไม่ต้องแก้อะไรเพิ่ม (ยืนยันด้วยโค้ด — ยังไม่พบใบ v2 ที่ `values.baseEps < 0` จริงในคลังตอนนี้ให้ดูยิง) · **ยัง blocked จริงสำหรับ 43 ใบ v1 ที่เหลือ**: `V1_READ.baseEPS` (`test/check-reports.js:221`) ยังใช้ regex `/EPS ฐาน…([0-9.]+)/` ที่ไม่จับเครื่องหมายลบหน้าเลข ⇒ EPS ฐานติดลบในไฟล์ v1 จะอ่านเป็นค่าบวกเสมอ ไม่ต้อง promote ก่อน — ปิดเต็มเมื่อ v1 = 0 (ดู #36) หรือแก้ regex ให้จับ `-`/`−` นำหน้า |
| 15 | คำขอคำศัพท์ 49 รายการใน `tags.json.requests` รอเจ้าของ | tag-system-2026-08:19 | เจ้าของรีวิว |
| 16 | `stock-ai.dotent.workers.dev` ยังเปิด (`workers_dev=true` ห้ามลบ) traffic ไม่ cache | workers-dev-cache-inert:21 | ไม่ทำ (บันทึกไว้) |
| 17 | ไม่มี `x-cache` header — วินิจฉัย cache จากเวลาอย่างเดียว | workers-dev-cache-inert:20 | ไม่ทำ (บันทึกไว้) |
| 20 | ~23 ใบ EPS ค้าง >2% (HSY SNNP COP NUE CF …) | data-source-traps:153 | ระยะ 3 WS7 ตามปฏิทินงบ (WS6 trigger — ปฏิทินงบยังเป็น fallback จนกว่า #24 ปิด) |
| 24 | **ปฏิทินงบ: Yahoo ให้วันที่ <80%** — วัดจริง 12 ก.ย. 2569 รอบเต็ม 908 ใบ: มีวันที่ 719 (79.2%) · **TH `.BK` แค่ 51/238 (21%)** · US 668/670 ⇒ ไม่มีวันที่ 20.8% เกินเกณฑ์ 20% ⇒ ไม่ commit `earnings-calendar.json` / ไม่เปิด workflow (ตัวสร้าง `tools/earnings-calendar.js` + สาย `earningsAfterOf` ใน preflight อยู่ในรีโปแล้ว ใช้นโยบายอายุอย่างเดียวไปก่อน) — ต้องหาแหล่งวันงบหุ้นไทย (ปฏิทินหลักทรัพย์ SET / StockAnalysis earnings date) มาเสริมก่อนเปิด · **ตอนเปิดใช้ให้รู้ไว้:** ตัวสร้างมีกติกา carry-forward แล้ว (รอบที่ยิงล้ม = "ถามไม่ได้" → ยก `next` เดิมมาต่อ ไม่ล้างทิ้ง · ยิงสำเร็จแต่ไม่มีวันที่ = "คำตอบจริง" → ล้าง) และเกณฑ์ 20% คิดจาก `ไม่มีวันที่ / (ทั้งหมด − ยิงล้ม)` (`EC.nodateRatio`) | Task 17 (WS6 ข้อ 2) | ระยะ 3 WS7 (คู่กับ #20 EPS ค้างตามปฏิทินงบ) |
| 25 | **หนี้ที่ระยะ 1 ทำให้ "มองเห็น" แล้ว** — W22 ยิง 24 ใบ (f47 legend↔FV 16 · f24 เป้า gauge↔การ์ด 4 · f46 fairLine↔FV 2 · f49 mFair↔FV 1 · f54 vcell↔.fv-box 1) · W23 ยิง 109 ใบ (ราคาหลุดกรอบ 52 สัปดาห์ที่พิมพ์ไว้ — ขยับตามราคาทุกวัน ห้ามเลื่อนเป็น E โดยไม่รัน sweep ซ้ำ) | phase1-exit §7 | ระยะ 3 fix-on-touch (WS7) |
| 26 | queue: แถวอายุสังเคราะห์ (synthetic age) เสีย `model`/`prepAt`/`postcheck` เมื่อรัน `preflight` ข้ามวันซ้ำภายในรอบเดียว — กู้คืนได้ด้วย `prep <SYM>`/`--model` มือ | phase1-exit §7 | ระยะ 2 / เจ้าของ |
| 27 | queue: guard `postcheck` ของ `shipStock` ไม่ผูกขอบเขตรอบ — `postcheck:'pass'` ค้างจากรอบก่อนทำให้ `ship <SYM>` commit ได้โดยไม่ต้องสั่ง `ship <SYM>` ชัดเจนใหม่ | phase1-exit §7 | ระยะ 2 |
| 28 | queue: `roundStart` ยังผูก `flaggedAt > prev` — flag ใหม่จริงที่ลงวันที่ก่อนรอบเริ่ม (เช่นค้างจากรอบก่อน) จะไม่เปิดรอบใหม่ (ตั้งใจแบบระวังไว้ก่อน สังเกตผ่านข้อความ "ค้างจากรอบก่อน") | phase1-exit §7 | เจ้าของ / ระยะ 2 |
| 29 | `earnings-calendar.js --out` ข้าม gate `--write` และ guard `--limit` ได้ (สงบนิ่งอยู่ตราบที่ปฏิทินงบยังปิดใช้งาน — คู่กับ #24) | phase1-exit §7 | งานเปิดใช้ปฏิทินงบ (ตอนปิด #24) |
| 30 | `gen-docs.js` เขียน 5 เป้าหมายแบบไม่ atomic · marker ที่อยู่ใน code fence ของเอกสาร render ออกมาเป็น HTML comment ที่มองเห็นได้ (รับสภาพ) | phase1-exit §7 | ระยะ 2 docs |
| 31 | เส้นทาง `--th` ของ `/forecast/` ยังไม่เคยทดสอบกับของจริง — ต้อง probe `quote/bkk/<SYM>/forecast/` สักครั้ง | phase1-exit §7 | ระยะ 3 / WS9(b) |
| 32 | `tools/median-multiples.js` นับทุกคอลัมน์ FY ส่วน `fyYears` (Task 24) นับเฉพาะ FY ที่มี EPS(dil) จริง — สองเครื่องมือขัดกันได้บนหุ้นตัวเดียวกัน | phase1-exit §7 | ระยะ 2 |
| 33 | cron schedule เขียน `17 0 * * *` UTC (07:17 น. ไทย) แต่ workflow ถูก GitHub สร้างจริงเวลา 04:40–04:48 UTC ทุกวัน (5/5 วัน · รันเอง ~8 นาที) ⇒ ลงเวลา ~11:45 น. ไทย ซึ่งอยู่**ในช่วง SET เปิดตลาด** ⇒ guard `isIntradayQuote` ข้ามหุ้นไทยทั้งช่วง (ล็อกรอบ 11 ก.ย. มี **230 ครั้ง** "ข้าม — ตลาดยังเปิด") · จำนวนใบ refresh สำเร็จวันธรรมดา **653/674/674** (≈908 − 238 หุ้นไทย) เทียบกับ **897 ใบ** ในรอบวันเสาร์ ⇒ **หุ้นไทย refresh ได้จริงเฉพาะรอบวันเสาร์** เท่านั้น flag ของหุ้นไทยจึงโผล่แค่ snapshot วันเสาร์ ⇒ replay รายสัปดาห์ของเกณฑ์ 5 **นับปริมาณ flag หุ้นไทยของวันธรรมดาต่ำกว่าจริง** | phase1-exit §4(ง)/§7 | เจ้าของ — ต้องตามต่อทันที: เลื่อน cron ไปราว 20:30 UTC (แม้หน่วง 4.5 ชม. ก็ยังก่อน SET เปิด) แล้ววัดใหม่ · "รับสภาพ" ไม่ได้ |
| 34 | รอบ cron แรกที่ถือโค้ดครบทุกส่วน A–E (summary-cell pass #11 · PREPATCH triage · earnings fallback) คือรอบ 13 ก.ย. 2569 — ต้องตรวจ `price-flags.json` + บรรทัด `✓`/`notes`/`patch-rejected` ของรอบนั้น · PONY เข้า quarantine แล้ว 12 ก.ย. ด้วย `W17 (patch ทำให้ตก)` — healer #7 งดออกความเห็นเมื่อสามคอลัมน์ฉาก (Bear/Base/Bull) ขัดกันเอง ⇒ ไฟล์แบบนี้จะตก `patch-rejected` ทุกวันจนกว่าคนจะแก้ (ออกแบบไว้แบบนี้ แต่ต้องมีคนไล่ดูทุกใบ) | phase1-exit §7 | ระยะ 3 / runbook REJECTED bucket |
| 35 | `forecastLine` มองว่า `epsFwd === 0` = ไม่มีค่า (`tools/fetch-fundamentals.js`) — เป็นตัวกันหารด้วยศูนย์ (benign) แต่ถ้า forward estimate จริงเป็น 0 จะพิมพ์ผิดว่า "ไม่มี epsFwd" | รีวิว ระยะ 1 (Task 22) | ระยะ 3 / WS9(b) |
| 36 | **W22 → error รอ v1 = 0** — ★ **นับใหม่หลังระยะ 3 Task 2–6 (15 ก.ย. 2569): v1 เหลือ 25/908 ใบ ⇒ residue > 0 ⇒ ยังไม่ยกระดับ `W22`/`level`/`healer` รอบนี้เช่นกัน** · 25 ใบคือ `ACN ALLE COHU CTAS DTM DXCM FANG FORM GNTX HMPRO LPLA LWLG MU PNC POET RELX SAPPE SHANG SWK TEAM TER TT VLTO VRTX WFC` — **เปิดเผยค่าที่ขัดกันรายใบไว้แล้วที่ `docs/superpowers/audit/2026-09-11-stock-analyzer/phase3-residue-disclosure.md`** (หมวด A = 21 ใบ ambiguous ตัดสินเชิงกลไม่ได้ · หมวด B = 4 ใบคนละคลาส: ACN เนื้อหาจริงจะหาย · FANG ป้าย gauge ไม่มีคำว่า "เหมาะสม" (ตรึงเป็นเทสโดยตั้งใจ) · LWLG บั๊ก rounding ของ cron patch ฝั่ง v1 ที่ v2 แก้ให้ รอคนยืนยันแล้วย้ายด้วย `--v2-baseline` · MU footer ผิดรูป ผูกกับ #38) · สำมะโนตรงกับไฟล์จริงแล้ว (`--census` รอบ 15 ก.ย. — ก่อนหน้านี้ค้างที่ 43 เพราะ Task 2–5 ย้าย 18 ใบโดยไม่ได้สั่ง `--census` และ `mergeCensus` คงแถว residue เดิมไว้ · แก้แล้วใน Task 6) · **ประโยคเดิมด้านล่างเก็บไว้เป็นบันทึกของรอบ 14 ก.ย.** — นับคลังหลัง merge ส่วน E (14 ก.ย. 2569): v1 ยังเหลือ **43/908** ใบ ⇒ ตาม Task 16 brief ("ถ้า v1 > 0 ไม่แตะ level") **ไม่ยกระดับ** `W22`/`level`/`healer` รอบนี้ · residue 43 ใบต่อชนิด (จาก `docs/superpowers/audit/2026-09-11-stock-analyzer/migration-v2-census.md` + `migration-v2-census.json`): legend≠FV >0.5% 16 (COHU CTAS DTM DXCM FORM GNTX HMPRO LPLA RELX SHANG SWK TER TT VLTO VRTX WFC) · จำนวนช่อง %/ปี ไม่เท่าเดิม 5 (ACN JMT RS SCGD SNNP) · ค่าไม่ตรงชั้น 1 (f36/f50) 4 (AMKR LWLG MTI TMAN) · site vcellTgt match ≠1 3 (BABA STX VRT) · site legend match ≠1 3 (FANG MPC MU) · site vcellFv match ≠1 2 (AEM BTG) · กรอบ FV สองที่ไม่ตรงกัน (vcell↔fv-box) 2 (ALLE POET) · FV ไม่ตรงกันเอง (chart.fairLine↔report-data.fv) 2 (PNC TEAM) · cron-diff v1-unstable gate-warn:W22 2 (PTG THCOM) · cron-diff v1-unstable meta:dividendYield,visible 1 (BNY) · site summary match ≠1 1 (MXL) · mFair≠FV >0.5% 1 (SAPPE) · cron-diff v1-unstable visible 1 (WWD) · **W21 คงเป็น warn ทั้งสองกรณี** (`healer: null` — เหตุผลเดิมจาก Task 13: f67 เป็น `owner:'worker'` ไม่มีฟังก์ชันซ่อมอัตโนมัติเทียบเท่า `mirrorStockMetaV2`) · ★ **แก้ข้อความ brief ของ Task 16 เอง**: brief เขียนว่ารอบที่ v1=0 ให้ตั้ง `healer:'build'` แต่ `task-13-report.md` หัวข้อ "ข้อสังเกตส่งต่อ (แก้ไขจากรอบแรก — ให้ Task 16)" **แก้ไขค่านี้แล้ว**: ต้องใช้ `healer:'patchReport'` (มีอยู่แล้วใน `HEALERS`) เพราะ `'build'` ถูกถอดออกจาก `HEALERS` ทั้งหมดใน Task 13 (ไม่มี check ใดในคลังเข้าเงื่อนไข "เสียเฉพาะเพราะ HTML render ค้างจาก JSON โดยไม่มีฟังก์ชันซ่อม") — รอบถัดที่ v1 = 0 ต้องใช้ `'patchReport'` ไม่ใช่ `'build'` | Task 16 · ระยะ 3 Task 6 (`test/check-reports.js` count · `migration-v2-census.md` · `phase3-residue-disclosure.md` · `task-13-report.md`) | เมื่อ residue 25 ใบที่เหลือถูก re-analyze/ย้ายครบ (v1 = 0) |
| 37 | **E44 = fix-on-touch ที่บังคับด้วย gate** (ระยะ 2 ส่วน F · Task 17 · ปรับตัวเลขตาม review fix round 1) — ใบเก่าทุกใบจะติด E44 **ครั้งแรกที่ถูก re-analyze** เพราะทุกโหมดต้องขยับ footer เป็นวันนี้ (กติกาใน SKILL 5B/5C · postcheck บังคับให้เฉพาะเส้นทาง queue ไม่ใช่ทั้ง gate) ⇒ worker ต้องแปลง prose ผูกราคาเป็น {{rd:…}} ในรอบนั้น (คลัง 14 ก.ย. 2569: **5,293 จุดใน 907 ใบ ~5.7 จุด/ใบ**) · ★★ **ต้องแยกสองตัวเลขนี้ออกจากกัน**: healer แทนจุดที่ literal ตรงทุก byte ได้ **1,534 จุด/550 ใบ (~30% ของจุด)** แต่ **`--heal-derived --write` เขียนไฟล์ได้จริงแค่ 27/550 ใบ (~5%)** เพราะมันเขียนต่อเมื่อ `gateAfterPatch` ของไฟล์นั้นผ่าน (all-or-nothing รายไฟล์ — safety property ที่ healer ทุกตัวพึ่งอยู่ **ห้ามแก้**) และไฟล์ส่วนใหญ่มีจุดที่ heal ไม่ได้ปนอยู่ ⇒ ลำดับที่ใช้ได้จริง = **dry-run ดูรายการ → แก้มือให้ครบ → `--write` ปิดท้าย** (SKILL 5A/5B เขียนตามนี้แล้ว) · อีก ~26% ของจุดต่างแค่รูปแบบ ($129 vs $129.00 · ~19% vs +19%) และ ~44% เป็นเลขค้างจริงซึ่ง**ต้องให้คนอ่านประโยคแล้วเขียนใหม่** · **ยังไม่มี loose-mode healer โดยตั้งใจ** (เขียนเลขในย่อหน้าใหม่อัตโนมัติ เช่น $297 → $332.27 ทำให้ประโยครอบ ๆ ผิดความหมายเงียบ ๆ — reviewer สุ่มอ่าน 8 จุดยืนยันว่าประโยครอบ ๆ ค้างด้วย) · ถ้าภาระเวฟสูงเกินรับ ทางเลือกคือเลื่อน `RV.PROSE_TOKEN_SINCE` ออกไป (แก้ค่าเดียว) ไม่ใช่ผ่อนเกณฑ์ · หมายเหตุ: ใบ post-SINCE ที่ prose มี literal ค้าง (ไม่ควรเกิด เพราะ gate บล็อกตั้งแต่ push) จะโดน cron กัก `patch-rejected` รายไฟล์ทุกวันจนกว่าคนจะแก้ — พฤติกรรมเดียวกับ PONY/W17 (#34) | Task 17 (`tools/report-values.js` · วัดด้วย `RV.proseBoundHits` ทั้งคลัง · review fix round 1) | ระยะ 3 — ทบทวนหลังเวฟ re-analysis แรกหลัง 14 ก.ย. 2569 |
| 38 | **footer "ข้อมูล ณ" ที่ `FOOTER_RE` อ่านไม่ได้ = ประตูวันที่ของ E44 fail-open** (Task 17 fix round 1 · finding 2) — **11 ใบ v2** (อยู่ในขอบเขต W24) + **1 ใบ v1 (`MU`) ที่อยู่นอกขอบเขตโดยดีไซน์** เพราะ E44/W24 ใช้กับไฟล์ v2 เท่านั้น (v1 ไม่มี token ให้ใช้) — ข้อความ footer จริงของแต่ละใบ (อ่านจากไฟล์ 14 ก.ย. 2569): `AMATAV` "ข้อมูล ณ มกราคม 2569" · `AMZN` "ข้อมูล ณ 31 ก.ค. – 2 ส.ค. 2026" · `CHD` "ข้อมูล ณ มิถุนายน 2569" · `CI` "ข้อมูล ณ มิถุนายน 2569" · `FNF` "ข้อมูล ณ กรกฎาคม 2569" · `ILM` "ข้อมูล ณ กุมภาพันธ์ 2569" · `MFEC` "ข้อมูล ณ กรกฎาคม 2569" · `PPG` "ข้อมูล ณ มิถุนายน 2569" · `SABINA` "ข้อมูล ณ เม.ย. 2569" · `TKN` "ข้อมูล ณ ธันวาคม 2568" · `TOST` "ข้อมูล ณ กรกฎาคม 2569" · (v1) `MU` "ข้อมูล ณ 24 มิ.ย.–13 ส.ค. 2026" ⇒ E44 **และ** `proseTokensIfNew` ข้ามทั้งใบ · ปิดเสียงเงียบแล้วด้วย **W24** (warn — ยก E44 ให้ยิงแทนจะบล็อก push 11 ใบเก่าที่ประตูวันที่ตั้งใจยกเว้น และงานนี้แก้ไฟล์ใน `reports/` ไม่ได้) · ทั้ง 12 ใบเขียนวันที่ที่คนอ่านออกว่าก่อน SINCE ⇒ **ไม่มี E44 จริงถูกซ่อน** · ★ `MU` (v1) ยังไม่มี check ไหนพูดถึง footer ที่อ่านไม่ได้ของมัน — ยอมรับไว้ก่อน (ถ้าอนาคต v1 = 0 ตาม #36 ข้อนี้หายไปเอง) · ⛔ **การขยาย `FOOTER_RE` ต้องเป็น task ของตัวเอง**: เป็นตัวอ่านร่วมของ `preflight`(triage)/`preserve-dates`/`postcheck` — ขยายแล้ว 12 ใบนี้จะพลิกจาก "อ่านไม่ออก" เป็น "อ่านออกและเก่า" ในสายตา preflight (เปลี่ยน bucket ของรอบคิว) และ "เดือนล้วน" ต้องตัดสินเองว่าใช้วันที่ 1 หรือวันสุดท้าย · ★ **residual ที่ต้องรู้ (fix round 2)**: `postcheck` **ไม่ใช่ gate** — ไม่อยู่ใน `verify`/`pre-push` · เดินเฉพาะ `npm run queue -- ship` และข้ามได้ด้วย `--force` · เส้นทาง "วิเคราะห์ X → verify → auto-push" (CLAUDE.md §2/§5) ไม่เรียกเลย ⇒ **ใบ NEW ที่ footer ผิดรูปจะผ่าน gate ด้วย warning แล้ว auto-push ได้ โดย E44 ไม่เคยถูกประเมินกับใบนั้น** · สิ่งที่กันอยู่จริงคือ (1) โครง skeleton ที่เขียน footer รูป parse ได้มาให้ (2) กติกาใน SKILL 5A/5B (3) W24 ที่เห็นใน verify — เป็นแบบ "warn ไม่บล็อก" ตามแบบที่รีโปนี้ใช้กับเคสขอบที่ซ่อมอัตโนมัติไม่ได้ ไม่ใช่ช่องเงียบ · ทางปิดถาวรคือทำ #38 นี้ (ขยาย FOOTER_RE อย่างระมัดระวัง) หรือรอให้ 12 ใบถูกแตะเอง | Task 17 fix round 1 (`tools/queue/footer-date.js` · `test/check-reports.js` W24) | ระยะ 3 |
| 39 | **E44 ชั้น (ข) — เลขที่บังเอิญเท่ากับค่าฐาน** (Task 17 fix round 1 · finding 3 · **ยังไม่มีเคสจริงในคลัง**) — ป้ายที่ชี้ได้หลายค่า (`Fair Value` ของฉาก · `โซน MOS 20%` ของ peer) ถ้าเลขบังเอิญเท่ากับ `report-data.fv`/`mos20`/`mos30` เป๊ะ E44 จะฟ้องและ healer จะแปลงเป็น token ⇒ เลขของฉาก/peer จะวิ่งตามค่าฐานตลอดไป · reviewer ค้นทั้งคลัง: 122 จุดเข้าข่าย **อ่านแล้วเป็นค่าฐานแท้ทุกจุด ⇒ 0 เคสจริง** · **ไม่แก้ด้วยโค้ดรอบนี้โดยตั้งใจ**: สัญญาณที่จะแยก "ของฉาก" ออกจาก "ของฐาน" ต้องอ่านความหมายประโยค — stop-list คำว่า ฉาก/กรณี/peer จะทั้งแคบเกินจนไม่มีผล (0 เคส) และเสี่ยงปิดปาก hit จริงในประโยคที่มีคำเหล่านั้นปนอยู่ · เตือนไว้ใน SKILL 5A แทน ("เขียนเลขให้ต่าง หรือย้ายออกนอก `<p>`/`<li>`") · ทบทวนใหม่ถ้าเจอเคสจริงใบแรก | Task 17 fix round 1 (`tools/report-values.js` `PROSE_BOUND` ชั้น (ข)) | ระยะ 3 |
| 40 | **cron รอบแรกบนคลัง v2 ยังไม่ได้ออกแรง write path** (Task 18 · เกณฑ์จบระยะ 2 §5.4) — run `34853640500` (14 ก.ย. ~21:14 +07 · main `1263d971`) สำเร็จ gate 908/908 error 0 · `patch-failed`/`patch-rejected` = **0** · แต่ **patch จริง 0 ใบ** (ข้ามเพราะตลาดเปิด 670 · ราคาไม่ขยับ 235 · freeze 3 = MTI mos-sign-flip · RS/SSP drift-gt-15pct ซึ่งเป็นชุดเดียวกับรอบ 17:50 ไม่เกี่ยวกับ v2) ⇒ **ตัวเลข `patch-failed 0` จริงแต่ยังไม่ใช่หลักฐานว่าเขียนได้** · หลักฐานที่มีอยู่มาจาก Task 14/14a (เทียบพฤติกรรม cron v1↔v2 ทุกใบที่ย้ายบนกริดราคา 61 จุด + เดิน 20 วัน ±2% · gate ตก 0 · throw 0 — offline ล้วน) · **ต้องตามต่อ**: รอบ cron ตามกำหนดรอบถัดไปที่ราคาขยับจริง → ตรวจ `price-flags.json` + บรรทัด `✓`/`notes`/`patch-rejected` ของรอบนั้น (คู่กับ #34 ที่ทำแบบเดียวกันให้ส่วน A–E) · ★ ระวัง #33: หุ้นไทย refresh จริงเฉพาะรอบวันเสาร์ ⇒ ใบไทย v2 อาจยังไม่ถูกแตะจนถึงเสาร์ | Task 18 (`phase2-exit.md` §5.4 · ruling ส่วน E ใน ledger) | ระยะ 3 / เจ้าของ — ตรวจรอบ cron ถัดไปที่มี patch จริง |
| 41 | **`V2TOKENS` ไม่ถูกนับในเลข "N/48" ของไฟล์** (Task 18 · residual ที่เปิดเผยไว้) — เป็น pseudo-error นอกตาราง `CHECKS` แบบเดียวกับ `EXPAND`/`V2SCHEMA` ⇒ ไฟล์ที่ติด `V2TOKENS` จะพิมพ์ `errPass` ต่ำกว่าที่ป้าย `N/48` สื่อ 1 หน่วย · เลือกแบบนี้เพราะยกเป็น E-code จริงต้องประกาศ healer ซึ่งไม่มี (ช่องที่กลับเป็น literal ต้องให้คนเขียน token คืน) และจะขยับจำนวน 48/19 ที่ `gen-docs` เป็นเจ้าของ | Task 18 (`test/check-reports.js` · precedent Task 10 `V2SCHEMA`) | เมื่อรีโปตัดสินใจนับ pseudo-error ในตัวหารเดียวกัน (ทั้ง 3 ตัวพร้อมกัน) |
| 42 | **ช่อง `.ret` หมวด 6 เสียคำขยาย/จำนวนเงินตอนย้าย v1→v2** (สแกน 876 การย้ายจริงในประวัติ — รีวิว Task 3 ระยะ 3) — **39 ใบเสียคำขยายที่ไม่มีตัวเลข** (`(รวมปันผล)` `(capital gain)` `(capital)` `(ราคา)` `(รวม div)` `(รวม)`): `TRUE UDR VTR WEC WELL XEL XOM SO SYY T TACC TEAMG TOP PLD PTT PTTEP RAM SAUCE SCC MRK NDSN NI KTC LNT LOW MAA FR HLT HUMAN CPT CRC DOC DTE EIX EVRG AOT AP APO BCH` · **+6 ใบเสียจำนวนเงินด้วย** (คลาสที่รายงานเดิมไม่ครอบ): `PLD` (`+1% (ราคา) + ปันผล ~$13`) `TRI` `PEP` `FR` `ECL` `BMY` · **+2 ใบจากระยะ 3 Task 5**: `WWD` เสีย `(3ปี)` ×3 คอลัมน์ · `THCOM` เสีย `~` ทั้ง 3 คอลัมน์ · ★ **หนักสุด `SO`** — `(capital gain)` หายและ hint ไม่ได้ประกาศฐานเลย ⇒ คนอ่านตัดสินไม่ได้ว่า +3% รวมปันผลหรือไม่ (utility yield ~3.5% ⇒ ต่างกันมาก) · ตรงกับคำเตือนใน CLAUDE.md §8 ("ห้ามเชื่อคำว่า รวมปันผล ใน hint") ⇒ เมื่อ `.ret` เคยเป็นที่เดียวที่บอกฐาน การทิ้งมันทำให้ฐานหายถาวร · **เหตุ**: ยามนับหน่วยเห็นเฉพาะ "หน่วยต่อปี" · `sameMoney` ไม่คุมช่องนี้ · ชั้น 2 `stripVolatile` blank ทั้งช่อง ⇒ ไม่มีด่านไหนเห็น · **ไม่ใช่ของที่ระยะ 3 สร้าง — เป็นหนี้ของระยะ 2 ที่ merge ไปแล้ว (Task 6 บันทึกไว้ ไม่ได้แก้)** · ทางแก้ที่เสนอ: ยามเทียบ "ข้อความที่ไม่ใช่โทเคน % ในช่อง `.ret`" ก่อน/หลัง + กวาดย้อนหลังใบที่ hint ไม่ประกาศฐาน (ตั้งต้น SO PLD FR BMY TRI PEP ECL) | รีวิว Task 3 + Task 5 ระยะ 3 · `phase3-residue-disclosure.md` หมวด C | รอบถัดไป (กวาดย้อนหลัง + ยามใน `tools/migrate-v2.js`) |
| 43 | **คลาสสีของช่องสรุปไม่ตรงเครื่องหมาย MOS — 14 ใบ** (รีวิว Task 2 ระยะ 3 · F5): ช่องสรุปเป็นสีเขียวขณะ MOS ติดลบ — `AKAM CEG COHU DELL DXCM DY ELV ETN EXPE GLW MSFT MXL NOK SAP` (หนักสุด DELL −31.9 · SAP −14) · **มีอยู่ก่อนระยะ 3 ทั้งคลัง** (13/14 เป็น v2 อยู่ก่อนแล้ว) ⇒ ไม่ใช่ regression ของการย้าย · เหตุ: **ไม่มีใครคำนวณคลาสสีของช่องสรุป** — `summaryPlan` (healer #11) เขียนข้อความ `MOS ~ ±X%` แต่ไม่ได้ตั้ง class และไม่มี check ไหนเทียบสีกับเครื่องหมาย (ต่างจากช่อง `.ret` ที่ v2 คิดสีจากตัวเลขที่พิมพ์เอง) | รีวิว Task 2 ระยะ 3 (F5) · `phase3-residue-disclosure.md` หมวด C | รอบถัดไป — ตัดสินก่อนว่าจะให้ cron เขียนสี (ขยาย `summaryPlan`) หรือเพิ่ม check ให้คนแก้ |

## ปิดแล้ว (ระยะ 0)

| # | รายการ | ปิดโดย |
|---|---|---|
| 3 | controller ต้อง diff เป้า/52wk/ปันผล/P-BV กับ prep ก่อน dispatch — อยู่แต่ใน memory | `npm run queue -- prep` (Task 12) |
| 12 | MEMORY.md บอกว่าเคส MC ใน quality-gate.md ผิด ทั้งที่แก้แล้ว | Task 19 Step 2 (memory) |
| 18 | เส้นทาง `analyze-wave` (Workflow) inject CLAUDE.md ไหม · default model ทางนี้ | Task 20 probe 12 ก.ย. 2569 (เจ้าของสั่ง · args ไม่ส่ง `model` · effort low): (1) "You are powered by the model named Sonnet 5" — ค่านี้มาจาก script pin `waveModel = args.model \|\| 'sonnet'` (`analyze-wave.js:20`) ไม่ใช่ default ของ harness ⇒ เส้นทางนี้**แน่นอน**ต่างจาก Agent tool (2) **ใช่** inject CLAUDE.md — worker เห็น "## 1. โครงสร้างโฟลเดอร์" (3) **ใช่** มี MEMORY.md index ~30 บรรทัด ⇒ role-scope `[controller]` ใน CLAUDE.md ใช้กับ worker ทั้งสองเส้นทาง |
| 21 | fixture ของ self-test/update-prices-test ผูกกับ BBL/AAPL จริง | Task 1–2 (PR #A) |

## ปิดแล้ว (ระยะ 1)

| # | รายการ | ปิดโดย |
|---|---|---|
| 2 | ป้าย marker gauge "เหมาะสม" ไม่มี gate (ICLR) | Task 9 (PR #32): field `f49` (ป้าย gauge mFair) ได้ binding `pair` → f44 (money) ⇒ ยิงเป็น **W22** เมื่อไม่ตรง |
| 4 | median-multiples ส่งค่าผิด 2 คลาส (สุดขั้ว · ผสมสกุล) — sanity ใน runbook แล้ว แต่ยังไม่มี fixture test ของตัวมันเอง | Task 23: `oneSymbol(spec, th, deps)` ฉีด `fetchFinPage`/`monthlyCloses`/`statementCurrency`/`finRow` ผ่าน `deps` (default = ของจริง) + `test/median-multiples-test.js` (offline, เรียกจาก `test/prep-stock-test.js`) ครอบ TTM/EPS≤0/outlier สุดขั้ว (เคส AU 2,074x)/ผสมสกุล (เคส CP)/MIN_POINTS |
| 7 | E41 ข้ามป้าย "P/E เฉลี่ย ~N ปี" — ป้ายอ้างช่วงยาวกว่าอายุหุ้น (GABLE) | Task 24: `parseVendor` นับ `fyYears` จากคอลัมน์ตัวเลขจริงบนแถว `EPS(dil)` ของตาราง [3] (หัก TTM เมื่อหัวตารางมี) → `prep()` บันทึกลง state + เติมบรรทัดเตือนใน prompt worker · `postcheck.checkFyYears(f55, fyYears)` ฟ้องเมื่อป้ายการ์ดเกินจริง — ★ นี่คือทางปิด #7 จริง แยกจาก manifest pair `f55↔f04` (`how:'years'`) ซึ่ง**ยังไม่ยิงใบไหนเลย (latent)** เพราะกราฟราคาไม่มีป้ายปี 4 หลักให้เทียบ — ขา manifest นั้นปล่อยไว้เป็นข้อมูลเสริมเฉย ๆ ไม่ใช่ตัวตัดสิน |
| 8 | `test:prep` ไม่อยู่ใน verify | Task 23: แทรก `node test/prep-stock-test.js` เป็นขั้นที่ 16 ของ `npm run verify` (หลัง `docs-test`) + `STEP_LABELS`/`.githooks/pre-push`/docs ผ่าน `tools/gen-docs.js` |
| 9 | bad-chart ผสมฐาน gate มองไม่เห็น (E36 ผิดพร้อมกัน) | Task 9 (PR #32): field `f41` (กรอบ 52 สัปดาห์) ได้ binding `pair` → f01 (contains · stale) ⇒ ยิงเป็น **W23** |
| 10 | ห้ามวาง shares ถัวเฉลี่ยลง prompt (CAMT) | Task 24: โค้ด (`SHARES_LABEL`/`SHARES_NOTE` ใน `printFinancialTable`) มีอยู่แล้วตั้งแต่ 17 ส.ค. 69 แต่ไม่มีเทสคุม — เพิ่ม `test/prep-stock-test.js` ยืนยัน `F.SHARES_LABEL === 'Shares(wAvgDil)'` และจับ stdout ของ `printFinancialTable` (export เพิ่มจาก `tools/fetch-fundamentals.js`) ว่าพิมพ์ `SHARES_NOTE` จริงเมื่อตาราง [3] มีแถว shares (fixture `amataFin`) และไม่พิมพ์เมื่อไม่มี |
| 13 | BBL ช่องสรุป "+2.1% (เกือบเต็มมูลค่า)" เครื่องหมาย/คำขัดกัน | ปิดเชิงกลไก (Task 11) · คลังกวาดแล้ว (Task 12 · 861 ใบ · W06 = 0) |
| 19 | W06 ยิง 553/908 ก่อน 17 ส.ค. — เดิมต้องให้คนแก้เพราะ cron ไม่แตะคำ | ปิดเชิงกลไก (Task 11) · คลังกวาดแล้ว (Task 12 · 861 ใบ · W06 = 0) |
| 22 | คิว price-flags เป็น snapshot — pre-patch ด้วย --force ล้าง flag ก่อนวิเคราะห์ ทำให้สถานะรอบอยู่แค่ใน .queue/state.json (per-machine) — ต้องการคิวถาวรใน repo | Task 15: state ย้ายไป `<checkout หลัก>/.queue` (`resolveQueueDir` ใน `tools/queue/state.js` ใช้ `git rev-parse --git-common-dir`) — ข้าม worktree ✓ ข้ามเครื่อง ✗ (ถ้าใช้หลายเครื่องต้องย้าย state เข้า repo) |
| 23 | lockfile ไม่มี heartbeat — holder ที่ทำงานเกิน 10 นาทีจะถูกยึด lock และตอนปล่อยจะลบ lock ของคนใหม่ (ยังไม่เกิดเพราะไม่มี holder ทำงานยาวใต้ lock) | Task 4 (PR #31): `tools/lockfile.js` — holder async ได้ heartbeat จน settle แล้วค่อยปล่อย lock (holder sync ยังห้ามยาวตามเดิม) |

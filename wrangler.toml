# Cloudflare Pages config
# โปรเจกต์นี้เป็น "Pages project" → ต้อง deploy ด้วย `wrangler pages deploy`
# (ห้ามใช้ `wrangler deploy` ซึ่งเป็นของ Workers)
# docs: https://developers.cloudflare.com/pages/configuration/build-configuration/

name = "stock-analysis"
pages_build_output_dir = "dist"
